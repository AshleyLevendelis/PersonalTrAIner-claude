# Local dev defaults to the TEST project (vswuurrtbzbrgubddefv), matching
# the Supabase CLI's default link (see CLAUDE.md's Database section, built
# 11 Aug 2026). The live app is unaffected by this file — Vercel's own
# dashboard env vars (Production/Preview) override it at build time.
# To point local dev at production instead, swap in sdkhuczcfnqqimdgfiks's
# URL/anon key temporarily; don't commit that swap.
VITE_SUPABASE_URL=https://vswuurrtbzbrgubddefv.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_0KR_ugIxVFSZ3r88-BCrmw_tBDm-XVH
