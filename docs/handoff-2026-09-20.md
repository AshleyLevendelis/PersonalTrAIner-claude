# What has to happen on Ashley's machine — 20 Sep 2026

Supersedes `handoff-2026-09-16.md`.

**Branch: `claude/coach-empty-text-fix-2c4bzv`**, fully pushed, working tree
clean. No commit hash is quoted anywhere in this file, deliberately: the last
three handovers went stale within the hour because writing the work down is
itself a commit. The branch NAME is the stable anchor — read the head off
origin and use whatever you find.

**THE PRE-MERGE SWEEP IS GREEN, AND WAS RE-RUN AFTER THE REST-DAY WORK.**
275 of 275 gates RAN. 272 passed. The three failures are named and accounted
for below; none of them can pass on a cloud machine.

The earlier run recorded here (273 gates, 269 passed, one real failure in
`test:bounds-and-boundaries`) is superseded: that failure was fixed in
`8d44366` and the gate passes in this run. The gate count rose by two because
the rest-day rebuild added `test:rest-day-card` and `verify:rest-day`.

**MERGED TO MAIN** on Ashley's explicit word, 20 Sep 2026, after this sweep.

---

## The state, measured against origin/main rather than recalled

Two of these numbers were reported wrong to Ashley earlier today and are
corrected here by re-deriving them from the diff:

| What | Count | Derived from |
|---|---|---|
| Migrations this branch ADDS | **2** | `git diff --name-status origin/main...HEAD -- supabase/migrations/` |
| Edge functions this branch CHANGES | **1** (`chat-gemini`) | same, over `supabase/functions/` |

The two migrations are `20260919140000_add_batch_cooking` and
`20260919160000_add_drop_index_to_set_logs`. A third,
`20260919120000_add_meal_prep_method`, is already ON MAIN as a file — whether
it has been APPLIED to the two databases is a different question, and
`db:push-both` answers it by applying whatever is unapplied.

`generate-meals` is byte-identical to main on this branch, so **this branch
does not require its deploy.** Earlier work may still: that is a question about
what is live, which no cloud session can see.

---

## Why the drop-index migration is not optional, and is not only about drops

`20260919160000` REPLACES the `unique_set_per_session` constraint with one that
includes `drop_index`. Both the browser client and `chat-gemini` name that
constraint by column list when they upsert a set.

Until the migration runs, a drop set is saved on the device, shown on the card,
and held in the offline queue rather than written over its parent — it syncs
itself once the migration lands.

**The order matters in one direction only.** Both writers were built to work on
BOTH sides of the migration: they try the new conflict target and fall back to
the old one on 42P10 alone. So neither order breaks anything. What WOULD have
broken — and is the reason this is called out — is deploying the OLD
`chat-gemini` after running the migration: the old one names only the old
constraint, matches no unique index, and **every set the coach logs fails**.
So: if you run the migration, deploy `chat-gemini` too.

---

## The steps

Claude Code can do steps 1, 2 and 5. It **cannot** do 3 or 4: `db:push-both`
and `deploy:functions:prod` both refuse when stdin is not a terminal
(`db-target.mjs` checks `isTTY`), deliberately, so a wrong-target command costs
a typed phrase. Claude Code hard-fails on them — hand it the safe half.

```
Read docs/handoff-2026-09-20.md in this repo, then do steps 1, 2 and 5 below.
Stop and tell me when you reach step 3 — I type those myself.

GOAL: get this branch's two migrations onto both databases and the coach
function deployed, then merge. The branch is claude/coach-empty-text-fix-2c4bzv.

1. git fetch origin && git checkout claude/coach-empty-text-fix-2c4bzv
   && git pull origin claude/coach-empty-text-fix-2c4bzv && npm install
   Report the head commit you land on. Do not assume the branch is where a
   document says it is.

2. VERIFY, don't trust a summary. Run these and read the output back to me:
      npx tsc --noEmit
      npm run test:drop-sets
      npm run test:coach-plan-context
      npm run verify:drop-sets
   The last one opens a real Chromium at phone size and logs a set, adds a
   drop, and saves it. It must print "38 checks ran" and "All drop-set screen
   checks passed." A shorter run is a crash, not a pass — say so if you see one.

3. ASHLEY TYPES THIS, NOT YOU: npm run db:push-both
   What proves it worked is NOT the words "remote database is up to date" —
   that prints identically whether the work happened or the file was missing
   from the checkout. What proves it is that BOTH databases list
   20260919160000_add_drop_index_to_set_logs among their applied migrations.
   After she runs it, you run `npm run test:schema-parity` and read back
   whether both projects report the identical migration set.

4. ASHLEY TYPES THIS TOO: npm run deploy:functions:prod -- chat-gemini
   It asks for the phrase yes-production and names the target on the deploy.
   Do this AFTER step 3, for the reason in the section above.

5. ALREADY DONE — main already carries this work; Ashley said "merge it" on
   20 Sep 2026 and the merge was pushed. Confirm it rather than redo it:
   `git log --oneline -1 origin/main` should be the merge commit. Steps 3 and
   4 are the only ones still outstanding.
```

---

## The three sweep failures, each read by its own output rather than its name

- `test:meal-quality` — *"Host not in allowlist: …supabase.co"*. Needs a live
  database. Environmental.
- `verify:rls` — same sentence, and it refuses to print PASSED over an
  unreachable database: *"INCONCLUSIVE… this run proves nothing."* Environmental.
- `test:schema-parity` — **does NOT print the allowlist sentence**, which is a
  correction to CLAUDE.md's own note. It prints *"Failed to link to TEST (…).
  Nothing was run against it."* Same honest shape, different words.
  Environmental — and it is the gate step 3 above turns into a real answer.

`test:bounds-and-boundaries` was the one real failure in the EARLIER run and is
green in this one. A check pinned the exact text of a render condition; the
cable-machine work added a clause to it, strictly narrower, so the property
never weakened and the pin went red at correct code. Re-anchored on the
property, 3 mutations, 3 caught.

---

## THE COACH EXAM — the run, and the thing that nearly got it wrong

Ashley chose this as the next piece of work, 20 Sep 2026. It is the only one
of the three promises with nothing measuring it.

**THE EXAM DOES NOT CALL PRODUCTION.** `run-coach-exam.mts` points at whatever
`.env.local` points at, which is TEST, and its own header says so. So
`deploy:functions:prod -- chat-gemini` alone would leave the exam grading a
STALE coach on the test project, and every score would be about code nobody is
running. This was nearly handed over wrong.

**The test deploy needs no typed phrase.** `deploy-functions.mjs` gates only
`target === PROD_REF`; the test path runs straight through. So Claude Code can
do it — only the production deploy and `db:push-both` need Ashley.

The exam prints the project ref before its first call, deliberately, so a run
against the wrong one is visible rather than inferred. Read it back.

### The order, and why each step is where it is

```
Read docs/handoff-2026-09-20.md, then do steps 3, 5, 6 and 7. Steps 1 and 2
are Ashley's — stop and tell me when you reach them.

GOAL: the first coach exam scores measured against the CURRENT coach. There
is exactly one previous run, 13 Sep, and it measured a coach and a grader
that have both since been rebuilt, so it is history and not a baseline.

Work on branch claude/coach-empty-text-fix-2c4bzv — the three newest exam
cases are there and not on main. Report the head commit you land on.

1. ASHLEY TYPES: npm run db:push-both
2. ASHLEY TYPES: npm run deploy:functions:prod -- chat-gemini
3. YOU RUN (no phrase needed): npm run deploy:functions:test -- chat-gemini
   Without this the exam grades a stale coach. This is the step that is easy
   to skip and impossible to notice afterwards.
4. Sanity, free: npx tsx scripts/run-coach-exam.mts --dry
   It must list 23 cases. Fewer means you are on the wrong branch.
5. THE RUN, which costs money: npx tsx scripts/run-coach-exam.mts
   Read back the project ref it prints BEFORE the first call. If it is not
   the test project, stop and say so rather than continuing.
6. npm run coach-exam:grade   (needs ANTHROPIC_API_KEY in the environment)
7. Report back, and do NOT round off the bad news: how many of the 23 cases
   breached a hard rule, WHICH rule each time, and the five judged
   dimensions. Quote the coach's own words for every breach — a rule name is
   not a finding, it says a property does not hold and not which way it
   broke. Then commit the scores, because a run nobody pushed is a run this
   repository cannot see, which is exactly what happened on 13 Sep.
```

### What the run is being asked, that it was not asked before

Three cases were added on 20 Sep, each grading a ruling Ashley had ALREADY
given, so nothing new was decided in order to write them:

- `scope-today-not-forever` — "40 minutes this morning" then "40 from now on".
  The figure is IDENTICAL on both turns on purpose: the tools differ on SCOPE
  and a coach keying on the number rebuilds a whole block for somebody who was
  running late once.
- `cardio-asks-before-carding` — the coach must ASK on turn one and card only
  on the yes. Her 15 Sep ruling is enforced in code in one direction only; the
  direction with no code behind it is this one.
- `goal-change-says-the-food-half` — the goal is the only setup answer feeding
  both the plan and the calories.

A tenth hard rule, `wrong-proposal-kind`, grades WHICH card appeared rather
than whether one did. 12 new checks, 10 mutations, 10 caught.

### The floor is still deliberately unset

There is no pass mark, and one should be proposed FROM THIS RUN'S NUMBERS
rather than guessed beforehand. Setting it in advance would either be met by
construction or fail for reasons nobody had looked at yet.

---

## What is NOT done, named rather than omitted

- **The target-weight block** from the ramp/working-sets handoff. Its
  specification is in a section of the handoff document that is not in this
  repository — `design_handoff_ramp_vs_working_sets/` does not exist here, and
  the plan doc records only the item's name. Building it would mean inventing
  what it says. Needs that section.
- **The coach exam against the current coach.** It needs credentials no cloud
  session has. The only run was 13 Sep, on Ashley's machine, and it measured a
  coach and a grader that both no longer exist. Until it runs again after the
  `chat-gemini` deploy, "best-in-class advice" is asserted, not known.
- **Logging a drop by chat.** `screen only`, with its reason written into
  `docs/coach-screen-parity.md`: a drop needs a parent set, and "I did a drop
  after squats" does not say after which one.
