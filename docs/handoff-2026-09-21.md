# What has to happen on Ashley's machine — 21 Sep 2026

Supersedes `handoff-2026-09-20.md`.

**Branch: `claude/coach-empty-text-fix-2c4bzv`**, fully pushed, working tree
clean. No commit hash quoted here, deliberately — read the head off origin.

**THE PRE-MERGE SWEEP IS GREEN.** `test:audit` 17,423/17,423 (100%).
`test:quality` 11.63/12 average, 0/9216 below the 7.2 floor. 70 directly
affected gates re-run clean, `npx tsc --noEmit` clean. Full details in
BACKLOG.md's newest two entries.

**Not yet asked of Ashley today**: merging this branch to `main`. That still
needs her explicit word, separately from the machine work below.

---

## What this branch needs, derived from the diff rather than recalled

| What | Count | Derived from |
|---|---|---|
| Migrations this branch ADDS vs `origin/main` | **3** | `git diff --name-status origin/main...HEAD -- supabase/migrations/` |
| Edge functions this branch CHANGES vs `origin/main` | **2** (`chat-gemini`, `generate-meals`) | same, over `supabase/functions/` |

The three migrations: `20260919120000_add_meal_prep_method`,
`20260919140000_add_batch_cooking`, `20260919160000_add_drop_index_to_set_logs`.
Whether any are already applied to TEST or PRODUCTION is a question only
`db:push-both` can answer — it applies whatever is unapplied and is a no-op
for whatever isn't.

Both edge functions differ from `main`: `generate-meals` picked up a small
change since the 20 Sep handoff (which had reported it byte-identical then —
that is no longer true). `chat-gemini` carries the coach-exam-relevant tool
and prompt work from the last two days.

---

## Why this matters more than usual: the coach exam

The test that grades whether the coach gives good advice (`coach-exam:run` +
`coach-exam:grade`) has never been run against the real, currently-deployed
coach — only once, on 13 Sep, against a version three rulings out of date.
It cannot be run meaningfully until `chat-gemini` reflects what is actually
on this branch. This is the reason today's handover is worth doing now
rather than batching it with the next thing.

---

## The prompt for Ashley's local Claude Code session

```
Pull the branch claude/coach-empty-text-fix-2c4bzv and read
docs/handoff-2026-09-21.md in full before doing anything.

Then, in order:

1. Confirm the branch and working tree: `git status`, `git log -1 --oneline`.
   Should be clean, HEAD matching origin.

2. Run `npm run db:push-both`. This pushes to TEST first, then asks you to
   type `yes-production` before touching PRODUCTION, then relinks back to
   TEST when done, success or failure. It applies the 3 pending migrations
   for this branch if they aren't already applied — it's safe to run even if
   some or all are already in, it just does nothing for those.
   VERIFY: the command's own final output says both TEST and PRODUCTION are
   up to date. If anything about the drop_index migration looks like it
   failed, stop and tell me exactly what it printed rather than retrying.

3. Deploy chat-gemini: `npm run deploy:functions:prod -- chat-gemini`, typing
   `yes-production` when it asks. Then generate-meals the same way:
   `npm run deploy:functions:prod -- generate-meals`.
   VERIFY: each deploy command's own success output, and note the function
   version number it reports for each — I'll want both.

4. Once both are deployed, run the coach exam for real (this costs money and
   needs your Gemini credentials, which is why it can't run from a cloud
   session): `npm run coach-exam:run` (NOT --dry) followed by
   `npm run coach-exam:grade`.
   VERIFY: the grader's own summary — how many of the 23 cases passed, which
   of the ten hard rules (if any) were breached, and the five judged
   dimensions against docs/coach-exam-rubric.md. Save the raw output; don't
   just paste the last line.

5. Report back: whether db:push-both succeeded cleanly, both functions'
   deployed version numbers, and the coach exam's full scorecard (not a
   summary of a summary — the actual numbers per rule and per dimension).
   If the exam finds real breaches, that's the useful outcome, not a failure
   of the process — say what it found plainly.

Do not merge to main as part of this — that's a separate step Ashley
authorizes on her own.
```

## What I cannot verify from here

Whether the migrations actually land, whether the deployed function versions
actually match this branch, and what the coach exam actually says about the
live coach — all three need her machine's credentials and network access,
which a cloud session does not have. Report those back as measured, not
assumed.
