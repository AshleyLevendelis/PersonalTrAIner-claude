# What has to happen on Ashley's machine — 16 Sep 2026

Supersedes `handoff-2026-09-15.md`.

**REVISED 16 Sep, evening**, after the local Claude Code session read the first
version of this file and reported back. Three things in it were wrong, and all
three are corrected below rather than patched over:

1. **Step 1 was already done.** The migration is applied on both databases. The
   local session verified it the right way — comparing the applied migration
   sets BY NAME (56 on each, identical), not off an "up to date" line, which is
   exactly what the old step 1 warned about. Step 1 is struck out below.
2. **The commit numbers were stale.** `56f17d1` / 27 commits was true when
   written and was superseded an hour later.
3. **Step 4 was not the first run.** The exam HAS been run once — 13 Sep, on
   Ashley's machine, one local commit that was never pushed. This file, and
   CLAUDE.md, said "NEVER RUN" for three days. See step 4.

**Current state, verified against origin 16 Sep:** branch
`claude/coach-empty-text-fix-2c4bzv`, fully pushed, working tree clean, ahead
of `main` (`7d6c7e6`) by thirty-odd commits.

**THIS FILE NO LONGER QUOTES THE HEAD COMMIT, AND THAT IS DELIBERATE.** It has
gone stale three times in two days — twice within the hour of being written —
because writing the work down is itself a commit. A number that is wrong more
often than right is worse than no number: it invites the reader to check the
wrong thing. The branch NAME is the stable anchor, and the instructions below
ask the local session to read the head off origin and use whatever it finds.

**THE SWEEP IS GREEN.** 244 of 244 gates ran — the same number the project
declares, so nothing dropped out of enumeration — and 3 failed, all three the
known ones that cannot pass from a cloud machine: every one of them names
`Host not in allowlist: vswuurrtbzbrgubddefv.supabase.co`, i.e. this machine
cannot reach the TEST database at all. Plan quality re-scored at **11.56 / 12
with 0 of 9,216 profiles below the 7.2 floor**, unchanged. Step 2 is cleared to
go.

**Order matters and is not arbitrary.** The `chat-gemini` deploy ships from
whatever is checked out, so deploying before the merge would put branch code
live while `main` says something else. And the coach exam calls the DEPLOYED
function, so running it before the deploy grades the old coach.

Paste the block below into Claude Code in VS Code.

---

```
I'm working in my local clone of PersonalTrAIner. A cloud Claude Code session
has pushed work to the branch claude/coach-empty-text-fix-2c4bzv. Its full
gate sweep is green: 244 of 244 ran, 3 failed, and all 3 fail only because that
machine cannot reach the test database.

DO NOT expect a commit hash from me — this file deliberately stopped quoting
one, because writing the work down is itself a commit and the number was wrong
more often than right. Start with `git fetch origin` and read
`git rev-parse --short origin/claude/coach-empty-text-fix-2c4bzv` yourself;
that is the head, whatever it is, and it is what you merge. Three things have to happen
here, on my machine, and one of them you CANNOT do — it refuses when stdin is
not a terminal, on purpose, so a wrong-target command costs a typed phrase. Do
the parts you can, stop at each typed confirmation and tell me exactly what to
type, then read the output back to me and tell me whether it actually worked.

STEP 1 — DONE ALREADY, DO NOT REPEAT

Migration 20260910160000_add_marked_missed is applied on both TEST and
PRODUCTION. This was verified on 16 Sep by comparing the applied migration sets
by name on both projects — 56 each, identical. Nothing to do. If you are reading
an older copy of this file that asks you to run `npm run db:push-both`, ignore
it.

STEP 2 — MERGE TO MAIN (cleared: the sweep came back green)

WHY: the branch carries seven pieces of work.
  1. An empty card I reported: the beginner's walking plan prescribes a walk
     with minutes, an effort target and a reason, and no screen read any of it.
  2. The coach can put a cardio session on the plan, and only offers when I
     sound definite about it.
  3. One voice: every card, receipt and refusal the app writes now comes from
     one phrasebook instead of being typed out wherever it was needed.
  4. Three mechanical bugs — an unbounded query, three day-counts that
     miscount around a clock change, and one piece of save logic in three
     copies.
  5. My calorie and protein targets now say what they moved FROM as well as to,
     and cover protein/carbs/fat rather than calories alone.
  6. "Give me a different session today" — new, on both the day menu and the
     chat. My main lift stays exactly as prescribed; everything else changes.
  7. Checks behind two claims that had none: that the plan is genuinely ranked
     rather than shuffled, and that the target notice cannot silently stop.

Frontend ships on merge, via Vercel. Do this:
  git fetch origin
  git checkout main && git pull origin main
  git merge origin/claude/coach-empty-text-fix-2c4bzv
  git push origin main

WHAT TO VERIFY: after pushing, confirm `git rev-list --count origin/main..origin/claude/coach-empty-text-fix-2c4bzv`
is ZERO — that is the merge having actually taken everything, and unlike a
hash comparison it stays true however many commits the branch gained while you
were reading this. Then tell me to check Vercel has built.
Do NOT report "merged" off a command that printed "Already up to date" — that
means the merge did nothing, and it is the most likely way this silently fails.

EXPECT ONE CHECK TO GO RED THE MOMENT YOU MERGE, and do not treat it as a
defect: `npm run test:coach-exam-fresh` fails when the coach's code no longer
matches the exam scores on record. The merge changes the coach; step 4 clears
it. That is the gate doing its job.

STEP 3 — DEPLOY THE COACH (only after step 2 has pushed to main)

WHY: FOUR changes to the coach are now sitting undeployed, not two.
  - Swapping a session for an activity ASKS before it writes (it was the only
    day-verb that wrote immediately).
  - A server-side guard stopping the coach claiming it changed something when
    no tool ran.
  - The coach can offer to add a cardio session, and refuses to when I only
    mentioned something in passing.
  - The coach can rebuild today's session around my main lift.

YOU CANNOT RUN THIS — isTTY refusal. The command is:
  npm run deploy:functions:prod -- chat-gemini
Tell me to run it and I'll type `yes-production`.

WHAT TO VERIFY: the deploy names its target on the deploy itself — read that
line back to me and confirm it says the PRODUCTION project
(sdkhuczcfnqqimdgfiks), not test. A named target prints the same whether new
code shipped or the file was unchanged, so also confirm the function version
number went UP.

STEP 4 — RUN THE COACH EXAM AGAINST THE COACH WE JUST SHIPPED (only after 3)

THIS IS NOT THE FIRST RUN, and the correction matters. There is one local
commit on this machine from 13 Sep holding an exam run that was never pushed.
KEEP IT — push it with the merge. It is the only record of what the exam said
before any of this was fixed, and the before/after comparison is the whole
value of having run it.

BUT DO NOT READ ITS 7-OF-20 AT FACE VALUE. Two of those breaches were "the
coach replied with no text at all", and that is a false positive that was
fixed on 16 Sep: when the coach correctly offers a change it shows a card and
says nothing in words, and the 13 Sep runner recorded no card — so the rule
named `silence` fired on a coach that had behaved correctly. On 13 Sep the
coach returned an empty reply beside a card in 28 separate places, measured at
commit 29221cf. A third breach is likely stale rather than real: the
exercise-ban tool was still a declining stub on 13 Sep and was wired the next
day. So the 13 Sep scores grade a coach AND a grader that both no longer
exist. The number to act on is this run's.

You CAN run this:
  npm run coach-exam:run
  npm run coach-exam:grade

It needs .env.local present (it reads VITE_SUPABASE_URL and
VITE_SUPABASE_ANON_KEY from there — it points at whatever that points at, which
should be TEST). The five judged dimensions additionally need ANTHROPIC_API_KEY
set; without it the grader still runs the NINE hard rules in code and prints
"judge: NOT RUN" for the rest. Tell me which half actually ran. It costs money
per run, so run it once and stop.

WHAT TO VERIFY: read me the Tier A (hard rules) result and the Tier B score,
and tell me whether coach-exam-scores.json now has a ranAt timestamp from
TODAY rather than 13 Sep. Then tell me, case by case, which breaches survive
from the 13 Sep run and which have gone — that comparison is the deliverable,
not the raw count. The floor is deliberately unset — these numbers are what we
use to propose one, so don't invent a pass/fail verdict.

FINALLY: run `npm run test:coach-exam-fresh` and read it back. It fails when
the coach on disk no longer matches the scores on record, so a green there is
what says the exam result actually describes the coach we just deployed.
```
