# What has to happen on Ashley's machine — 16 Sep 2026

Supersedes `handoff-2026-09-15.md`, which is now stale in its numbers (it says
13 commits at `aeedc4a`) though the four steps and their order are unchanged.

**Current state:** branch `claude/coach-empty-text-fix-2c4bzv` at `56f17d1`,
**27 commits** ahead of `main` (`7d6c7e6`), working tree clean, verified
against origin.

**Order matters and is not arbitrary.** The `chat-gemini` deploy ships from
whatever is checked out, so deploying before the merge would put branch code
live while `main` says something else. And the coach exam calls the DEPLOYED
function, so running it before the deploy grades the old coach.

Paste the block below into Claude Code in VS Code.

---

```
I'm working in my local clone of PersonalTrAIner. A cloud Claude Code session
has pushed work to the branch claude/coach-empty-text-fix-2c4bzv (head 56f17d1,
27 commits ahead of main). Four things have to happen here, on my machine, and
two of them you CANNOT do — they refuse when stdin is not a terminal, on
purpose, so a wrong-target command costs a typed phrase. Do the parts you can,
stop at each typed confirmation and tell me exactly what to type, then read the
output back to me and tell me whether it actually worked.

STEP 1 — THE MISSING MIGRATION (can be done now, independent of everything else)

WHY: migration 20260910160000_add_marked_missed.sql has been committed for days
and has never been pushed to either database. It backs "I missed that session" —
the app recording a missed day as a fact, distinct from a rest day. The code
shipped; the column did not. Until this runs, that write fails against the live
schema.

YOU CANNOT RUN THIS. `npm run db:push-both` checks isTTY and hard-fails for you.
Tell me to run it myself. It pushes TEST first, then asks again for PRODUCTION,
and relinks back to TEST when it finishes either way. I will type
`yes-production` when it asks.

WHAT TO VERIFY, not just that it exited 0: "Remote database is up to date"
prints identically whether the migration ran or was absent from the checkout.
After I run it, have me run `npm run test:schema-parity` and read it back — it
compares the applied migration set on BOTH projects and is the thing that
actually proves it landed on production and not only on test.

STEP 2 — MERGE TO MAIN (wait for my go-ahead — the cloud session is running a
full 244-check sweep and will tell me when it is green)

WHY: the branch carries seven pieces of work.
  1. An empty card I reported: the beginner's walking plan prescribes a walk
     with minutes, an effort target and a reason, and no screen read any of it.
  2. The coach can put a cardio session on the plan, and only offers when I
     sound definite about it.
  3. One voice: every card, receipt and refusal the app writes now comes from
     one phrasebook instead of being typed out wherever it was needed.
  4. Three mechanical bugs — an unbounded query, three day-counts that
     miscount around a clock change, and one piece of save logic in three
     copies.
  5. My calorie and protein targets now say what they moved FROM as well as to,
     and cover protein/carbs/fat rather than calories alone.
  6. "Give me a different session today" — new, on both the day menu and the
     chat. My main lift stays exactly as prescribed; everything else changes.
  7. Checks behind two claims that had none: that the plan is genuinely ranked
     rather than shuffled, and that the target notice cannot silently stop.

Frontend ships on merge, via Vercel. Do this:
  git fetch origin
  git checkout main && git pull origin main
  git merge origin/claude/coach-empty-text-fix-2c4bzv
  git push origin main

WHAT TO VERIFY: after pushing, confirm `git log --oneline -1 origin/main` shows
56f17d1 or a merge commit above it. Then tell me to check Vercel has built.
Do NOT report "merged" off a command that printed "Already up to date" — that
means the merge did nothing, and it is the most likely way this silently fails.

STEP 3 — DEPLOY THE COACH (only after step 2 has pushed to main)

WHY: FOUR changes to the coach are now sitting undeployed, not two.
  - Swapping a session for an activity ASKS before it writes (it was the only
    day-verb that wrote immediately).
  - A server-side guard stopping the coach claiming it changed something when
    no tool ran.
  - The coach can offer to add a cardio session, and refuses to when I only
    mentioned something in passing.
  - The coach can rebuild today's session around my main lift.

YOU CANNOT RUN THIS either — same isTTY refusal. The command is:
  npm run deploy:functions:prod -- chat-gemini
Tell me to run it and I'll type `yes-production`.

WHAT TO VERIFY: the deploy names its target on the deploy itself — read that
line back to me and confirm it says the PRODUCTION project
(sdkhuczcfnqqimdgfiks), not test. A named target prints the same whether new
code shipped or the file was unchanged, so also confirm the function version
number went UP.

STEP 4 — RUN THE COACH EXAM FOR THE FIRST TIME (only after step 3)

WHY: the exam is 20 realistic conversations and 37 turns, graded against a
written rubric. Built on 13 Sep, and it HAS NEVER BEEN RUN, because it needs
credentials a cloud session doesn't have. Until it runs once, "the coach gives
best-in-class advice" is asserted, not known.

A REAL DEFECT IN IT WAS FIXED ON 16 SEP, before you spend money on it: when the
coach correctly offers a change it shows a card and says nothing in words, and
the exam recorded that as the coach having said nothing at all — then flagged it
as a rule breach. It would have scored the coach worst on the turns where it
behaved best. Fixed, along with three things that would have cost money or time
on the first run.

It must run AFTER step 3 or it grades the coach that's currently live, not the
one we just shipped.

You CAN run this one:
  npm run coach-exam:run
  npm run coach-exam:grade

It needs .env.local present (it reads VITE_SUPABASE_URL and
VITE_SUPABASE_ANON_KEY from there — it points at whatever that points at, which
should be TEST). The five judged dimensions additionally need ANTHROPIC_API_KEY
set; without it the grader still runs the NINE hard rules in code and prints
"judge: NOT RUN" for the rest. Tell me which half actually ran. It costs money
per run, so run it once and stop.

WHAT TO VERIFY: read me the Tier A (hard rules) result and the Tier B score, and
tell me whether coach-exam-scores.json now has a real ranAt timestamp instead of
null. The floor is deliberately unset — the first run's numbers are what we use
to propose one, so don't invent a pass/fail verdict.

FINALLY: after all four, run `npm run test:coach-exam-fresh` and read it back.
It fails when the coach on disk no longer matches the scores on record, so a
green there is what says the exam result actually describes the coach we just
deployed.
```
