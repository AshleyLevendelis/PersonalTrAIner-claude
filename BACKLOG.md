# Backlog

Newest first. One line each.

- [x] **"I DON'T LIKE THE 'I'M NEW / NOT SURE' — JUST SAY NOT SURE. AND A BEGINNER SHOULD ALWAYS GET A CALIBRATION WEEK."** Ashley, on the live onboarding card. The label called someone new for not having tested lately, and tapping the *other* option set `skip_calibration_week` **whatever experience they had just reported** — so a self-described beginner could start heavy on a figure nobody had checked. Label is now **"Not sure"**, and `generateMesocycle` refuses to skip calibration for a beginner. Measured across all four tiers: beginner week 1 `isCalibrationWeek: true` at 72.5 kg heaviest; novice/intermediate/advanced unchanged at 75 kg and still skipping.

- [x] **THEN SHE WENT FURTHER: "A BEGINNER SHOULDN'T BE ASKED THEIR NUMBERS."** Offered the narrower option — ask anyway, and tell them week 1 still verifies — she chose not to ask at all. I recommended against it once and she confirmed; her version is the more coherent one, because a beginner's number is precisely the number the engine has already decided it will not prescribe off, so asking and then verifying anyway is a question whose answer we half-distrust. `willBeLiftingBarbells` now excludes every beginner, not just the sedentary ones it already excluded, and the three lift-weight questions close behind it.

- [x] AND IT HOLDS ON **BOTH** SURFACES, WHICH THE CHIP CHECK ALONE COULDN'T SEE. `buildSlotCatalog` filters on the same gate, so the model is never told the question exists and cannot ask it in prose — the route by which a "removed" question keeps getting asked conversationally with no chip guard anywhere near it. Gated as its own check.

- [x] **THE RENAME CREATED A LIVE REGRESSION AND THE GREP FOUND IT.** `STUCK_SIGNAL` matches `/^not sure$/`, and `handleResolveSingle` sends the tapped option's **label** as the message text — so every tap on the new chip read as *"I don't know"* and drew the rescue reply *"No problem — here are the options."*, offered to someone who had just answered plainly. Typing the same two words hit it via the exact-label backstop. The answer was never lost, so this was tone, not data — but it is the exact failure `STUCK_SIGNAL`'s own comment warns against.

- [x] FIXED AS THE CLASS, NOT THE LABEL: the rescue now stands down whenever **this message already recorded an answer**, keyed on that rather than on "was it a tap", so a future label colliding with the stuck vocabulary is covered too. Deliberately a **different flag from `immediateCommit`**, which the allergen backstop also sets for an unrelated reason — sharing it would mean disclosing an allergy suppressed a genuine "I don't know". The gate reports which labels collide rather than banning collisions, because the label is Ashley's call and the handling is ours.

- [x] SEVEN CHECKS ADDED, FIVE MUTATIONS BITE (revert the label, drop either beginner guard, restore the stale comment, remove the rescue guard). The `test-onboarding-slots` source checks **strip comments before matching** — this gate family has been broken *and* silently satisfied by its own explanatory prose more than once, and a rule about code should not be provable by a sentence about it.

- [x] ONE STALE CHECK CORRECTED RATHER THAN DELETED: §13 asserted *asked about lifts ⟺ engine prescribes lifting*. Ashley's ruling deliberately breaks the ⟸ half — an active beginner gets a lifting plan and is still not asked — so the safety direction (*a walks plan is never asked about barbell weights*) is now asserted on its own, and the replaced half is restated as its own rule so it cannot quietly rot back.

- [ ] **A BEGINNER CAN NO LONGER VOLUNTEER THEIR NUMBERS EITHER, AND IT HAPPENS SILENTLY.** The `isSlotApplicable` guard refuses a model-driven write into a closed slot, so *"I already squat 60kg"* from a beginner is dropped with only a `console.warn`. Correct safety direction and it follows from the ruling — but silently discarding a volunteered number is the exact defect the slot-ordering work was done to fix. One line of coach acknowledgement is the obvious answer; it is Ashley's call, not a mechanical fix.

- [ ] THE SEEDLING ICON ON THAT OPTION WAS CHANGED TO `❓` UNPROMPTED — flagging it because it goes past the letter of the instruction. 🌱 is also the Beginner chip's icon, so the option still *said* "new" in pictures after the words stopped. Trivially revertible if Ashley wants it back.

- [x] **635 FORM CUES, WRITTEN, SHIPPED IN EVERY BUNDLE, AND NEVER ONCE SHOWN TO ANYONE.** Ashley: *"there's nowhere in the app to see an exercise."* All **158** catalogue entries carry `form_cues` (~4 each) and all 158 carry `coach_note_swap` — and `form_cues` had **zero readers in the entire repo**: not the UI, not a prompt, not a test. The only route to "how does this go?" was chat typing a **YouTube search-results URL** that leaves the app and needs signal in a gym.

- [x] **"How to do it" now opens from the ⋮ menu**, in today's session and the peek, one shared dialog on the `ExerciseHistoryDialog` precedent. Cues numbered, the rationale that previously appeared only on *other* exercises, muscles, equipment, joint load — joints through `jointListDisplay`, never the raw tag. Plus one honest line: cues are a reminder, not coaching, and anything that hurts is a reason to stop.

- [x] AND SEARCHING THE CATALOGUE NO LONGER TELLS YOU **LESS** THAN BROWSING IT. `SwapDialog`'s ranked candidates carried a coaching sentence; its free-search results carried two taxonomy badges and nothing — on the surface where you are *least* likely to know the movement. Now both.

- [x] **A DEFECT I SHIPPED LAST NIGHT, FOUND WHILE EXPLORING THIS.** `exercise/WeekStrip.tsx` **is dead code — no importers.** `d517738` extracted `week-glyphs.ts` so Home and Exercise could not drift and wired it into **the dead file**; the strip users see (`WeekContextRow.tsx`) kept a private `GLYPH` copy. `test-tab-ownership` asserted *"Exercise's strip IS interactive"* against a component nothing renders — **a gate passing on dead code**, the exact failure the M5 mutation was written to catch, reached from a direction nothing checked.

- [x] IT WAS HIDING A LIVE ACCESSIBILITY BUG TOO: `WeekContextRow`'s `aria-label` interpolated the **raw state**, so a screen reader announced *"Monday: before_plan"* — an identifier. `STATE_LABEL` was written to fix exactly that, and the fix went into the copy nobody renders. Both files deleted, the live strip re-pointed, and the gate now **requires every strip file it speaks for to be imported by something**.

- [x] FOUR MUTATIONS BITE — and two of them only after the mutation caught **my own checks** being wrong: one matched the same prop names in a *destructuring* rather than the *forwarding* that makes the menu appear; another passed on `{false && (…)}` because the string was still present in unreachable markup. Both were character-window regexes; the fix was to slice the real function body instead of guessing a distance, which I had got wrong three times.

- [x] **DAYLIGHT'S ACCENT DARKENED TO `#008C72` — Ashley's ruling.** `#00A88A` measured **2.74:1** against its own canvas, under the 3:1 floor the same design brief introduced, so the new theme's first act was warning about its own colour. `#008C72` measures **3.82:1**; the deep step moves to `#00705B` so the CTA gradient keeps its depth instead of going flat.

- [x] **AND MINT'S DARK STEP WITH IT — beyond the letter of the instruction, flagged here because it was.** That value was *also* `#00A88A`, so choosing the Mint chip on Daylight re-created the exact warning the theme fix had just cleared, one tap away. Same colour, same failure, and it only applies to light canvases — of which Daylight is the only one. **All 30 combinations now clear the floor**, up from 28.

- [x] THE GATE'S CLAIM CHANGED SHAPE WITH THE PALETTE. It asserted *"some combination trips the guard"* to prove the guard was not vacuous — a true statement about a palette that shipped a theme warning about itself, and one that went red the moment the palette got better. It now asserts the **stronger** property (every shipped pair clears 3:1) and proves reachability against a **colour chosen to fail**, because "the guard works" and "we ship something broken" are different claims.

- [x] AND A HOLE THE MUTATION FOUND: the css/preview parity check pinned each theme's `--background` and **not its accent** — so `index.css` and `appearance-palette.ts` could disagree about the very value this ruling changed in both files, silently, on the one screen whose job is showing you what you are choosing. Now pinned for all five themes.

- [x] **"I DIDN'T TELL THE APP I COULD DEADLIFT 150 BUT IT CLAIMS I DID."** Ashley's live Exercise tab: **Trap Bar Deadlift 152.5 kg**, labelled **YOU TOLD US**, ramping to 140×1. Her second point is the one that matters: *"150kg is a lot and someone who hasn't specified or shown they can lift that could injure themselves."*

- [x] **THE APP ALREADY HELD THE PROOF AND NEVER LOOKED.** Against its own `STRENGTH_STANDARDS_1RM_PER_BW` for an 86 kg advanced male: the stated **bench of 150 kg is 116% of the advanced one-rep-max estimate** (129 kg) — a *working* weight above what it thinks a top-tier lifter's single max is. And the stated **deadlift ÷ bench = 1.00×** when the **lowest ratio anywhere in that table**, either sex and every level, is **1.67×**. The only validation was `isNumberIn(1, 500)`. The app has a typo guard for what weights you *own* and none for what you can *lift*.

- [x] AND THE NUMBER WAS THEN TRUSTED ABOVE EVERYTHING: `load_source: 'known_weight'` outranks the estimate, skips the starting-light hedge, and skips the calibration week. **One unverified figure became a confident heavy prescription on day one.**

- [x] **ASHLEY'S RULING: "ask once, and never skip calibration on it."** Both halves built. `lift-plausibility.ts` **imports** the standards rather than restating them, so the rule and the prescription can never disagree. Two narrow rules — above the **top-tier** 1RM ceiling (skipped when bodyweight was assumed, since a ratio against a guess proves nothing), and a deadlift under 1.25× bench (needs no bodyweight, so it still protects someone who declined it). **Zero false positives** across five plausible lifters including a bench specialist at 1.4×.

- [x] MEASURED: her week-1 heaviest drops from a **152.5 kg deadlift to a 97.5 kg squat**, and `isCalibrationWeek` flips **true** — while a trustworthy profile still skips calibration and still anchors at 137.5 kg, so the feature is not broken for everyone. Five mutations bite; reverting the calibration guard reproduces **exactly 152.5 kg**, the number from the screenshot.

- [ ] **STILL NO WAY TO CORRECT A STATED LIFT IN THE PROFILE SCREEN.** There is no squat/bench/deadlift row in `ProfileScreen.tsx` — the only route is chat, via `record_goal` → `fact-compiler.ts` → the same columns. Indirect and undiscoverable. Ashley's own profile still carries bench 150 / deadlift 150 until she tells the coach otherwise.

- [x] **"THE TOUR NEEDS UPDATING SINCE WE CHANGED THE APP LAYOUT."** Ashley, and she was right — `test:app-tour` had just passed. §1–2 check every spotlight **target** still exists, which they did; nothing could see that three steps were describing a screen that no longer existed. *"Calories and water"* over a row of **three** tiles; a Nutrition step that never mentioned the steps row that had just moved onto it; a settings step listing everything behind the gear except the two things the gear had gained.

- [x] AND ONE LOOSE END I HAD TIED BADLY MYSELF: the welcome step now promises the tour is *"in the settings menu"*, and the settings step never pointed at it. A promise that dead-ends one screen later.

- [x] EACH CLAIM IS NOW TIED TO THE SOURCE FACT IT DEPENDS ON — not *"the copy says steps"* but *"the copy says steps **if** the tile renders steps"*. So the check fails when the app changes, which is the only moment it matters. Read from `TOUR_STEPS` as **data**, never by grepping the file: two copy checks in this repo have already gone red on their own explanatory comments. Four mutations bite, one per claim.

- [x] **PACKAGES 1 + 2 — HOME RESTRUCTURED, AND ONE FACT NOW HAS ONE OWNER.** Home's order is what's next → morning check-in → progress: date + streak chip, week-strip label, strip, hero, coach bubble, progress, three tiles, weigh-in. Gone: the duplicate `Streak: N days`, the bottom phase line, the standalone consistency line, `6 weigh-ins` (kept at **one**, where the app must admit the trend isn't real), the tomorrow line, and the second `Nutrition ›`.

- [x] **THE SESSION AT A GLANCE, from data that was already there and never shown.** `Prone Y-T Raises · Trap Bar Deadlift · Chest Dips · +3 more` became `6 exercises · ~52 min · trap bar from 92.5 kg`. The minutes come from **the same `estimateDaySeconds` the plan's time cap and `test:audit` use**, so Home cannot disagree with the plan it is describing.

- [ ] **DELIBERATE DEVIATION:** the handoff specifies `bench 92.5 kg **next**` in progress. The app knows the session's heaviest lift but **not the running order**, so "next" would be a claim it cannot support — the same class of invention as a fabricated weight. The figure ships without the word.

- [x] **STEPS MOVED TO NUTRITION.** `steps-target.ts` had already decided it: the step target derives from the same `activity_level` as the calorie target's PAL multipliers, deliberately, so the two "never disagree about who is more active". Home keeps a read-only tile. **Second deviation:** the handoff sketches an inline `edit` for the target, but `daily_step_target` is a profile column with no setter on that path, and a second place to change one number is how two surfaces come to disagree — the row says where the override lives instead.

- [x] **THE WEEK STRIP IS NOW TWO THINGS FROM ONE VOCABULARY.** Home's is the RECORD (26px, no handler, no cursor, not a button); Exercise's is the NAVIGATOR (38px, tap to peek). `src/lib/week-glyphs.ts` is the single source, so a mark cannot come to mean two things — gated in both directions, and `test:coach-promises` was re-pointed at the shared module rather than the file it used to live in.

- [x] Coach bubble replaces the ⚡ caption: 26px avatar matching chat's, the tip and `whatsLeftLine` as one block, and reply chips **keyed to the rule that produced the tip** — `coach-tips.ts` always carried a `key`, `selectCoachTip` just threw it away. Chips **fill the composer and do not send**: a chip that fires a request the moment it is touched turns a suggestion into a command.

- [x] Tools joined the visual system — hairline tab underline instead of a filled 3-up, grocery off bordered `Card`s, 44px stopwatch, and the two lines the tab was missing: *"Your rest timer runs itself inside a session"* and where the grocery list comes from. VISION-ARCHITECTURE gains **§5.1a** with the ownership rule, and §5.1's "owns no number" is amended to name the weigh-in exception rather than left broken. `test:tab-ownership`, four mutations biting.

- [x] **PACKAGE 3 — APPEARANCE. THE BUG FIRST: there was no way back to your theme's own accent.** `AccentOverride` allowed `'theme'` and `DEFAULT_APPEARANCE.accent` **was** `'theme'`, but `ACCENT_OPTIONS` listed only the five hues — so tapping any accent was permanent short of clearing localStorage. A state the type allowed and no control could reach. **"Match theme"** is now the first chip, default-selected, its swatch showing the *current* theme's accent so it moves when the theme does.

- [x] **DAYLIGHT — the first light canvas**, plus retuned Ember and Field. Field mattered most: its old `#D6DF6B` sat in the **same hue family as its own canvas**, so the one element that must read as "tap this" was the closest thing to the background. New accent set (mint/coral/violet/sky/lime) with stored values migrated — `orange→coral`, `yellowgreen→lime`, `blue→sky`, `purple→violet`, anything unreadable → `theme` rather than a guessed hue.

- [x] A LIGHT CANVAS CHANGES TWO THINGS, BY RULE NOT BY COMBINATION. `data-canvas="light"` drives both the accent's dark step and the glow clamp, so a future light theme inherits both **without a line of CSS**. Glow is clamped at *resolve* time only — pick Daylight and Full becomes Subtle, switch back and Full returns, because the preference was never overwritten.

- [x] **THE SHEET NOW SHOWS YOU THE CHOICE BEFORE YOU MAKE IT.** 90 combinations were picked from inside a dialog covering the app it repaints. A live Home preview sits above the controls, painted from explicit theme values — necessary because the sheet must render a theme it is **not wearing**, and custom properties cannot be read from a theme that was never applied. `appearance-palette.ts` duplicates `index.css` for that reason and a gate asserts the two agree.

- [x] **THE HANDOFF'S CONTRAST EXAMPLE IS WRONG, AND DAYLIGHT TRIPPED ITS OWN WARNING — now fixed on Ashley's ruling (`#008C72`).** Its verification step says the guard fires on *"Field + Lime … 2.4:1"*. Measured: **Field + Lime is 13.9:1** — one of the highest-contrast pairs in the set, and high *because* of the retune the same document specifies. Swept all 30 combinations: exactly **two** fall under 3:1, both **Daylight + deep mint at 2.7:1** — which is Daylight's own default. So a user picking the new theme sees an amber warning about the theme's shipped colour. `#008C72` (its existing `--theme-primary-2`) would measure ~3.5:1 and clear it. **Not changed unilaterally** — it is a token the handoff specifies. Ashley's call.

- [x] **THE QUESTION-BESIDE-THE-BUTTON FIX ONLY COVERED ONE OF TWO PATHS, AND ASHLEY PHOTOGRAPHED THE OTHER.** The review opens by two independent routes: the model calling `complete_onboarding` (swept) and **the client's own safety net**, an effect that fires the moment `readyToGenerate` flips whether or not the model called anything (**not swept**). That net exists because the conversation once went silent on the final answer with no way forward, so it can never be removed — it simply had no sweep of its own. Same *"built in two halves"* defect this repo keeps producing.

- [x] IT NEEDED ITS OWN ENTRY POINT, not a second call to the same function: the net holds the **whole transcript**, and sweeping that would rewrite questions answered ten turns ago into a wall of identical closing lines. `closeOutTrailingQuestions` cuts at the last user message and sweeps only the tail. Gated with a six-message transcript that proves the trailing question closes, the two earlier ones don't, receipts survive, and nothing is lost or duplicated — plus the no-user-turn-yet edge case.

- [x] **"SKIP ANYTIME; I'LL KEEP YOUR PLACE."** True until the previous commit, false the moment Skip started ending the tour — a promise broken **by fixing a bug**, in the app's own voice, against VISION.md's *"never claims a capability it doesn't have"*. Copy and behaviour live in different files and no check joined them. Now reads *"it's in the settings menu whenever you want it"*, and a gate reads `TOUR_STEPS` as **data** rather than grepping the source — the first version went red on its **own comment**, which quotes the retired promise.

- [x] AND THE COPY LIVED IN A DEAD TWIN. `app-tour-steps.ts` held **two byte-identical 2,411-character step arrays** — an unexported `STEPS` and the live `TOUR_STEPS`. Nothing referenced the first, and being first in the file it is the one you would naturally edit: the fix would have read as done and changed **nothing on screen**. Deleted, with a gate asserting exactly one array survives. Four mutations bite across both files.

- [x] **"THE SKIP TOUR DOESN'T ACTUALLY SKIP IT."** Ashley, with screenshots. `skip()` wrote the current step and moved to `'skipped'` — which renders the *"Resume the tour · 1 of 10"* pill — and the **only** route to `'done'` was reaching the last of ten stops. So nothing a person could do would dismiss it, and the pill sat over the Weigh-in row **covering the number**. Skip now does exactly what finishing does.

- [x] AND THE PERMANENCE IS ONLY SAFE BECAUSE OF THE SECOND HALF. Nothing anywhere in the app could restart a finished tour — no Settings row, no reset, nothing reading `fitplan_tour_v1` except the tour itself — so Skip-means-gone would have meant **one mistaken tap destroys it forever**. Ashley's ruling was gone-with-a-way-back: a **Replay the tour** row now sits in the settings menu. Both halves are gated together, deliberately.

- [x] THE RESUME PILL IS KEPT, for the one case it is right for: the app was closed part-way through, where offering to pick up is useful rather than nagging. `test:app-tour` §6, **five mutations biting** — and the sixth found a hole in this gate rather than in the app: the resume check first grepped `status === 'skipped'` and `ResumePill` as two separate strings, and stayed **green** when the whole branch was replaced by `if (false)`, because that comparison also appears in the `stepIndex` expression above. Rewritten to match the render branch itself.

- [x] **REHAB FOR ANKLES, WRISTS AND ELBOWS — 0 of 576 TRAINING DAYS → 576 of 576.** VISION.md already required it (*"an injury should produce a plan that actively rehabilitates it, not one that merely avoids the joint"*) and four of eight injuries did not meet it. A trainee with bad **wrists** lost **21 of 149** exercises and got back nothing; ankles 13, elbows 10. Nine primer-tier entries — bands, isometrics, mobility. Verified **per equipment tier**, so it holds at home and not only in a full gym.

- [x] NOTHING BEARS WEIGHT ON THE INJURED JOINT, and that is a safety property rather than a style choice: `isContraindicatedFor` returns false whenever `isIndicatedFor` is true, so marking a movement indicated **forces it past the contraindication filter** — the one filter protecting these people. A loaded calf raise (already contraindicated for ankles) or a hands-on-the-floor wrist drill would land on exactly the trainee it is wrong for.

- [x] **THE NECK IS OUT OF SCOPE AND THAT IS NOW ASSERTED, NOT MERELY LEFT ALONE.** Ashley's ruling: *"my neck bothers me"* spans a stiff desk neck and a nerve problem that must not be loaded, and the app cannot tell which — so per VISION.md's *"never claims a capability it doesn't have"* it says so instead of prescribing. Two gates go red if neck rehab ever appears, and both say to **ask her** rather than update the test.

- [x] A LATENT BUG THE WORK EXPOSED: `assignSetsRepsFromConfig` returned `"5"` reps for **every** primer, before the `prescription_type` dispatch — invisible for as long as all 25 primers were rep-based. The first two time-based ones came out prescribed **2x5 for a movement you hold**, which `test:audit` caught **1,459 times**. Fixed at the choke point; provably inert for the existing 25.

- [x] AND A REGRESSION I INTRODUCED, THEN FOUND BY MEASURING RATHER THAN REASONING. Giving the nine the obvious `primer_pattern_affinity` put one of them on **40.1% of UNINJURED training days** (231/576). Nobody asked for everyone's warm-up to change. Dropping the affinity took it to **0/576** with the guarantee untouched — `pickRehabMovement` never consults affinity — and that zero is now gated.

- [x] **"SHORTEN THE REHAB SET WHEN A SESSION WOULD OVERRUN"** — Ashley's second ruling. Rehab was **doubly** untouchable (a required slot *and* a primer, and primers were excluded from trimming outright), so a wrist-injured trainee on bodyweight / 30-45 / bodybuilding ran **43min against a 37min budget**. The budget pass may now take a rehab primer from two sets to one, **never below** — the guarantee is presence, not volume. Accessories still give first: trimming a squat to protect two sets of wrist circles would be the wrong way round. `test:audit` back to **13,967 / 0 failed**, its exact baseline.

- [ ] **NOT GATED: which exercise gives way first.** Mutating the trim order so rehab is sacrificed *before* accessories leaves `test:audit` green — it measures session length, not which movement paid for it. The ordering is a judgment call no check currently sees.

- [x] **A QUESTION AND A GENERATE BUTTON, SIDE BY SIDE.** Ashley: *"the questions should be finished by the time the generate plan button appears."* The coach asked *"how much time do you want to spend cooking?"* directly above **Generate My Plan**, so there was no way to tell whether onboarding was over. **Neither half was misbehaving** — that is why it needed a gate, not just a fix. She had answered `dislikedFoods`, the last slot that can block, so `complete_onboarding` was correctly accepted and the review correctly opened; the model meanwhile spent its reply text on `cookingTime`.

- [x] THE CAUSE WAS A CATALOG THAT OVERSTATED ITSELF. The slot catalog is introduced to the model as *"the answers you need"*, and `required` was its only qualifier — but `required: false` does not mean *skippable*: **age and injuries are optional and must still be asked**. Four slots (`cookingTime`, `includeSnacks`, `favoriteCuisines`, `breakfastStyle`) were long ago demoted so they can never delay anyone, and **nothing told the model that**. They now carry `neverBlocks`, render as `BONUS`, and the prompt states that finishing and asking are mutually exclusive — naming the measured incident and the same-turn case that actually fired.

- [x] AND A DETERMINISTIC HALF, because a prompt is advisory. `closeOutOpenQuestions` runs once the review opens and strips every question from that turn — the trailing one becomes the app's own closing line, a second is dropped rather than repeating it, and any chip card offered alongside is removed. **Ashley's ruling was the timing, not the slots**: the coach may still raise cooking time mid-conversation, never once the essentials are done. `test:no-question-beside-generate`, **six mutations biting** — including one proving the gate catches the sweep existing but never being called.

- [ ] **NEEDS `npm run deploy:functions:prod -- onboarding-chat`** for the catalog marker and the prompt rule. The client sweep ships with the Vercel push and is the half that holds regardless.

- [x] **FEET AND STONE ARE ACCEPTED NOW, AND THE APP SAYS WHAT IT STORED.** Ashley's ruling. `5'10` → **178cm**, `13st 2` → **83.5kg**, with the receipt reading `(from 5'10")` — because a silent conversion is only safe if the result is visible next to what was typed, while it can still be corrected. Formats: `178`, `178cm`, `1.78m`, `5'10"`, `5ft10`, `5 foot 10`, `70in`; `87kg`, `180lb`, `13st`, `13 stone 2`.

- [x] A CONVERSION IS ONLY EVER APPLIED WHEN THE INPUT NAMES ITS UNIT. A bare `70` stays 70 and fails the 100–250 bound exactly as before — guessing whether it meant centimetres or inches would be **the same offence as the `100, 150` lift mix-up this session started with**, one field over. `5'13"` and `13st 15lb` are refused rather than silently rolled into `6'1"` and `14st 1lb`.

- [x] **THE PLAN PREDICTED NONE OF THE THREE THINGS ACTUALLY IN THE WAY.** Writing the parser and its 40-case table proved nothing about whether anyone could use it: **(1)** the field was `type="number"`, which physically discards an apostrophe, so `5'10` could never have reached the converter; **(2)** the card's own `isOk` validated the RAW string against `isNumberIn(100,250)` and rejected it with "Give a number between 100 and 250" before the converter was consulted; **(3)** the label still said `cm`, so nothing told anyone the other form was allowed. **Every unit test passed the whole time.** Only driving the real field in Chromium found any of it.

- [x] WHY IT WAS WORTH THE CARE: `height_cm` and `weight_kg` feed `resolveBodyMetrics` → `computeBMR` → `computeStaticTDEE` (every macro target) **and** the standards table that sets starting loads. A misread height is a wrong calorie target and a wrong weight on the bar — and unlike a *missing* metric, which `load_source: 'assumed_body'` hedges every load against, **nothing hedges a wrong one**. Four mutations bite: promoting a bare number, dropping the read-back, restoring `type="number"` (8 red), and validating the raw string again (4 red).

- [x] **THE COMPOSER SAID "YOUR AGE…" UNDER A CARD ASKING THREE THINGS — WITH THE AGE ALREADY FILLED IN.** A grouped numeric card (age / height / weight) put its FIRST member's hint in the box, and the first member is the likeliest to be the one already answered. The hint now names what is still **outstanding**, in the card's own order, and narrows as boxes are filled: with 37 entered it reads *"height and weight…"*.

- [x] AND THE FIX GENERALISED, which the harness caught rather than me: the squat/bench/deadlift card is a group too, so its fixture's expected *"Your squat, in kg…"* **moved** to *"squat, bench and deadlift…"*. Naming one lift under a card showing three boxes was the same defect one screen over. Six composer states are now covered.

- [x] **A LIFT WEIGHT IS NEVER AGAIN ASSIGNED TO A LIFT THE USER DIDN'T NAME.** Ashley's ruling: *"show all three labelled boxes."* **And that card already existed** — `NUMERIC_GROUPS` has had the squat/bench/deadlift trio all along and `SlotNumericCard` renders the group with every field labelled. This was never a missing feature; the model bypassed it, asked in prose, and recorded two `set_slot` calls off the order **its own sentence** happened to use.

- [x] **WHICH LAYER DID IT WAS ESTABLISHED BEFORE ANYTHING WAS WRITTEN**, as the plan required: the model. The client's typed-answer path (`tryExactLabelMatch`) has branches for `single` and `multi` only — it never records a typed numeric at all, so it could not have been the one. The last three bugs in this flow were each a different layer than the obvious one, which is why this is checked rather than assumed.

- [x] FIXED IN TWO LAYERS, because a prompt is advisory and these slots write `load_source: 'known_weight'` — the app's most-trusted basis, outranking its own estimate and skipping the starting-light hedge. **The prompt** forbids the guess, names the measured incident, and points at the grouped card by slot key. **A client guard** in `executeActions` refuses a lift write when the turn records more than one lift and the user's own text never named it, showing the labelled boxes instead — and it reads **what the user typed, never the question**, which is the entire distinction the bug turned on. Exercised: `"100, 150"` refuses both, `"squat 100, bench 150"` allows both, one lift with one number allows, `age` untouched. Four mutations bite.

- [ ] **NEEDS `npm run deploy:functions:prod -- onboarding-chat`** for the prompt half. The client guard ships with the Vercel push and is the half that cannot be talked out of.

- [x] **"THE APP GOT COMPLETELY STUCK AND I HAD TO REFRESH" — IT WASN'T STUCK, IT LOOKED STUCK.** The request has a 45s abort and a `finally` that always clears `busy`, so it does recover. But **three animated dots that never change for three-quarters of a minute is indistinguishable from frozen**, and refreshing is the rational response. After 9 seconds the coach now says so: *"Still thinking — this one's taking a moment. Nothing's lost; it'll land or I'll say so."* Deliberately NOT a shorter abort — cutting off a reply that would have arrived at 20s trades a cosmetic problem for a real one.

- [ ] **`100, 150` BECAME SQUAT 100 AND BENCH 150, INFERRED FROM THE ORDER THE QUESTION LISTED THEM IN.** Ashley asked for exactly this to be a confirmation, not a presumption. It matters more than it looks: those slots set `load_source: 'known_weight'`, the app's **most-trusted** basis — it outranks the estimate and skips the starting-light hedge — so a mis-assigned number produces a **confident** wrong load for a whole mesocycle. Same family as "NEVER INVENT REPS OR SETS", one level up: those rules govern **values**, and here the **assignment of a value to a field** was invented, which no rule covers. `docs/PLAN-two-numbers-three-lifts.md` — **plan only, not built**; load prescription gets a plan first, and which layer decided it (model or client) is genuinely unknown.

- [ ] **NO QUICK-REPLY BUTTONS ON THE QUESTION AFTER A CORRECTION** — most likely not a bug at all: the "call `present_slot` EVERY time you ask a question that has a set list of answers" rule is already in the repo (`17431e1`), so this is very probably the **undeployed `onboarding-chat`**. Needs `npm run deploy:functions:prod -- onboarding-chat` confirmed before spending anything on it.

- [x] **THE COMPOSER SAID "WHICH DAYS?" UNDER A QUESTION ABOUT SQUAT, BENCH AND DEADLIFT** — the third time this placeholder has described a different question than the one on screen. **Two independent faults**, either of which alone would have caused it.

- [x] **FAULT 1: SIX OF 27 SLOTS SHIPPED WITH NO `inputHint` AT ALL** — all three lift questions, plus snacks, cuisines and breakfast. So even when the app worked out correctly which question was live, it had nothing to show and fell through. Now every slot has one, and a gate fails if a seventh appears (or if a hint grows too long for the box).

- [x] **FAULT 2: THE FALLBACK IS A GUESS, AND IT WAS OVERRIDING A QUESTION ON SCREEN.** When no slot could be identified, the composer named "the canonical next open slot" — and that helper **cannot see the lift slots at all** (they are `requiredIf`-conditional), so it skipped them and landed on `trainingDays`. The guess is still right when nothing is pending — after an answered card the next canonical question genuinely is next — but when the coach has just asked something unmappable, **"Say anything…" is true and naming a different question is not**.

- [x] AND A THIRD BUG FOUND WHILE FIXING THOSE: **`asksSlot` never survived a reload.** `toDraftMessages` mapped five fields and silently dropped it, so a question asked WITHOUT a card — the opener's "what should I call you?" is the built-in one — lost that fact on every refresh and the composer went blind. The `DraftMessage` type had always declared the field; only the mapper dropped it.

- [x] MY FIRST FIX PASSED LIVE AND FAILED IN THE HARNESS, which is the useful direction. On resume the app appends a **RESUME_BANNER** ("Welcome back — picking up right where we left off"), so "the last assistant message" was the banner, not the question behind it. Receipts are assistant lines too. Both are now skipped when looking for what was asked.

- [x] AND THE HARNESS WAS READING THE WRONG BOX: `input[placeholder]` matched the **numeric card's own field**, which renders ABOVE the composer, so it reported `1–500` as the composer's placeholder. It targets `.ob-composer-fade` now. Two new fixtures cover exactly Ashley's case (a freehand question) and its opposite (the same question WITH its card). **A fourth check was satisfied by its own comment** — the mapper check matched the prose beside it — so comments are stripped before that one runs. Five mutations bite.

- [x] **"NEW PLAN" DROPPED STRAIGHT ONTO THE REVIEW CARD WITH EVERY OLD ANSWER FILLED IN.** Reported live: tapping reset went to "Generate My Plan" — age, height, weight, sex, meals, all still there — instead of a fresh conversation. `handleReset` wiped the profile and six caches but **not `fitplan_onboarding_draft`**, which is a single GLOBAL key rather than a per-profile one. The next run restored a draft with every slot confirmed, so `readyToGenerate` was true on mount and the review opened immediately. Completion already cleared the draft; **reset was the only path that didn't**.

- [x] SO THE FIX IS A LIST, NOT A LINE. The bug isn't "we forgot this key", it's that **nothing said which keys a reset owns**. `test:reset-clears-draft` now classifies **every global localStorage key in `src/`** as cleared-or-deliberately-kept with a stated reason, and a new global key fails the gate until someone decides which it is. Offline queues (set-log, water, cardio, grocery, meal-event) are kept on purpose — each entry carries its own profile id and can still sync, and dropping them would silently delete logged work.

- [x] AND THE LIST IMMEDIATELY FOUND A SECOND LEAK: **`fitplan_last_tab`** also survived, so the tab the OLD profile was last on would greet the new one after onboarding. Same shape, one screen over. Now cleared.

- [x] **THE GATE FOUND A BUG IN ITSELF FIRST.** Its first run reported set-log keys living in the cardio and grocery stores — because it resolved `const NAME = 'literal'` into ONE shared map, and **five different stores each declare a `PENDING_KEY`** (three a `DEAD_LETTER_KEY`), so the last file read won. Constants are resolved per file now. My own earlier hand audit had made exactly the same mistake and would have shipped a wrong list of "safe" keys. Four mutations bite: dropping the draft clear, dropping the last-tab clear, wrongly clearing an offline queue (the over-fire control), and adding a new global key nobody classified.

- [x] **A NODE WARNING PRINTED ITSELF ON TOP OF THE `yes-production` PROMPT**, during a real production migration push. Harmless in itself (DEP0190, `shell: true` with an args array), but it landed on the single most consequential prompt in the repo — the one whose entire purpose is that reaching production costs a deliberate, legible moment. Two fixes: the Windows branch now builds one command string instead of passing an args array, which is the documented way to silence DEP0190 and is exactly as safe here because every argument is already validated against `/^[a-z0-9-]+$/` or is a literal `--flag`; and the prompt gets a leading newline so async chatter can never share its line again. Off Windows it now passes no shell at all, which is stricter than before. **POSIX path verified here; the Windows path cannot be run from the sandbox and is unverified until Ashley's next push.**

- [x] **THE ONBOARDING QUICK REPLIES ARE STACKED ROWS NOW — option 2b from Ashley's design handoff.** Label + description, hairline dividers, mint tint with a 2px left rail and a check on the selected row, no emoji, no scale animation. The seven training days collapse to a 7-across segmented strip; label-only sets over seven (dietary 22, cuisines 10, injuries 8) become wrapping pills. Measured at 390px: **nothing overflows**, and dietary — the worst case — is **634px, down from 771px**.

- [x] THE HANDOFF NAMED TWO TOKENS THAT DO NOT EXIST, and the real ones were used instead. `#C2BCE8` is **`--text-tertiary`**, not `--foreground-dim` (which is in no theme block); and the hover value `rgba(69,60,142,.28)` is **`--surface-raised`**, not `--accent` (`--accent` is `#2E2662`). Both real tokens are defined in all four themes, so the rows theme correctly rather than being right only on Nightshift.

- [x] **THE SHAPE RULE AS WRITTEN MIS-ROUTED ONE QUESTION.** The table says Strip is "no descriptions, ≤ 7 options with short labels" — but `gender` has **two** options and no descriptions, so it would have rendered a two-answer question as a seven-across day grid. The handoff's own prose lists sex under Rows. The strip now tests for **exactly seven**, `gender` falls through to rows, and the gate asserts that with the reasoning attached so it cannot be "simplified" back.

- [x] THE TYPED-TEXT LANDMINE THE HANDOFF FLAGGED WAS ALREADY SAFE, verified rather than assumed: `sameText` (`ConversationalOnboarding.tsx:179`) lowercases both sides, so the "user typed exactly the chip label" shortcut survives the relabel. **92 option `value`s are byte-identical before and after** — dumped either side of the change rather than eyeballed in a diff, because they are persisted and matched against stored data.

- [x] SENTENCE CASE WENT FURTHER THAN THE HANDOFF'S LIST, flagged rather than done quietly: its examples split on spaces and slashes, so the hyphenated allergen pills ("Dairy-Free", "Low-Carb") were not named. The stated rule is "all `label` fields", so they went too — **Low-FODMAP keeps its acronym**.

- [x] **TWO HARNESS BUGS FOUND EN ROUTE, BOTH SILENTLY PASSING.** `measure.mjs` picked *the first `.html` found by walking dist* — and the moment `composer.html` joined that bundle earlier today it sorted first, so the driver has been measuring **the wrong page**, printing "screens that overflow: none" against a document containing no questions at all. Pinned to its own page, and an empty run now **fails** instead of reporting none. Separately: `captureScreenshot`'s clip is in PAGE coordinates, not viewport, and needs `captureBeyondViewport` for anything below the fold — two blank screenshots before that landed.

- [x] THE GATE MOVED WITH THE DESIGN RATHER THAN BEING LOOSENED. The icon rule got **stronger** — from "an icon repeated on every option is not rendered" to "the chips render no emoji at all, and the row component has no icon prop to pass one through". The stranded-last-card rule is retired *and its retirement asserted* (`grid-cols-2` and `col-span-2` must both be gone, so the two-column grid cannot creep back). New: all three shapes reachable, the strip requires exactly seven, and a multi-select footer reads the picks back in words.

- [x] **26 OF 91 CONTROLS WERE TOO SMALL TO TAP RELIABLY. NOW 4, AND NOTHING MOVED ON SCREEN.** Found by asking the browser the question a thumb asks — *if I land 22px off centre, do I hit this?* — rather than by measuring boxes, because a bounding rect cannot see a pseudo-element hit-slop. Worst offenders: **"Save" on the weigh-in at 20px tall**, the **+250/+500 water buttons at 16px**, **"Add Set" at 24px**. Apple's minimum is 44pt, Android's 48dp.

- [x] THE BIGGEST LEVER WAS ONE FILE: **every `Button` size variant except `cta` is under 44px** (`h-9`=36, `h-8`=32, `h-6`=24, `size-10`=40). `hit-slop-44` on the variants fixed **26 → 17** at a stroke, app-wide, invisibly — it is an `::after` that expands the touch area and moves no layout. The rest were raw `<button>`s in Dashboard, NutritionDisplay, MacroSplitCard, ExerciseRow and ExerciseLine. **Dashboard and Nutrition are now completely clean.**

- [x] **I GOT THE MEASUREMENT WRONG TWICE AND BOTH CORRECTIONS MATTER MORE THAN THE FIX.** (1) The first run reported **38 of 93 and "15 stolen taps"** — `document.elementFromPoint` works in VIEWPORT coordinates and returns null outside it, so every control below the fold counted as both unreachable and stolen. The dashboard page is 1342px against an 844px viewport. Scrolling each control into view first: the real figure was **26**, and the stolen count was **zero**. (2) The bar itself was unachievable: demanding a full 44px reach in both axes fails for any two controls closer than 88px. Probed directly — the "Expand phase context" button's `::after` really is 44x44 and resolves above, below and left; 21px right lands on the next button. **A neighbour owning that point is arithmetic, not a defect.**

- [ ] **THE REMAINING 4 ARE ONE VISUAL QUESTION, NOT A MECHANICAL ONE, so they are not built.** Three are `<input>` elements — the two set-log number fields (28px) and the grocery add field (32px) — and an `<input>` **cannot have a `::after`**, so the invisible trick does not exist for them; the only fix is real height. The fourth is the button beside the grocery field. **Plus 9 more that only pass because a neighbour is pressed against them** (the SetGrid icon row at 28x28, the 14x14 phase-context caret, the 3d/7d/14d chips). Making those comfortable means a less dense set grid — what a user sees, so it is Ashley's call.

- [x] AND THE SWEEP TURNED UP **FOUR ICON-ONLY BUTTONS WITH NO ACCESSIBLE NAME AT ALL** — they announce as just "button" to a screen reader. Two are user-facing (grocery add, ProfileScreen add) and now carry `aria-label`; two are in `DevTestPage`. Found because the harness prints a control's label and two of them printed as `<button>`.

- [x] **THE MAIN CHAT HAD THE SAME BUG ASHLEY REPORTED, AND I HAD CALLED IT "FIXED BY PARITY" WITHOUT SEEING IT.** Now measured against the real component: with a 336px keyboard up, the composer rides to **top 416** while the chat Card is a fixed `h-[600px]` ending at **648** — so it covers **232px of the message list**, and the newest message, at 512-552, was **entirely behind it**. The scroller reserved a static `pb-24` (96px) that never grew. Onboarding grows its padding with the keyboard; this never did.

- [x] THE FIX MEASURES THE OVERLAP RATHER THAN DERIVING IT. Onboarding can use `insetPx + 112` because its scroller ends at the viewport bottom; the chat's ends at the **Card's** bottom, an unknown distance above that which depends on App.tsx's own page padding — a constant there would go stale the moment that padding changed. A `useLayoutEffect` reads the two boxes and pads by the real gap. No feedback loop: padding sits inside the border box, so growing it doesn't move the scroller's own rect. **Padding 96px → 244px; newest message 512-552 → 364-404, clearing the composer by 12px.** Keyboard-shut geometry is byte-identical.

- [x] AND THE PARITY `min-h-0` I ADDED EARLIER WAS UNNECESSARY — measured, the chat scroller was **already scrollable** (`scrollHeight 1410` vs `clientHeight 600`), because its `CardContent` parent carries `overflow-hidden`, which already zeroes a flex item's automatic minimum. Harmless, and now known rather than assumed. **This is the correction: I reported that fix as "applied by parity, unmeasured" and it turned out the thing I fixed there wasn't broken, while a different thing on the same screen was.**

- [x] **THE HARNESS COULD NOT SEE ANY OF IT UNTIL TWO HEADLESS FACTS WERE FIXED.** (1) `real.tsx`'s chat tab was a **stub div**, so the real chat had never been driven at all — `.tour-harness/chat.tsx` now mounts the real `ChatAssistant` inside App.tsx's shell **copied verbatim**, since the Card/composer collision depends entirely on where `<main>`'s padding puts the card. (2) A headless page is **not focused**, so `element.focus()` sets `activeElement` **without dispatching focus events** — measured, `focusin` fired **0 times** — and `useViewportInset` gates on a focused input, so the layout never entered the state under test. `Emulation.setFocusEmulationEnabled` fixes it. Without that, this harness would have cheerfully reported everything fine.

- [x] MY FIRST HARNESS SEEDED THE CONVERSATION UNDER THE WRONG KEY — `fitplan_chat_cache_` with a `{ messages, savedAt }` wrapper; the real one is `chat_history_cache_` holding a **bare array**. The chat restored nothing and I measured an **empty thread**, which would have reported "the scroller doesn't need to scroll" as a finding about the app. Caught by checking whether the fixture had actually loaded before reading any number off it. `verify:chat-shell` now asserts the thread really overflows before it asserts anything else, and `test:composer-focus` §6 pins the copied wrapper to App.tsx's so it cannot drift.

- [x] **I CHECKED WHETHER THE ASSISTANCE LEAK HAD SIBLINGS. IT DOESN'T — AND THAT NEGATIVE RESULT IS NOW A GATE.** The leak was a carry-forward fallback (`load ? … : ex.<field>`) surviving a rotation that changed the slot's identity. **Four sibling fields use the identical shape** — `load_guidance`, `suggested_load`, `suggested_load_kg`, `load_source` — so a barbell lift rotating out for a bodyweight movement could have taken its weight with it. **Swept 74,628 exercises: it does not happen.** Assistance leaked precisely *because* `prescribeAssistance` sits OUTSIDE the load branch (its own comment says so) and had no equivalent guard.

- [x] THE INVARIANT WAS GENUINELY UNGATED, though — `isExternallyLoaded` appears in eight gate scripts and is used as a **filter** in every one, never as an assertion. "A movement with nothing to load carries no kg number" is now checked over the sweep: **11,478 of 19,436 exercises have nothing to load**, so it is nowhere near vacuous, and forcing the fallback to carry makes it fail naming Band Face Pulls, Band Pull-Aparts, Prone Y-T Raises and Standing Band Hip Abduction at 42.5kg.

- [x] **THE COACH NOW GETS EVERY PRESCRIBED NUMBER THE APP HOLDS, AND A GATE SWEEPS 19,436 EXERCISES TO PROVE IT.** The four gaps a review found in `f516748` are closed: **machine assistance** (an assisted pull-up reached the coach as `@ Bodyweight` — a phrase the prompt teaches it to read as *no external load*, while `AssistanceChip` showed "35kg assist" one screen away), the **`assumed_body` hedge**, **primers** quoted as if `Light` were a weight, and **days with no gym session** arriving as a dangling `Sunday: Active recovery - `. Tempo and intensity go too — for a rep-based lift with no weight to add, the tempo IS the prescription, and those are exactly the movements whose load clause reads `Bodyweight`.

- [x] **AND THE GATE'S OWN TEETH CHECK FOUND A BUG NOBODY WAS LOOKING FOR: 84 PRESCRIPTIONS GAVE MACHINE ASSISTANCE TO EXERCISES WITH NO MACHINE.** Asserting that `suggested_assistance_kg` actually *occurs* in the sweep — so its check can't be vacuously true — turned up **Kneeling Band Lat Pulldown (64)** and **Lat Pulldown (20)** carrying a counterweight. Cause: a slot's identity changes week to week, and `suggested_assistance_kg: assistance ? … : ex.suggested_assistance_kg` carried the **previous** exercise's number onto the new one when a rotation swapped Pull-Ups (Assisted) out. `suggested_added_load_kg` on the very next line already nulls instead of carrying; this line was the one that didn't follow the rule. **Worse than a stray number**: `AssistanceChip` renders it as *"40kg assist · less over time = stronger"* — an inverted progress cue on a lift where more weight IS the progress.

- [x] DECIDED WITHOUT ASKING, on Ashley's "fix it all in order", and recorded because it was one of the two open questions: an `assumed_body` load now reaches the coach as `~40kg [STARTING LIGHT — no body details, deliberately low, not a target]`, my recommended option (a). It costs one clause, it keeps chat and the Exercise tab saying the same thing about the same number, and the whole reason `load_source` exists is that a guessed load must never be presented as a known one. The over-fire control is gated too — a `known_weight` or `estimate` load carries **no** hedge, or the hedge would mean nothing.

- [x] ONE DELIBERATE DEVIATION FROM THE PLAN, flagged rather than quietly taken. The plan said *send the progressed added load rather than the stored one*. `getAddedLoadProgression` is an async per-exercise DB read and `buildContext` is synchronous, so doing it properly means a progression query on every chat turn. Instead the coach is **told** the number is the plan's base, that the Exercise tab may show a higher one, and that **the screen is right** if they disagree. That removes the false confidence without duplicating the progression engine into the chat path.

- [x] `test:coach-plan-context` is new and registered. It sweeps real generated plans and asserts that **if an exercise carries a prescribed number in any field, that number appears in what the coach is sent** — the shape that would have caught the assisted pull-up on day one, where the old gate checked a single hand-built object with no load fields at all. Eight mutations bite, including an over-fire control and the leaky fallback (which fails naming Kneeling Band Lat Pulldown and Lat Pulldown by name and week).

- [ ] **THE COMMIT THAT FIXED THE MISSING WEIGHT MISSED FOUR MORE NUMBERS, AND ONE OF THEM STATES SOMETHING FALSE.** A review of `f516748` found the same omission it fixed still live in `suggested_assistance_kg`, the progressed added load, primers, and days with no exercises — each **verified by running the builder**. An assisted pull-up with **35kg of assistance** reaches the coach as `@ Bodyweight`, which the new prompt teaches it to read as *no external load* — worse than sending nothing, and the exact shape of the incident this thread started with. `docs/PLAN-the-coach-still-cannot-see-four-numbers.md`. **Plan only, not built** — load prescription gets a plan first, and the last change here skipped that, which is how these got through.

- [x] TWO HONESTY DEFECTS IN THE PROMPT I WROTE TODAY, FIXED IMMEDIATELY because they are corrections to my own new text rather than new behaviour. **(1)** It said *"the never-invent rule below"* — that rule is **137 lines above**, so the one sentence separating "quote a prescription" from "log a prescription" sent the model looking the wrong way. Now asserted **by position**, not by wording, so it cannot go stale if either block moves. **(2)** It claimed *"you HAVE the prescribed weights"* **unconditionally** — but `buildCoachExerciseSummary({ days: [] })` returns `""`, and the summary carries **only the active week** while Ashley's original question was about a **past** one. Claiming data you were not given is the same failure as denying data you were, pointing the other way. Both limits are now stated and gated; four mutations bite.

- [x] **THE KEYBOARD CLOSED AFTER EVERY ONBOARDING ANSWER, AND THE AUTO-SCROLL HAD BEEN A NO-OP THE WHOLE TIME.** Ashley: *"each time I enter an input and hit send the keyboard closes and I have to reopen it for the next message and the conversation is not at the latest message and it cuts off the bottom so I have to scroll down which is incredibly frustrating."* Two independent causes, both **measured in Chromium against the real screen before anything was changed** (`npm run verify:keyboard`), not reasoned about.

- [x] **CAUSE 1, FOCUS: after tapping send, `document.activeElement` was `<body>`.** Nothing focused — which is a phone putting the keyboard away. A tap focuses the button, and emptying the box flips it to `disabled` in the same tick, so not even the button keeps it. **The input already knew this**: it carries a comment reading *"NOT `disabled` while busy: on a phone, disabling the focused input dismisses the keyboard, so the user had to re-tap the field on every single turn."* The lesson was learned, written down, and applied to one of the two focusable controls in the composer. The other one is this. Same shape found and fixed in the main chat's send **and mic** buttons.

- [x] **CAUSE 2, SCROLL: the scroll container did not scroll.** Measured at 390x844: the canvas was **875px tall in an 844px viewport**, and the scroller reported **`scrollHeight === clientHeight === 799`** — nothing to scroll, so every `scrollTo` in the auto-scroll block was a **no-op**. That block carries thirty lines of comment about ResizeObservers and why watching the message list was the wrong signal; all of it was correct and none of it could ever fire. What actually moved was the **page**, which nothing there controls, parking the newest message **11px underneath** the fixed composer. Cause: `min-h-[100dvh]` plus a flex child with the default `min-height:auto`, which refuses to shrink below its content. `h-[100dvh] overflow-hidden` + `min-h-0`. **Clearance −11px → +20px, and the scroller now scrolls (scrollTop 31).**

- [x] A THIRD FACT THE FILE ASSERTED IS FALSE, and it is why this was easy to miss: the comment above the padding reads *"100dvh above shrinks with the keyboard on Chrome Android."* It does not. Chrome 108 made **`resizes-visual` the default** — the on-screen keyboard resizes the visual viewport only, leaving the layout viewport and `100dvh` at full height ([Chrome for Developers](https://developer.chrome.com/blog/viewport-resize-behavior)). Verified against Chrome's own documentation rather than from memory. The JS inset compensation happens to cover it, so this is a wrong premise rather than a live second bug — corrected in place, and worth knowing before anyone trusts that comment again.

- [x] `test:composer-focus` holds all of it, and six mutations bite: removing the guard from either send button (1 each), removing `min-h-0` (1), reverting the canvas to `min-h` (2), switching the helper to `pointerdown` (1), and hard-disabling the input again (2). `onMouseDown` deliberately, **not** `onPointerDown` — on touch, `mousedown` is the compatibility event that assigns focus, and preventing it stops the focus change without swallowing the `click`.

- [x] TWO OF MY OWN CHECKS WERE WRONG BEFORE THE CODE WAS. The new gate first went red **on its own doc comment** (which contains the words "rather than onPointerDown:") — the third time in this repo a check has been satisfied or broken by prose ABOUT the thing it looks for. And `test:onboarding-style` sliced a **fixed 3400 characters** from `ob-composer-fade`; five added lines of comment pushed `size-[22px]` out of the window and turned a passing check red with the markup untouched. A gate that fails on comment length is measuring the wrong thing — the window now runs to end of file, and the check still bites when the icon size actually changes.

- [x] AND THE HARNESS ITSELF WAS MEASURING A PAGE NO USER LOADS: `.onb-harness/composer.html` had **no viewport meta tag**, so the first geometry run reported the composer at `top: 2029px` on an 844px screen. It reads the app's real meta tag now. The placeholder check it was built for never needed a viewport; the moment it was asked a layout question, it was wrong.

- [ ] **NOT MEASURED: the main chat's scroll geometry.** The focus fix there is by parity with the measured onboarding defect (identical construct, read directly), and `min-h-0` was added to its scroller on the same reasoning — but the tour harness's chat tab is a **stub**, not the real `ChatAssistant`, so nothing drove it. Mounting the real component there is what would close this.

- [x] **THE COACH SAID IT DIDN'T HAVE HER PRESCRIBED WEIGHTS. IT WAS TELLING THE TRUTH.** Ashley, mid-log: *"I don't actually have your past weights on hand to look up what was prescribed"* — with the Exercise tab one tap away reading **Deadlifts 72.5 kg SUGGESTED, S1/S2/S3 all 72.5kg**. `ChatAssistant`'s plan summary sent the day, the focus, the exercise name, sets, reps, rest and the selection note. **It never sent the load.** So the coach asked for a number the app had already computed, printed on the next screen, and withheld — and then apologised for not having it.

- [x] THE FIX SENDS THE FORMATTED STRING, NOT THE NUMBER. `suggested_load` (`~14kg per hand`), never `suggested_load_kg` (`14`) — the per-hand distinction is 47.8% of externally-loaded prescriptions and a bare 14 reads as a total. Measured across a 4-equipment x 3-experience x 4-goal sweep: **26,912 of 26,912 exercises now carry a load clause**, 4,956 (18.4%) of them a per-hand qualifier that a raw number would have lost. The before figure is 0 **by construction, not by sweep** — the old template literal contained no load token of any kind.

- [x] A RAMP SENDS EVERY SET, NOT THE TOP SET THREE TIMES. **923 of 26,912 prescriptions ramp** (`~35kg, ~40kg, ~42.5kg`), so *"I did the prescribed weights"* would otherwise have logged 42.5x3 — the same invented-number defect as filling reps from the prescription, one field over. Weighted bodyweight work sends its `+17.5kg added` too, which `suggested_load` alone ("Bodyweight") loses.

- [x] IT WAS AN INLINE TEMPLATE LITERAL, WHICH IS WHY NOTHING CAUGHT IT — a gate can't call a string interpolation inside a component, and a regex over the source would have passed against the broken version, because the broken version was perfectly well-formed. It's `src/lib/chat-plan-context.ts` now and `test:log-correction` §6 **runs it and reads the output**. Six mutations bite: dropping the load turns 6 checks red, sending the raw kg 5, collapsing a ramp 2, dropping added load 1, hand-rolling the summary back into the component 2, softening the prompt rule 1.

- [x] AND A PROMPT HALF, because data alone doesn't stop the sentence being said again. `CURRENT EXERCISE PLAN` now states that the `@` clause **is** the prescribed weight, how to read per-hand/single-side/added/ramped, that the coach therefore has them, and that quoting a prescription is **never** logging it as done — the never-invent rule is untouched. It asks for confirmation (*"The plan had you at ~72.5kg — was that what you lifted?"*) rather than an open question.

- [ ] **NEEDS `npm run deploy:functions:prod -- chat-gemini` ON ASHLEY'S MACHINE.** The client half ships with the Vercel push and is the half that fixes the missing data. The prompt half is inert until deployed — and so is `NEVER ASK THE SAME QUESTION TWICE IN A ROW`, which already sits on `main` carrying **Ashley's exact case** as its recorded example ("What weight did you use for Deadlifts?" asked twice verbatim in her screenshot) and has never reached production.

- [x] **THE PROFILE FORGOT EIGHT ANSWERS ON EVERY RELOAD — FIXED, AND GATED SO A NINTH CAN'T.** `restoreSession` rebuilds the profile column by column, so anything missing from that list was `undefined` for the whole session however faithfully Postgres stored it. Audited by parsing every `fitness_profiles` column out of the migrations and diffing: **51 columns, 42 restored, 9 dropped, 8 with live consumers.** All eight were written correctly — nothing in the database was ever wrong, only the read-back.

- [x] WHAT EACH ONE COST A USER: the three implement ceilings and `load_ceilings_declined` meant **"I'm not sure" never stuck** — the button that exists to stop the asking didn't, and the function's own comment says *"a trainee who says 'I don't know what my dumbbells weigh' must be able to say it once"*. The three macro fields **silently moved a protein target** back to Balanced on every open, which is worse than re-asking because nothing tells you. `water_target_ml` reverted to 2000ml.

- [x] **THE ROOT CAUSE WAS A LOCAL INTERSECTION TYPE.** The ceilings were declared inside `load-prescription.ts` and `load-ceiling-prompt.ts` as `UserProfile & { max_dumbbell_kg?: ... }`, so TypeScript never required `restoreSession` to map them and never complained. They're on `UserProfile` now, and reverting that is caught **by the compiler** — a cast that adds fields the profile doesn't really have will always compile and can never be checked.

- [x] EXISTING PLANS REBUILD, on Ashley's ruling over waiting up to sixteen weeks for it to age out. `ceiling-reconcile.ts` detects **the violation, not a marker** — is any prescribed load above what this person said they own — which is self-correcting and safe to run every load. From the **active week forward only**: rewriting a load someone already trained against would change what their own logs are measured against. No new rebuild path; `rebuildAgainstProfile` already takes the profile and the profile now carries the ceiling. The coach says why, naming their own number back to them.

- [x] MEASURED, AND IN THE DIRECTION I ASSERTED BEFOREHAND: with a 24kg-per-hand ceiling, **8 of 130 dumbbell prescriptions move DOWN, 0 move up** — a ceiling is a cap, so a rise would have meant the fix was wrong. Someone whose dumbbells stop at 24kg was being prescribed **30kg Farmer's Walks**.

- [x] AND A SMALL LIE IN THE COACH'S PROMPT, found because my gate first called `training_time_preference` dead. It isn't dead — `chat-gemini` reads it — but the client never sends it, so the prompt printed a constant **"Training Time: morning"** directly beneath the real `Preferred Time: evening`. Both usages removed; the column is now genuinely unused and flagged for deletion rather than quietly mapped. The gate searches `supabase/functions/` too now: **a check that decides what is dead has to look everywhere the thing could be alive.**

- [x] **THE STEP TARGET IS EDITABLE NOW** — `daily_step_target` on `fitness_profiles` (migration `20260828140000`), nullable, no default. Set it and the ring measures against it; leave it and the band derived from activity level stands. The settings row sits directly under Activity level, which is what it overrides, and its placeholder shows the band that activity level produces — so an empty box reads as a considered default rather than a gap. `stepsTargetFor` takes the **profile**, not the two fields, so no caller can read one and forget the other.

- [ ] **NEEDS `npm run db:push-both` ON ASHLEY'S MACHINE.** The column does not exist on TEST or PRODUCTION until that runs, and the app tolerates its absence (the derivation stands) — but nobody can set a target until it does.

- [x] **CLOSED BY `1226ba2`, and this line sat unticked afterwards** — `restoreSession` now maps all four ceiling fields (`App.tsx:344-347`), verified by reading the current file rather than by trusting this entry. Left in place because the diagnosis below is what the fix was built from. Original finding follows.
- [x] **THE APP FORGETS A LOAD CEILING YOU GAVE IT, ON EVERY RELOAD.** `App.tsx`'s `restoreSession` builds the profile **column by column**, and `max_dumbbell_kg`, `max_single_implement_kg`, `max_improvised_kg` and `load_ceilings_declined` are **not in that list**. `hasStatedCeiling` reads them off that restored object and returns `v != null`, so after any reload they are `undefined` → never stated → **the app asks again**. A trainee who said "my dumbbells go to 32kg", or who tapped "I'm not sure" to stop being asked, gets the same prompt next time they open the app. Not fixed here: load prescription gets a plan before a build. Found while adding `daily_step_target` and checking I was not about to repeat the same shape.

- [x] **A REAL PRE-EXISTING BUG, FOUND BY THE NEW SCORE ON ITS FIRST RENDER: days before the plan existed were counted as plan week 1.** `getActiveMesocycleWeek` clamps with `Math.max(0, elapsedDays)`, and `dashboard-data` looks back 35 days — so on a plan created today, **all 35 landed in "this plan week"**. The consistency score read *"0/19 planned sessions"* for a single week, which is what exposed it. It also let coach-tips' `perfect_adherence` announce *"Every planned session this week, done — 19 for 19"*, and skewed `session_pace`'s week-on-week comparison. Related to but not the same as the earlier "stop marking pre-plan days as missed" fix — that taught the STREAK to ignore them and left these aggregates behind.

- [x] **CONSISTENCY SCORE, NAMED FOR WHAT IT MEASURES.** Ashley asked for a readiness score "combining workout consistency, sleep/rest days, and nutrition compliance" — the app has **never asked how anyone slept**; `recovery_capacity` is one onboarding answer that never changes. Her call when the choice was put to her: build it from what's real and name it honestly. Sessions and protein days over the current plan week, ratios averaged (not raw counts, so 3/3 sessions can't drown out 1/6 protein days), components spelled out so a lone percentage can't read as a verdict, and **absent rather than 0%** when nothing measurable has happened yet. Renders `Consistency: 50% · 1/1 planned sessions · 0/2 protein days`.

- [x] WATER IS DELIBERATELY OUT OF THE SCORE — the app has it for today only, and seven more per-day reads would break `dashboard-data`'s stated read-cost discipline. A one-day component beside two week-long ones would also be quietly mis-weighted. **Better absent and explained than present and wrong.** The protein loop was restructured to collect days rather than break on the first miss, so the week count costs **zero extra reads**.

- [x] **THE WATER COACH TIP — ASHLEY'S OWN EXAMPLE — AND THE NaN I PUT IN A USER-FACING SENTENCE.** Written as `if (ctx.waterTargetMl <= 0) return null`, the rule ran on a context carrying no water fields at all (`undefined <= 0` is false), fell through to the arithmetic, and produced **"You're about NaNml behind on water for this time of day."** Caught by the existing coach-tip gate's "zero supporting data anywhere → null" check, which is exactly what that check is for. Every input is now checked for being a finite number, not just for its value.

- [x] THE TIP ITSELF IS PRO-RATA AGAINST A WAKING DAY (08:00–22:00, not midnight, or everyone is "behind" every morning), silent before 10am and after 10pm, silent under 250ml, and it **joins the same seeded daily rotation as every other rule rather than jumping the queue** — being behind on water does not outrank a new PR, and a rule that always won would be a daily nag. It is arithmetic on a target the app set and a log the user made, unlike the energy/readiness line in the same request, which would have needed sleep data that is never collected.

- [x] **QUICK REPLIES ARE BACK ON EVERY ONBOARDING QUESTION** — Ashley, reversing this morning's narrowing: *"ive changed my mind and I think actually the quick replies on the onboarding are better for all questions."* The four that had been exempted (do you know your lifts, male/female, how many meals, snacks yes/no) offer their options again — 2, 2, 3 and 2 respectively.

- [x] THE EXEMPTION MECHANISM WAS **REMOVED**, NOT LEFT TAGGING NOTHING. `obviousAnswer` is gone from the slot type, the four slots, the catalogue sent to the model, the client's `present_slot` guard and the prompt's `[NO CHIPS]` rule. A flag no slot carries still reads as meaningful to the next person and does nothing — the same dead-code-with-a-truthful-comment shape this log keeps recording.

- [x] THREE RULINGS ON ONE QUESTION, AND THE GATE NOW CARRIES THE HISTORY so a future session doesn't re-litigate it: **(1)** *"Always, pills or cards as fits"* — options had become invisible; **(2)** *"Some have obvious answers"* — four exempted; **(3)** *"better for all questions"* — back to always. `test:onboarding-chips` asserts the property plainly (a slot with a list offers it, 18 of them) and that no suppression mechanism exists in any of the four places it used to live. Bites: re-exempting one slot turns 2 red, restoring the client guard 1, dropping "every time" from the prompt 1.

- [x] **HALF THE WEIGHTS IN A PLAN SAID THE WRONG THING, AND NOW THEY DON'T.** `formatLoad()` has always produced `~14kg per hand` and stored it on every exercise as `suggested_load`; only `LoadChip` read it. `ExerciseLine` and `ExerciseRow` re-rendered the raw number with a hard-coded "kg", so **1126 of 2356 prescriptions (47.8%)** showed a per-hand number in the same format as a total, sitting directly beneath one — a 14kg RDL reading as a third of a 42.5kg squat when it is two-thirds. Wording is Ashley's: **"14kg per hand"**, chosen from six candidates all measured against the real column width.

- [x] THREE SURFACES, ONE UNIT, verified in Chromium at 390px: collapsed line `3×11-13 · ~14kg per hand`, expanded number `14` + `kg per hand`, logging header `Log weight · per hand`. A barbell lift is untouched at `~42.5kg`. **The logging column mattered most** — it doesn't just tell you a number, it asks you for one, and `exercise_set_logs` carries no unit of its own to catch a 2× mistake with. No prescribed number changed and no schema changed.

- [x] `test:load-display` holds it as **"nobody hand-rolls a kg string"**, not "these two files look right today" — this is the two-halves shape, so a gate pinned to the two files that were wrong would pass while a third surface repeated it. Bites on five mutations: reverting ExerciseLine (3 checks red), hard-coding kg in ExerciseRow (1), SetGrid re-deriving the unit itself (1), the header dropping the label (1), `formatLoad` losing the qualifier (1).

- [x] TWO OF MY OWN MISTAKES WHILE BUILDING IT. The gate fired on **the comment I had just written explaining the bug** — third time this session a check of mine was satisfiable by its own documentation; it strips block comments now, since the explanation lives in a JSX `{/* … */}`. And I reported the header "isn't getting the prop" after reading `<span>Weight</span>` off the page: there are **two** headers on screen, a page-wide search returns the first, and that one belongs to a bodyweight exercise where plain "Weight" is correct. Scoped to the expanded block it read `Log weight · per hand` the whole time.

- [x] **A CORRECTION I OWE: THE "DOES NOT OVER-FIRE" CONTROL I QUOTED THIS WEEK WAS A TAUTOLOGY.** `report-rehab-coverage` printed *"CONTROL — no injury flagged: 0/576 days"* and I reported that zero as evidence the rehab slot doesn't fire for uninjured people. It was evidence of nothing: it ran the sweep with an **empty flagged-joint set** and then counted movements indicated *for the flagged joints*. Nothing matches an empty set, so it read 0 whatever the plans contained — it would have read 0 with the slot firing on every day of every plan. **How I got it wrong:** I added that control in the same change as the hips/lower-back work, reused `sweep(injury)` for it by passing a fake injury code, and never asked what the reused function was actually comparing against. The number looked like a measurement because it came out of a sweep.

- [x] THE PROPERTY IS TRUE ANYWAY, for a better reason: `pickRehabMovement` returns null on an empty joint set before it looks at the pool (`exercise-plan.ts:1638`). Found while looking at a real rendered plan — an uninjured user's screen showed **Side-Lying Hip Abduction** and **Banded Terminal Knee Extension**, which contradicted the 0/576 on its face. They arrive through ordinary primer/accessory slots, which is by design; twelve joint-tagged movements do.

- [x] THE CONTROL NOW MEASURES SOMETHING THAT MOVES, **per joint**, because pooling all four joints gives 88.4% and makes the guarantee look worthless — an artefact, not a finding. Uninjured trainees already meet a **shoulder** movement on 49.3% of days, **knee** 28.6%, **hip** 30.0%, **lower back** 33.5%; the injured rate is 100%, so the guarantee is worth **50.7 / 71.4 / 70.0 / 66.5 points** respectively. That is the first honest statement of what the rehab work actually buys.

- [x] AND THE NEW CHECKS' COVERAGE IS STATED RATHER THAN ASSUMED, after the first version of my own comment overclaimed. Deleting the early return fires **only** the source-shape check — the behavioural one still passes, because with no flagged joints the pool filter matches nothing and null comes back by a second mechanism, which makes it nearly the same tautology on that mutation. Adding a `return pool[0] ?? null` fallback — the realistic refactor error, and the one that would actually put rehab in an uninjured plan — fires **four**. Both kept; neither covers the other's mutation.

- [x] **THE TOUR HAS NOW BEEN DRIVEN AGAINST THE REAL SCREENS, AND IT FOUND A REAL DEFECT ON THE FIRST FULL RUN.** `verify:tour-real` mounts the actual Dashboard, NutritionDisplay, MealPlan, ExerciseTab, ToolsTab and BottomTabBar with a real generated mesocycle, and walks all ten stops in Chromium at 390px. Every stop's spotlight now lands on its real target to within 2px, every hole is inside the viewport, and Finish closes it.

- [x] **THE SET STOP TOLD THE USER TO DO SOMETHING THE APP REFUSED.** It says "Leave the fields blank and I'll take the prescribed numbers." Weight had a prescribed fallback; **reps did not** — it fell back to `''` and the save was rejected with *"Enter reps to log this set"*, while the reps box suggested **"0"**, the single value the save explicitly refuses. It could only ever bite on week 1, and week 1 is the only week the tour ever runs in (`tourArmed` is set at exactly one call site — the moment onboarding succeeds), so the two were guaranteed to meet. Blank reps now logs the **bottom** of the prescribed range on Ashley's ruling (9 of "9-11", 33 of "33-48s", 40 of "40m") — bottom not top, because the number feeds the progression engine and erring high would hand someone heavier weights off a set the app invented. The `reps <= 0 never logs` guard is untouched.

- [x] FOUR OF THE FIVE THINGS THAT RUN FOUND WERE THE HARNESS, NOT THE APP, and that is the ratio the `.tour-harness` README already warns about. (1) No `<meta viewport>` in the harness HTML → Chrome laid out at 980px and every rect was off-screen. (2) The harness wrote `#/nutrition` where the app's own `tabHash` writes `#/tab/nutrition`. (3) The driver's spotlight selector matched the tour's own *card* shadow. (4) The driver carried a hand-written copy of the stop list with the chat tab's key as `navChat`; it is `chatfab`, and it reported a target missing that was never missing — it now reads `TOUR_STEPS` out of the page. Plus one of mine in the fake Supabase client: `insert().select().single()` returned the payload rather than the stored row, so it had no generated `id` and every caller needing one gave up silently.

- [x] AND ONE OF MY OWN CHECKS WENT GREEN AGAINST A MUTATION THAT DELETED THE THING IT CHECKED — `/reps <= 0/` matched the **comment I had just written above the fallback** explaining the guard. Same shape as the earlier "no slot is named in the shape decision" false pass. It now matches the condition itself.

- [x] **THE POST-ONBOARDING CHAT OPENER IS ONE MESSAGE, DOWN FROM FOUR** (Ashley: *"we dont need to say that much and it could be 1 message"*). Three went for a reason, not just for length: "I'm your coach" repeated the header that says so permanently; **"Talk to me like you'd talk to a person"** was hers to cut — *"as far as the user is concerned it is a person so I dont like this wording"* — and naming the thing it is pretending not to be is the one line that breaks it; and "nothing moves without your say-so" had already been said minutes earlier in the onboarding opener.

- [x] AND IT NOW KNOWS WHEN DAY ONE ACTUALLY IS. Ashley's sketch was "Day one starts right now with {session}", which is wrong for anyone whose first training day isn't today — and the code it replaced was worse there: it fell through to *"it's a rest day on your plan. How's the recovery going?"*, asking someone who finished onboarding ninety seconds ago how they were recovering from nothing. Four branches now: today, a training day past their usual training hour ("ready when you are — tonight or tomorrow, your call"), a named future day, and a no-session fallback that invents nothing. The first quick reply follows: **"Talk me through day one"** when it isn't today, because "Talk me through today" under "Day one is Monday" is the app contradicting itself on one screen.

- [x] **NOT EVERY QUESTION NEEDS BUTTONS — Ashley narrowed the always-offer ruling after living with it:** *"I dont think every question needs a quick reply always. Some have obvious answers."* The line she picked: buttons stay wherever the app has its own wording for the answer — nobody can guess where "moderate" ends and "active" begins, or that `home_gym` means barbell + dumbbells + bench — and go where a person already knows what to type. Four drop them: **do you know your lifts, snacks or meals only, male or female, how many meals a day.** The other fourteen keep them. This narrows the earlier ruling rather than undoing its reasoning: options had become invisible, which is not the same as every question wanting them.

- [x] IT SUPPRESSES THE FIRST OFFER ONLY, and that is the whole safety of it. Two doors stay open, both the original "chips as a rescue" idea: an answer that fails to map re-asks with the real options attached, and the stuck-rescue offers them to anyone who says "I don't know". Nothing becomes unanswerable by tapping — it stops being the opening move. **`injuries` and `dietaryPreferences` can never be marked obvious**, whatever they look like: both are enforced downstream, so a missed tap there is a filtering miss or a food the plan will serve them, and `test:onboarding-chips` fails if either is ever flagged.

- [x] **THE COMPOSER WAS DESCRIBING A DIFFERENT QUESTION THAN THE ONE ON SCREEN**, photographed twice in one run: *"what should I call you?"* with `Tell me your goal…` underneath, and a question about training style with `How active is your day?`. Reproduced in a real browser before touching it (`npm run verify:composer`, which mounts the actual `ConversationalOnboarding`, not a replica) and both rows came back wrong. The name one could never have been right: `displayName` is in `NEVER_BLOCKING_SLOTS`, so it is in neither open-slot list, so on the app's very first question the hint was **guaranteed** to name a different question. Now the placeholder reads the conversation — the live chip card, or the opener's own tagged name ask — and only falls back to the slot list when nothing is on screen. All three cases match after.

- [x] **THE QUESTION ORDER THIS APP CAREFULLY MAINTAINS WAS BEING RE-SORTED BEFORE THE COACH EVER SAW IT.** Every caller built the outstanding-question list as `[...missingRequiredSlots, ...unconfirmedOptionalSlots]`, and that list is `remaining` — literally what the model is told is left to ask. Required-first is not the app's order. Measured: **injuries 5th → 11th**, mealsPerDay 15th → 10th. Injuries is the one that matters — it is `required: false` only because a plan can be built without it, and Ashley moved it from 16th to 5th precisely so it could not be stranded behind the questions people abandon on. A safety ruling was being quietly undone by a concatenation. `openSlotsInOrder` now returns the same membership in the file's own order; injuries is 5th as sent.

- [x] SAYING "I'M STRUGGLING TO LOSE WEIGHT" GOT NO ACKNOWLEDGEMENT — the coach took the 12% number, said what it meant for the plan, and asked the next question. The prompt already had a "be on their side out loud when something sounds hard" rule; it lost to the anti-flattery ban sitting next to it, which is emphatic and carries a live example about this exact "12% body fat" message. The two are now ordered explicitly: **a difficulty always gets a reaction and it outranks the no-grading rule**, one clause, no pep talk and no promise about results, then straight on with the question. **Needs the edge-function deploy to take effect.**

- [x] **THE SAME DEPLOY WENT TO THE WRONG SUPABASE PROJECT TWICE, AND THE SECOND TIME THE SANCTIONED ROUTE COULD NOT RUN ON ASHLEY'S MACHINE AT ALL.** `npm run db:link-prod` was `bash scripts/db-link-prod.sh`. Her shell is Windows PowerShell, where `bash` is not on PATH — it printed `'bash' is not recognized`, the link never happened, and `npx supabase functions deploy` ran anyway against TEST. The first time had a different cause (the docs said the ambient link was PRODUCTION; it is TEST) and the identical outcome: a confident `Deployed Functions on project vswuurrtbzbrgubddefv` line that reads exactly like success. **The always-offer-options prompt is still not on production.**

- [x] THE COMMON CAUSE IS THE TWO-STEP, NOT THE TYPO. Nothing connected "link to production" to "deploy", so when step 1 failed, step 2 still ran against whatever was linked before. Fixed mechanically rather than by another instruction to read the output line — "read carefully" had already failed twice as a control. **`npm run deploy:functions:prod -- onboarding-chat`** now asks for the phrase, links, deploys with `--project-ref` naming the target *on the deploy call itself*, and relinks to TEST on the way out. Proven with a recording stub standing in for the CLI: a failed production link produces **zero** deploy calls and still returns the CLI to TEST.

- [x] ALL FOUR `scripts/db-*.sh` ARE NODE NOW — Node is a hard dependency of this repo, bash was an assumption about the developer's shell, and it was false for the only person who needs to reach production. The npm command names are unchanged, so CLAUDE.md stays true. `test:deploy-path` holds it: no npm script may shell out to bash, production costs the typed phrase, a non-interactive stdin is refused rather than left hanging, and exactly one file names the live project ref.

- [x] ONE TRANSLATION BUG THE PORT ALMOST SHIPPED WITH, CAUGHT BEFORE COMMIT: bash's `trap … EXIT` fires on every exit path; its obvious Node equivalent, `try/finally`, **does not** — `process.exit()` skips finally blocks entirely. A script that bailed out mid-production-leg would have left the CLI pointed at the live database, the exact state these scripts exist to prevent. The relink is a `process.on('exit')` handler, and the gate asserts that specifically.

- [x] MY OWN GATE FIRED ON FIVE SMOKE SCRIPTS AND THE GATE WAS WRONG, NOT THE SCRIPTS. "Only one file holds either project ref" went red on `smoke-check-week`, `smoke-regen-week` and three `verify-*-live` scripts. Reading them showed every hit is a *refusal guard* — `if (!url.includes(TEST)) { Refusing… }` — a script declining to run anywhere but TEST, and a guard that imports its own definition of "safe" is one a bad edit can switch off everywhere at once. None held the production ref. The check is now the narrower, stronger property: nothing but `db-target.mjs` may name PRODUCTION, and every other ref mention must sit in a refusal guard.

- [x] **THE TYPEWRITER WAS TOO SLOW AND NOT SMOOTH — TWO PROBLEMS, DIFFERENT CAUSES, AND GUESSING WOULD HAVE FIXED THE WRONG ONE.** Measured in a browser with a MutationObserver recording when each word actually lands, not when its timer fired. **Slow** was the tick: 110ms/word, so one 67-word coach reply took **8.9 seconds**. **Not smooth was NOT timer drift** — min 110ms, median 110ms, p90 111ms, the tick was rock steady. It was the sentence-end pause: **380ms on top of the tick, producing 491ms gaps four times a message.** Steady, steady, steady, stall.

- [x] FIXED BOTH, AND MEASURED AGAIN: total **8907ms → 3681ms**, gap max **491ms → 133ms**, jitter (std dev) **90ms → 19ms**, gaps over 150ms **4 → 0**. The pause ratio came down from ~3.45× the tick to ~1.6× — still a beat at a full stop, no longer a halt. `normal` lands at 50ms, near the app's *original* default; it had been doubled to 110 on an earlier "roughly double it" request and the request came back the other way after living with it. `slow` sits at 7.0s, close to what the old default did, so anyone who liked that pace has it one tap away rather than lost.

- [x] **THE REVEAL IS A FRAME LOOP AGAINST AN ABSOLUTE CLOCK NOW, NOT A CHAIN OF setTimeouts** — and that half is for real devices rather than the bench. The old version scheduled each word from the moment the previous one *rendered*, so every late timer pushed the rest of the message further behind with no way to recover; error accumulated. Now the whole schedule is computed once up front and each frame asks "which words are due by now" — a stalled frame catches up on the next instead of falling permanently behind, and updates land on frames rather than between them. Headless Chromium has far more even timers than a phone mid-scroll, so the honest reading of that steady 110ms median is *"this bench cannot see drift"*, not *"drift does not exist"*.

- [x] `test:reveal-timing` holds both halves — the frame loop and the timing budget — and `npm run verify:reveal` re-runs the measurement. Proven to bite: reverting `normal` to 110/380 turns four checks red, and swapping the frame loop back to a setTimeout chain turns two more.

- [x] ONE CHECK OF MINE HAD THE WRONG SHAPE AND I LOOSENED IT, SAYING SO. An absolute "worst gap under 150ms" fired on `slow` at 245ms — but against slow's own 95ms tick that is 2.6×, the identical proportion to normal's 130ms against 50ms. The absolute number was encoding *"the default must feel fluid"*, which is a claim about `fast` and `normal`; the ratio check already covers all three. Slow is exempt by name, with the reason in the file, rather than the threshold being quietly raised.

- [x] **SECOND PASS ON THE ONBOARDING — WALKED ALL 27 SCREENS AT 390px AND FIXED WHAT THE WALK SHOWED.** Nothing overflows a phone now, which is the structural check; the rest were things only visible by looking.

- [x] **THE TRAINING HALF WAS SPLIT DOWN THE MIDDLE.** Style, cardio and recovery are training questions and sat at #17-19, *after* the four body metrics — so the flow went training → body → training → food, a context switch out and back for no reason. They move up with days and session length. The body block stays intact and stays ahead of the food half, so the rule the order gate enforces is untouched. `dislikedExercises` came with them from **#27**, where it had been stranded seven questions past anything it related to.

- [x] **THE LAST REQUIRED QUESTION WAS #22, BEHIND THE TALLEST SCREEN IN THE FLOW.** `mealsPerDay` sat after `dietaryPreferences` and `dislikedFoods`, so the biggest friction point stood between the user and a plan — give up there and nineteen answers bought nothing. It moves to **#21**, leaving six genuinely optional questions after it: abandon any time from there and a full plan still generates.

- [x] TWO QUESTIONS READ AS THE SAME QUESTION ASKED TWICE, and moving them next to each other made it obvious: dietary's composer hint was literally *"Anything you avoid?"* and the next question was *"Any foods you just won't eat?"*. Dietary's hint now names what the tag list is for (*"Vegan, gluten-free, an allergy…"*) and the follow-up opens with **"Anything else"**. Deliberately not narrowed to "don't like the taste of" — `detectAllergenTags` reads that field, so it must stay somewhere an allergy can be typed.

- [x] AND ONE QUESTION WAS ITS OWN PLACEHOLDER: `conditioningPreference` showed *"How do you feel about cardio?"* in the composer, under *"How do you feel about cardio?"*. `dislikedExercises` had no placeholder at all. Both gated now, across every slot.

- [x] **FOUR LAYOUT FIXES THE MEASUREMENTS COULD NOT HAVE FOUND** — they needed the screenshot. (1) An odd number of cards stranded the last one at half width beside an empty cell; five questions have exactly three options, so it was most of them. The last card spans now. (2) A full-width 44px "Pick at least one" bar under one tidy row of pills was the heaviest thing on the lightest screen — the footer follows the option shape now, pill under pills. (3) `mealsPerDay` showed the **same plate emoji three times** under "2 meals / 3 meals / 4 meals" (364px → 276px once dropped) — same judgement as the pill rule, applied to icons. (4) Numeric fields carried no unit: a box labelled "Height" with placeholder "100–250", under a question that has scrolled — and the grouped cards put three fields under one question, so at most one could ever have taken its unit from the sentence above.

- [x] ONE THING I NEARLY REPORTED AS A DEFECT AND WAS NOT: the three barbell questions each render a card showing all three fields. That is `NUMERIC_GROUPS` working — Save resolves the whole group in one call, so the coach asks once. The harness renders each slot standalone, which is a harness artifact. Checked the resolve path before writing it up.

- [x] AND THE GATE CAUGHT ITSELF AGAIN: `no slot is named in the shape decision` fired on a **comment** that named `mealsPerDay` as an example of what the rule produces — the opposite of the failure it exists for. It strips comments now and asserts against the shape logic only.

- [x] **"SOMETIMES THE QUICK REPLIES COME UP AND SOMETIMES THEY DON'T" — THEY WERE DOING EXACTLY WHAT THEY WERE TOLD.** `present_slot` read *"This is a RESCUE, not the way questions are asked… Never call it on the first asking"*, so whether you got help depended on the model judging you stuck. Unpredictable from the user's side and unmeasured from the codebase's — nothing asserted anything about it either way. Ashley's ruling: offer them on every closed-set question, first asking included.

- [x] **THE OLD RULE WAS RIGHT ABOUT THE CAUSE AND WRONG ABOUT THE FIX.** It existed because chips-under-everything made onboarding feel like a form. Measured at 390px, the culprit was SIZE, not existence: **goal 213px, cardio 260px, days 308px, injuries 308px, dietary 771px** — the dietary question alone taking 771px of an 844px screen, before the coach's message above it or the keyboard below. And all seven training-day cards carried the **same 📅**: seven identical icons, each in a box sized to hold one, to say "Mon Tue Wed".

- [x] SO THE SHAPE IS NOW READ OFF THE SLOT'S OWN DATA: **a card when the options carry a description, pills when they don't.** Not a hand-list — the description is exactly what a card exists to show. It lands where it should: cards keep goal, experience, activity, equipment, session length, style, cardio, recovery, meals, cooking, snacks, breakfast and the lifts question (all ≤4 options); pills take days, injuries, dietary, cuisines and sex. **Dietary 771px → 223px, days and injuries 308px → 96px.** The pill is the coach chat's quick-reply shape exactly — rounded-full, surface-raised, text-xs, 44px tap target — because a new user meets both surfaces within minutes.

- [x] **INJURIES MOVED 16th → 6th** (Ashley's ruling), straight after equipment. It is the only question whose absence can HURT someone rather than cost accuracy, and at 16th it sat behind the age/height/weight/sex block people most often abandon on — so someone who answered eleven questions and stopped got a complete plan with no injury filtering. Above the barbell chain deliberately: asking for a deadlift number before someone has had a chance to mention their back is the wrong order.

- [x] **THE OPENER: 88 WORDS → 46, THREE MESSAGES → TWO.** The one that went was the capability pitch. Not because it was bad but because its job moved: the post-onboarding tour now demonstrates those same four things on the real screens, and arguing for the app before showing it is the weaker half of that pair. *"Nothing changes without your okay"* was kept deliberately and moved beside the name question — the one line that is reassurance rather than sales.

- [x] A GATE WAS ASSERTING THE OLD RULE AND WENT RED, CORRECTLY. `test:onboarding-conversational` §2–3 locked in *"chips are described as a rescue"* and the three stuck cases. Inverted rather than deleted, with the reversal and its reason recorded in the gate itself — the fact that a documented decision was overturned is the interesting part, and a deleted section says nothing. New `test:onboarding-chips` owns the shape rule, the word budget on the opener, and the injury position.

- [ ] **NEEDS `npx supabase functions deploy onboarding-chat` ON BOTH PROJECTS** — the always-offer rule is a prompt change and is inert until deployed. Sanctioned path in PROJECT-LOG §7.3: bare deploy hits TEST, production needs `db:link-prod` first.

- [ ] **NOT VERIFIED, AND IT IS THE THING THAT DECIDES WHETHER THIS WORKED**: model compliance. The prompt now says call `present_slot` every time, but no conversation has been run against it — `*.supabase.co` is unreachable from the sandbox. If the model skips the call on some turns, the inconsistency Ashley reported survives in a milder form. A deterministic client-side backstop was considered and NOT built: the client would have to guess which question the sentence asked, and the code is explicit that options under the wrong question are worse than none. Measure first, then decide.

- [x] **EVERYTHING FROM THIS SESSION IS LIVE**, 27 Aug 2026. Frontend `94508e1 → 841fce6` on `main` (Vercel): the onboarding intro and Ashley's opener, `tone:compare`, the injury-substitution and calf-ceiling fixes, hips/elbows/ankles filtering, the band-slots property rewrite, hip and lower-back rehab, and the post-onboarding tour. Edge function: `onboarding-chat` on production for the warm-voice prompt. No migrations in any of it — `db:push-both` was not needed and was not run.

- [ ] **THE TONE MEASUREMENT IS NOW UNBLOCKED AND IS THE LAST OPEN THREAD.** It has been outstanding all session for a reason that no longer holds: the new prompt was undeployed. Both projects now have it, and `.env.local` points at TEST, so `before.json` (captured against TEST while it still ran the OLD prompt) and a fresh `after.json` are the same endpoint, same persona, one variable. Run `npx tsx scripts/probe-onboarding-tone.mts scripts/probe-personas/conversational.json after.json` then `npm run tone:compare -- before.json after.json`. Watch the two guard rails first — empty replies must stay 0 (the fa683fc failure shape) and graded answers must stay 0 — before reading any warmth number.

- [x] **RESOLVED — `onboarding-chat` IS ON PRODUCTION**, 27 Aug 2026. Verified from the CLI output, not inferred: `Deployed Functions on project sdkhuczcfnqqimdgfiks: onboarding-chat`, and `db:link-test` relinked to the safe default afterwards. Both projects now carry the warm-voice prompt (TEST got it from the first, mistargeted deploy). Original finding follows.

- [x] **THE ONBOARDING TONE DEPLOY FIRST WENT TO TEST, NOT PRODUCTION.** The CLI printed `Deployed Functions on project vswuurrtbzbrgubddefv` — that is TEST. The ambient link is TEST *by design*, and the 27 Aug production deploy deliberately relinked back to it afterwards, so a bare `functions deploy` has pointed at TEST ever since. `223d3c7` (the warm-voice prompt) is **not** an ancestor of `94508e1`, so production is still serving the old onboarding prompt. Live path: `npm run db:link-prod` → `npx supabase functions deploy onboarding-chat` → `npm run db:link-test`.

- [x] **PROJECT-LOG §7.1 SAID THE OPPOSITE, AND THAT IS WHY THIS WAS EASY TO MISS.** It claimed `.env.local` was `sdkhuczcfnqqimdgfiks` and that `functions deploy` targets it. Both false: `.env.local` is **TEST** (added 12 Aug, the day after the split), `.env` is a **third, older** project (`aoksyzjrrikvuhxatljy`), and the CLI link is TEST. Corrected in place with the original kept, because its last sentence — *"don't assume; check which file actually supplied the URL"* — was exactly right even as its facts went stale. §7.3 now gives the three-command production path.

- [x] AND IT CORRECTS A SECOND STALE CLAIM: the 27 Aug note said the tone probes and `test:meal-quality` read `.env.local` and would therefore "create profile rows on the live database". They read `.env.local`, which is TEST. Combined with the earlier finding that `onboarding-chat` performs no DB writes at all, those probes were never a production risk in either respect.

- [ ] WORTH ONE LOOK, NOT YET CHECKED: `.env.local` is **committed**, not gitignored, and points at TEST. Vite prefers real process-env vars over `.env` files, so Vercel's own dashboard variables should win at build time — but if they are unset, a production build would take the TEST URL from this file. Cannot be verified from the sandbox; confirm `VITE_SUPABASE_URL` is set in the Vercel project.

- [x] **THE POST-ONBOARDING TOUR IS BUILT — "TAP-TO-LEARN", TEN STOPS.** From Ashley's design handoff. It never renders a mock of a screen: it measures a real element by `data-tour` key, cuts a hole in a scrim, and lets the real app take the tap. So every stop shows the CURRENT app with the user's own plan in it, and the nav stops advance because the hash actually changed. The set at stop 7 is a genuine week-1 calibration set — the point, not a side effect. Ends in Chat where `buildFirstRunIntro` takes over.

- [x] **TWO REAL DEFECTS, BOTH FOUND BY DRIVING IT IN A BROWSER, NEITHER VISIBLE TO ANY STATIC CHECK.** (1) The tour detects "the set saved" by `data-tour="setrow"` disappearing — and detected "today is a rest day" by *the same absence*, so logging the set skipped the stop the user had just completed and renumbered to "of 9" behind them. (2) `bringIntoView` tried to bring the fixed tab bar to 130px from the top; being fixed it never moved, and the whole page scrolled 650px underneath it instead.

- [x] SO `npm run verify:tour` EXISTS: the real component driven in the pre-installed Chromium over CDP, using Node's built-in WebSocket so it adds no dependency. 34 checks — the tap phase blocks every non-target and passes the tap through the hole, the set stop advances only on a real save, skip stores the place and the pill resumes it, Finish writes `done` and a reload does not restart it. `test:app-tour` covers the half a browser cannot: that the keys exist on the right elements.

- [x] A THIRD FINDING WAS THE HARNESS'S OWN FAULT, and it is worth recording because it looked exactly like a real bug: rooted at the harness folder, Tailwind never scanned `src/`, so `inset-0` and `z-[60]` were absent from the bundle and the overlay rendered unpositioned — reported as "clicks are not blocked". **A harness has to be built the way the app is or its verdict is about the harness.** Diagnosed by diffing the two CSS bundles for `.inset-0` rather than by reasoning.

- [x] **THE GATE CAUGHT ITSELF FIRST.** `test:app-tour` scans for `data-tour` literals, and the tab bar sets its own through a `TOUR_KEY` map so a new tab cannot silently go untagged — so the first run reported all four nav targets missing. They were not; the check was. It reads the map now, and checks BOTH directions: an orphan attribute nobody points at reads as wired and is a failure too. Proven to bite: renaming `hero` turns two checks red.

- [ ] NOT VERIFIED HERE, AND IT IS THE HALF THAT MATTERS MOST: the tour has never run against the real screens. The harness uses stub targets, and `*.supabase.co` is unreachable from the sandbox, so a fresh TEST profile through the actual onboarding UI — the handoff's own instruction — is still Ashley's machine. What is unproven: that each key sits on the element the copy describes, the rest-day variant, and the callout's placement against real content.

- [ ] The handoff names `exercise/ExerciseRow.tsx` for the set-log ✓; it is actually in `exercise/SetGrid.tsx`. Tagged there.

- [x] **`test:injury-rebuild` WAS TESTING RANDOMNESS, AND MY FIRST FIX FOR IT WAS TOO.** Its "the two modes genuinely differ" check read `rebuiltSlots > subSlots` — a proxy that tested "rebuild is not a no-op" by way of substitution DROPPING slots it could not safely fill (436 vs 432). The four new hip movements are safe for a shoulder injury, so substitution can now fill those four and both modes return 436. The check went red **because the app got better**: a shoulder-injured trainee's substituted plan is fuller than it was.

- [x] I REPLACED IT WITH A CONTENT DIFF AND THE MUTATION SAID NO. Comparing the two modes' exercise names showed 332 of 436 slots differing — but mutating the substitute call to `'rebuild'`, so both did the same thing, **still showed 252 differing** and stayed green. `executeLastingInjury` is non-deterministic: two identical rebuilds diverge by shuffle alone. The check was measuring the RNG. Both calls now run under one seed, and the same mutation reads 0 of 452 and turns two checks red. Comparison is over the common prefix only, since the modes legitimately return different lengths and counting positions past the shorter array against `undefined` is not a content difference.

- [x] THAT IS THE THIRD COUNT-PROXY IN THIS AREA TO GO FALSE WITHOUT ANYTHING BREAKING — after `test:band-slots`' frozen 335 and its rehab total. The shape is consistent: a number that stands in for a property drifts when the catalogue grows, and the honest repair is to assert the property. Worth checking the remaining gates for the same shape.

- [x] **A BAD HIP AND A BAD BACK GET REHAB NOW, NOT JUST SUBTRACTION.** Both measured at **0 of 576 training days** and **144 of 144 plans with none at all**; both are now **576/576** and **0/144**. Knees and shoulders unchanged at 576/576, and the uninjured control still 0/576 — the slot does not fire for people who reported nothing. The back rotates near-perfectly evenly (Bird Dog 193, Side Plank 192, Dead Bug 191); hips rotates across Clamshell, Side-Lying Hip Abduction and Standing Band Hip Abduction.

- [x] THE LOWER BACK WAS TAGGING; **THE HIP WAS NOT, AND I SAID OTHERWISE.** I told Ashley both were "mostly labelling what's already there" — true for the back, false for the hip, and she chose scope on that basis before I corrected it. How I got it wrong: I searched the catalogue for hip-rehab-sounding *names* (Glute Bridge, Hip Thrust, Step-Ups all matched) and reported the count without checking them against the mechanism that would prescribe them. `pickRehabMovement` excludes `tier2_compound` outright — Ashley's own ruling — and **all four are tier2_compound**. Tagging them would have been a no-op that read as a fix. Same shape as the Hip Thrust and tone-probe errors: reasoning about a file instead of reading the contract it must satisfy.

- [x] SO THE HIP GOT **FOUR NEW ENTRIES**, not tags: Clamshell, Side-Lying Hip Abduction and Standing Band Hip Abduction as `activation` primers (matching the seven shoulder-rehab entries rather than inventing a shape), plus Bird Dog. All bodyweight or band, so minimalist and bodyweight tiers get them too — a hip is not only injured in a commercial gym.

- [x] **DURATIONS AND TIERS ARE LOAD-BEARING HERE, AND THE BAND IS SHARED ACROSS JOINTS.** The slot keeps everything within 1.25× the cheapest indicated movement, so 30/30/32 gives the hip a three-way rotation. Pallof Press was deliberately NOT tagged for the back: at 30s it would have become the band's only member and excluded Dead Bug and Side Plank, reducing variety to one — the documented 576/576 monotony failure. Bird Dog stays tier3 for the same class of reason: the picker takes the primer tier *exclusively*, so making it a primer would have left it alone in the back's band and collapsed the rotation it was added to widen.

- [x] **THREE GATES WERE ASSERTING THE OLD GAP AS CORRECT** and went red on purpose. `test:rehab-prescribed` used `'hips'` as its example of an unmapped code (now swapped to `ankles`, which is the stronger case — a joint the app knows about but cannot treat) and asserted only "the two joints this feature covers"; `test:joint-tag-states` listed `lower_back` among "injuries not yet reviewed". Its six repeated `['shoulders','knees']` literals became one named list, because six places to update is how hips and the back would have stayed untested while the gate went on passing.

- [x] AND ONE GATE CAUGHT A REAL DEFECT IN MY OWN ENTRIES — *"every indicated movement records the joint it loads, honestly"*. Dead Bug, Side Plank and Bird Dog claimed to be back rehab while recording that they load nothing. Fixed by declaring `loads_joints`, **with an explicit `contraindicated_joints: []` alongside**: that getter is `contraindicated_joints ?? loads_joints`, so naming the joint without it would have marked each movement dangerous for the very joint it treats — the `31b05d7` trap running the other way. Asserted now: a bad back still excludes exactly 12 things, and the legacy path diverges by exactly those three names.

- [ ] **"LOADS YOUR LOWER BACK AXIAL" WAS ALREADY SHIPPING.** Both injury sentences rendered joint tags with `.replace(/_/g,' ')`, and one tag is not a phrase. Anyone with a bad back looking at a conflicting exercise read that today — a pre-existing copy bug this work would have doubled rather than caused. Now goes through `jointListDisplay`, so it reads "Loads your hip, knee and lower back". Same rule as the meal refusals: English, never tag names.

- [ ] FOUND EN ROUTE, NOT FIXED: **core work categorises as `isolation_chest`** — Dead Bug, Plank, Side Plank, Pallof Press, Wall Sit and now Bird Dog, via the `default:` case at `load-prescription.ts:394`. Pre-existing and verified **inert** rather than assumed so: every one prescribes "Bodyweight" with `starting_weight_kg` null, so the wrong category never becomes a wrong weight. Frozen honestly in `test:categorize-precedence` — recorded as it is, not as it ought to be.

- [ ] **FOUR INJURIES STILL GET SUBTRACTION AND NO REHAB**: ankles, elbows, wrists, neck. Down from six. Ashley's call, and content rather than mechanism — for ankles there is nothing suitable at all (the calf raises are exactly what you would avoid), and neck work is the one worth most caution about the app prescribing. `test:band-slots` freezes the covered list so filling any of the four turns it red on purpose.

- [ ] **SIX OF THE EIGHT INJURIES GET SUBTRACTION AND NO REHAB.** Measured across the full grid at HEAD: a knee-injured trainee gets a knee-indicated exercise in **432 of 432** profiles, a shoulder-injured one in **432 of 432** — and a bad **hip, elbow, ankle, wrist, neck or lower back** gets one in **0 of 432**, on every equipment tier. Not a selector bug: `indicated_joints` is tagged on exactly two joints in the whole catalogue (`knee` 10 entries, `shoulder` 9) and the other six have nothing to select. So the app removes what would hurt those joints — the hips/elbows/ankles work just extended that to three more — and prescribes nothing back. **Ashley's call**, and a content decision, not a mechanical one: which exercises, and whether the app should be prescribing rehab for a neck or a wrist at all. `test:band-slots` now freezes the two-joint list so filling any of the six turns it red on purpose.

- [x] **`test:band-slots` HAD BEEN RED ON MAIN, AND ITS NUMBER IS NOW ROOT-CAUSED.** It froze a raw total — rehab bands in main slots ≥ 335 — which drifted three times (348 → 340 → 318), each drop benign, each one re-breaking the gate. Bisected: it held at 340 through `a1b894e` and fell to 318 at **`987531f`** ("Prefer the best tool the trainee owns"). `isEquipmentQualityExempt` spares a rehab band the −1 penalty, but an exemption only zeroes the band's *own* factor — it does not stop a barbell rival collecting +1, so 22 placements lost a main slot to a better implement. Every one was Spanish Squat.

- [x] IT COST NOBODY THEIR REHAB, which is the distinction the total could not draw — 318 reads identically whether every trainee got one or half got two. Per profile it is 432/432 covered and 432/432 in a main slot; the drop is weeks that carried Spanish Squat twice now carrying it once. **Scale change:** the headline is a share of profiles now, not a placement count, so it is not comparable to the 348/340/318 series and the 335 floor is gone. Denominator went **up**, 48 profiles on one injury → 432 on each of two, read off the sweep that already runs. Proven to bite on three mutations.

- [x] **A CORRECTION USED TO SIT ALONGSIDE THE MISTAKE INSTEAD OF REPLACING IT** — the worst of four bugs Ashley hit in one exchange, because it silently corrupted her training log. She logged deadlifts, the reps came out wrong, she said *"No 3x10 deadlifts"* — and the app wrote sets 4, 5 and 6 next to the wrong 1, 2 and 3. Her Exercise tab read **"3 working sets · 6 logged"**, and every future weight for that lift would have built on three sets she never did. `log_workout` now takes `corrects_previous`, which clears that exercise's sets for the day before writing, so the log refills from set 1.

- [x] APPENDING IS STILL THE DEFAULT AND MUST BE — "I did three more sets" is the common case and `allocateSetNumbers` was right about it. The bug was never the appending; it was that nothing upstream could tell a correction from extra work. The gate asserts BOTH directions, and asserts the flag cannot half-apply: with no `deleteSet` to hand it appends and honestly reports nothing replaced, rather than claiming a correction it did not make.

- [x] **THE RECEIPT NOW SAYS WHICH IT DID.** A correction and an addition write identical rows, so an identical receipt makes a mis-called correction invisible until the numbers are wrong weeks later. A replacement is titled "Corrected ·" and names the count replaced, while Undo is still one tap away.

- [x] **THE APP INVENTED A NUMBER SHE NEVER SAID.** Asked "how many sets and reps?", she answered **"3"** — and it logged **3×8**, because 8 was the PRESCRIBED rep target. `weight_kg` had carried a hard guard for a while (*"ONLY if the user actually stated one… do NOT guess or default to 0"*) and `exercise_name` had one too (*"Never invent/guess one from context or history"*). **Reps had none**, and were required, so the model had to supply something. Now guarded identically on both logging tools, and required precisely so an unstated rep count blocks the call instead of being filled in.

- [x] TWO MORE FROM THE SAME SCREENSHOT, both prompt-side: *"No 3x10 deadlifts"* states the exercise, the sets AND the reps, and the coach discarded all three to ask for a weight — so a rule now says carry every stated value into the call and ask only for what is genuinely missing. And it asked **"What weight did you use for Deadlifts?" three times running**, taking "100kg" each time, so a rule now forbids repeating an identical question: say what you have and what is missing, or ask something narrower.

- [x] THE BEHAVIOURAL HALF IS TESTED FOR REAL, not grepped — `test:log-correction` runs the executor against an in-memory set store and asserts the log holds 3 sets and not 6, that they carry the corrected reps, and that they refill from set 1. Proven to bite on three mutations; disabling the replace path reproduces Ashley's screenshot exactly, `[8,8,8,10,10,10]`.

- [x] **CELERY, SESAME, MUSTARD, LUPIN AND SULPHITES ARE ENFORCED NOW** — five legally-declarable allergens that could previously only be REMEMBERED. And it was worse than "no mechanism": **celery, mustard, sesame oil and sesame seeds were real, servable entries in the food database with empty tag sets.** Not theoretical. All five now have tags, rules, options and prompt text, taking the app from 17 dietary preferences to 22.

- [x] **THE PARITY GATE WAS WRITTEN FIRST, BEFORE ANY TAGGING, AND WAS RED ON ALL EIGHT CHECKS.** The food database exists TWICE — `src/lib/food-db.ts` (332 foods) and `supabase/functions/_shared/food-db.ts` (323), because a Deno edge function cannot import across the `src/lib` boundary — and nothing kept them in step. A tag added to one protects nothing in the other, and the result would have looked finished. `test:food-db-parity` proves both copies agree on every allergen tag; proven to bite by untagging sesame seeds on the server side only, which is precisely the half-safe failure it exists for.

- [x] IT TOOK FOUR PLACES, NOT TWO, and `test:diet-tag-sync` caught the one I missed: `FORBIDDEN_TAGS`, both food-db copies' tags, **generate-meals' prompt block**, and **chat-gemini's prompt block**. A preference absent from either prompt is one the model is never told about however well it is tagged. The 17→22 count assertion in that gate is a deliberate tripwire and it worked exactly as designed.

- [x] **A GATE WAS ASSERTING THE GAP AS CORRECT.** `test:onboarding-slots` contained *"the five untagged allergens (no enforcement mechanism exists) are correctly never returned"* — and passed. A gate that describes a hole as intended is how the hole survives: it makes the missing thing look deliberate and defended. Now inverted: the same disclosure must return all five.

- [x] THE DETECTOR MISSED AN ORDINARY PHRASING, and the fix reaches all twelve allergens rather than the five. "Sulphites give me a reaction" scanned completely clean — the signal list matched the noun form "reaction to" but not "gives me a reaction" or "I react to". Broadened deliberately: this governs every allergen, and a missed disclosure is a worse failure than a tagged food someone could have eaten.

- [x] LUPIN FLOUR WAS ADDED TO THE DATABASE, and that is a safety decision rather than a gate-satisfying one. **An ingredient absent from the database cannot be tag-checked at all** — a proposal containing lupin flour was unfilterable by construction. `test:diet-tag-sync` [11] independently forbids a rule with zero data (the `is_grain` bug, where a rule read correct and never fired), and this satisfies it honestly rather than by exemption.

- [x] **AND ONE OF MY OWN NEW CHECKS WAS A LIAR.** A section landed AFTER the `if (failures > 0) process.exit(1)` line, so `test:onboarding-slots` printed a ✗ on screen and exited 0. A gate that reports pass while showing a failure is worse than no gate. The epilogue now sits at the end of the file, and the same run exits 1 as it should.

- [x] FOUND EN ROUTE: **choosing "Mediterranean" did nothing whatsoever.** It has no hard exclusions by design — it is a style, not a restriction — but it was also absent from the meal generator entirely, so the option was offered and silently ignored. It now gets a positive style steer, phrased so it cannot read as a ban. The new coverage check found this on its first run.

- [ ] `test:meal-quality` cannot run from the sandbox — it creates a throwaway profile against the TEST project and `*.supabase.co` is unreachable here. Verified pre-existing by stashing the changes and re-running. It is the gate that would prove end-to-end that a tagged allergen never reaches a plate, and it needs Ashley's machine.

- [x] **THE SOFT PREFERENCES ARE READ BY SOMETHING NOW** — they were compiled and read by nothing. "I prefer chicken to fish", "not a fan of burpees but I'll do them" were recorded, compiled by `compileSoftExercisePreferences`, and had **zero call sites** outside the file defining them. The comment above it said *"scoped to swap-candidate ranking only (mesocycle-edit.getReplacementCandidates)"* — describing a consumer that did not exist. A truthful-looking comment over dead code is how the next person gets misled, which is exactly what happened when the capture trace first read it.

- [x] NOT A NEW DECISION — VISION-ARCHITECTURE.md §1.2 already ruled it: soft exercise dislike/like → *"ranking"*, *"scoped to `getReplacementCandidates` only"*, rotation explicitly unaffected (the doc even records why rotation is out of reach — threading a ranker through `rotateVariation` means changing two exported signatures, `generateMesocycle`'s parameter list, and the audit's independent copies). Wiring it executes that ruling rather than inventing one.

- [x] **IT REORDERS, IT NEVER REMOVES, and that is the line the gate defends.** A lean is not a ban: a movement someone is lukewarm about sinks to the bottom of the swap list and is **still offered**, because they may well pick it anyway. A hard dislike is a different channel entirely — it becomes an exclusion — and `test:soft-preferences` asserts a hard fact can never leak into ranking, where it would leave a banned movement on the menu. Proven to bite: making a soft dislike filter instead of sink turns two checks red.

- [ ] **THE FOOD HALF IS STILL UNREAD.** `compileSoftFoodPreferences` has the same zero call sites, and §1.2 says it belongs in `assembleDay` candidate scoring. Not done here — meal generation is a different subsystem and deserves its own pass rather than being tacked onto an exercise change.

- [x] **`test:audit` IS BACK TO 0 / 13,967** — the two `rotation_relative_load` failures Ashley chose to ship around are fixed. But the audit's description of them was wrong, and the real defect is both simpler and a hundred times larger.

- [x] **THE SAME LIFT, IN THE SAME WEEK, WITH IDENTICAL SETS/REPS/RPE, WAS PRESCRIBED TWO DIFFERENT WEIGHTS.** Not a rotation problem at all. `Calf Raises 3×15-20 @ RPE 6-7` came out at **12.5kg on one day and 20kg on another in the same week** — a user opening the app has no way to tell which is right. The audit only compares consecutive weeks at the same slot INDEX, so it saw just the 2 cases where the second instance happened to land in a slot that changed hands. **Measured across the audit's own 4×4×4 sweep: 202 of 1,536 lift-weeks, not 2.**

- [x] IT IS THE BUG THAT WAS ALREADY FIXED FOR REPS AND NEVER FOR WEIGHT, and `exercise-plan.ts` says so in its own words: *"A lift can hold two slots in a week, and the streak is keyed by name, so without this the second slot incremented the streak a second time and came out one rep above the first — the same exercise, the same weight, '4-6' on Monday and '5-7' on Thursday."* That was solved with a per-week memo; there is a second one for carry distance. **The weight had none**, so the second slot re-derived independently and landed an increment out. Third memo of exactly the same shape.

- [x] KEYED ON THE PRESCRIPTION, NOT THE NAME — two slots of one lift at different sets/reps/RPE legitimately differ (heavier for lower reps). Measured: of 203 same-lift different-weight lift-weeks, **202 were identical work**, so the key costs one legitimate case and catches everything else. The LOWER weight wins, matching the file's other one-way rules (`Math.min(name-keyed, SLOT-keyed)`; the main-lift floor that is "a FLOOR, never a ceiling"). Re-prescribed via `forceStartingWeightKg` rather than patching the number, because `display` and `per_set` derive from it.

- [x] **THE REMAINING 96 ARE DELIBERATE, AND THE GATE DRAWS THAT LINE RATHER THAN PRETENDING IT IS ZERO.** `enforceLoadCoherence` caps against THAT DAY's main lift — a unilateral accessory against the day's bilateral main, curls/laterals/shrugs against the day's main press. Different days, different main lifts, legitimately different ceilings, and a safety ceiling outranks week-level consistency. All 96 are one of those two shapes; `test:week-load-consistency` fails on anything that is not, and ratchets the population so it cannot drift back. Proven to bite: disabling the memo reproduces exactly 202 and turns three checks red.

- [ ] **THE AUDIT MISNAMED ITS OWN FAILING CONFIGURATION**, and it cost two reproduction attempts. It reported `home_gym / none / 45-60 / hybrid`; `baseMesocycleProfile` actually uses `session_duration_preference: '60-90'` — the `'45-60'` was a hardcoded string at the point the case is recorded, describing a profile that was never generated. Fixed in both call sites. A failure label that misnames its own inputs is worse than no label, because it is believed.

- [x] **THE COACH CHAT CAN NOW BE MEASURED, WHICH IT NEVER COULD BEFORE.** Phase 1 of the tone work, and deliberately no prompt change: `scripts/probe-coach-tone.mts` + a fixed `coach-warmth` persona, mirroring the onboarding probe that has existed for a while. **`chat-gemini` had no probe, no persona, and — verified today — no reply guarantee either**, while `onboarding-chat` has all three. So the risk is the opposite way round from how it looks: the function that already burned us is the protected one, and the coach chat is the exposed one.

- [x] THE NUMBER THAT DECIDES IT IS PRINTED FIRST AND LABELLED AS SUCH: **turns that spoke at all**. `fa683fc` shipped a voice improvement that fixed 7/7 grading openers and simultaneously silenced the model on 4 of 7 turns — worse than neutral, because an empty reply trips the dead-air guard that prints the slot's canonical form wording. A change aimed at less form-feel produced more of it. Nothing on this summary outranks that line, and the probe says so out loud.

- [x] FOUR SECONDARY MEASURES so a before/after is readable without re-reading two transcripts: verdict openers, longer-than-a-text, lists/headers, more than one emoji. All deterministic text checks, verified against canned replies covering each shape — silent turn, verdict opener, wall of text, emoji pile.

- [ ] AND ONE OF THOSE MEASURES WAS WRONG ON ITS FIRST RUN, caught by the same canned check: a bulleted reply scored 3 sentences because a list has few full stops, so a plainly-too-long reply passed the length test. Now `sentences > 3 || lines > 3`. Stated as what it is — a proxy for "too long to be a text message", not a parser.

- [ ] PHASE 2 IS NOT BUILT, on purpose. The wording change to both prompts waits until this probe has run against the deployed function and answered a question nothing currently can: **does `chat-gemini` ever return an empty reply today?** If it does, that is a live bug independent of tone and it changes what the tone change is allowed to do. Needs Ashley's machine — `*.supabase.co` is unreachable from the sandbox and the probe costs real Gemini calls.

- [x] **ONBOARDING v2 — THE CONVERSATION NOW HAS SOMEONE AT THE TOP OF IT.** A header with a slowly pulsing mint avatar, "Personal TrAIner", and what it is currently doing ("Building your plan"). That header is also what let the per-message COACH label GO: v1 stamped it above every coach line, which said the same thing twice once identity was stated permanently. The run-suppression helper that existed only to stop the label repeating went with it — dead code removed rather than left looking finished, which is exactly the failure the capture-trace report had just named.

- [x] **EVERY HEX IN THE v2 SPEC WAS CONVERTED, NOT PASTED.** The design names `#453C8E`, `#332A6E`, `#1E1941`, `#3BAE8E`, `rgba(91,233,194,…)`, `rgba(69,60,142,…)` — all correct in Nightshift, all wrong in Ember, Field and Graphite. Each maps to something the app already has: `--border` IS `#453C8E`, `--glow-rgb` IS the accent's rgb triplet, `--primary-2` IS the gradient's darker mint, `--input` IS `#241E4E`. The two the app had no token for (`#332A6E`, `#1E1941`) are `color-mix` steps between tokens. **Zero hex in the component**, and the gate fails on any.

- [x] PROGRESS TICKS REPLACE THE HAIRLINE — four bars, one filling per answered slot. Deliberately NOT a step counter: "12 of 18 answered" tells you you're working through a list, which is the exact feel this redesign removes. The real counts still reach a screen reader through the `progressbar` role, which is how the old invisible bar was fixed once already. The gate bites if a visible count creeps back.

- [x] THE SEND BUTTON NOW HAS A REAL DISABLED STATE, and that is the point rather than decoration: it is the only feedback that Enter will do nothing on an empty box. Dim until there is something to send, then accent-on-accent. Also a circle now, not a squircle — v2 overrides v1 on that, on the active-press scale (.92 not .94), on the input fill (filled with a neutral hairline that turns mint on FOCUS, so the accent means "you are here" rather than being permanently on), and on the typing dots (mint, not grey — grey read as the app buffering rather than the coach composing).

- [x] **THE PLACEHOLDER NOW FOLLOWS THE QUESTION** — "Tell me your goal…", "Gym, home, or a mix?", "How long have you got?". With no buttons on screen this is the only standing hint at what KIND of answer fits. Each hint is owned by its slot (`inputHint` on `SlotDef`, 20 of them) rather than hard-coded in the view, and it resolves from the SAME canonical-next slot the stuck-rescue and dead-air guard use, so all three agree on what the coach is waiting for instead of each guessing separately. The gate rejects a hint written as a field label ("Goal:"), which would put the form's vocabulary straight back on screen.

- [ ] FIVE GATE FAILURES ON THE FIRST v2 RUN WERE ALL THE GATE, NOT THE CODE — moved anchors, a search window 270 characters too short, and an unscoped `border-t` check that flagged the review card's own paragraphs. Worth writing down because the sixth was real: the user bubble legitimately stopped using `bg-secondary` when the fill moved into a class, and the gate now asserts the gradient is still `--secondary`-derived rather than just that a class name is present. A sixth mutation test also silently did nothing — the pattern never matched multi-line JSX — and had to be redone; a mutation that fails to apply looks exactly like a gate with no teeth.

- [ ] **`test:audit` IS 2 / 13,967 ON `main` FROM THIS MERGE, AND THAT IS A KNOWN, ACCEPTED STATE — not a fresh regression.** Both failures are `rotation_relative_load`: `home_gym / none / 45-60 / hybrid` and `minimalist / none / 45-60 / hybrid`, where a rotation lands "Band Lateral Raise" → "Lateral Raises" at 200% and "Step-Down (Eccentric)" → "Walking Lunges" at 133% of a fresh estimate, past the 125% category band. **The equipment-quality feature EXPOSES this bug rather than causing it.** Ashley was given the choice — leave equipment-quality on the branch, ship it anyway, or fix the load bug first — and chose to ship now, with the consequence stated. Fixing it is still worth doing and, being load prescription, gets a plan before a build.

- [x] **THE COACH NO LONGER SPEAKS FROM A BUBBLE** — text-only onboarding design implemented from Ashley's handoff. Two bubbles facing each other read as messaging furniture; the point of this screen is a person talking. So the coach is now plain text on the canvas at 19px/1.6 under a small COACH mark, and only the user gets a fill (17px, compact, asymmetric corner). One voice reading as speech and one as a reply is what makes it feel like being talked to rather than like a transcript.

- [x] EVERY COLOUR IS A TOKEN, which is the handoff's own hardest constraint — *"never hard-code hex, the app has 4 themes × accent overrides"*. The design was drawn in Nightshift, so a hex lifted from it looks perfect there and is wrong in Ember, Field and Graphite; `--primary` is an accent override on top, so even the mint is not a constant. **`test:onboarding-style` fails on any hex in the file at all**, and bites when one from the mock is pasted in.

- [x] THE COACH MARK IS SUPPRESSED ON A RUN — **a deviation from the handoff, which labels every coach message.** In the mock each coach turn is separated by a user reply; in the real flow onboarding sends two or three in a row (the intro, the dead-air guard, the stuck rescue), and stacking COACH three times reads as three speakers rather than one person still talking. A receipt does not break a run either, or recording an answer mid-reply would re-label the coach's own next sentence.

- [x] THE TYPING INDICATOR REPLACES A SINGLE "…" CHARACTER, which read as a rendering glitch more than as someone typing. Three 7px dots, 1.2s loop, 0.15s stagger, a 3px lift — deliberately far smaller than Tailwind's `animate-bounce`, which lifts 25% and reads as a toy. Kept OUT of the glow system on purpose: this is motion, not a halo, so the "Off" glow setting must not be able to switch it off. Reduced motion stops the movement and keeps the dots visible — removing them would leave a silent gap where the app is in fact working.

- [x] SEEN BEFORE SHIPPING, not just typechecked: `render:screens` gained an `onboarding-conversation` screen at 412px. The first render pushed the composer and the typing dots off the bottom of the frame — the two things most worth checking — so the fixture was trimmed until they were visible.

- [ ] **ONE THING IN THE HANDOFF WAS NOT DONE, and it contradicts a ruling Ashley gave earlier the same day.** The handoff says *"No tappable answer options anywhere in onboarding — free text only."* Asked directly, she chose **"only when you're stuck"** over "never — always just text", and that is what shipped: the coach asks and waits, and chips appear only on a bare "I don't know", an unmappable answer, or a re-ask. Left as-is rather than silently reversing her own decision — it needs her word, not mine.

- [ ] A SECOND HANDOFF DETAIL IS FACTUALLY OFF: it says the onboarding prompt should *"NEVER emit `[QUICK_REPLIES: ...]`"*. `[QUICK_REPLIES]` is the COACH CHAT's mechanism; onboarding never used it — it renders chips through the `present_slot` tool. The intent was already handled by the earlier change; no prompt edit was needed for this.

- [x] **"NEVER GIVE ME BURPEES" NOW WORKS WHEN SAID IN ONBOARDING** — it previously did nothing at all. Found by tracing where every onboarding answer ends up, after Ashley pointed out that text-only onboarding means people volunteer things the buttons never asked for. Saying *"I won't eat mushrooms"* landed in `user_facts` and was kept out of every meal; *"never give me burpees"*, said in the same breath, had **nowhere to go**. Exercise exclusions compile from `user_facts` rows tagged `exercise_preference`, and the only two writers were the coach chat and the ⇄ button. **Onboarding never wrote one** — so a person could rule out a food and an exercise together and have only the food honoured, until they happened to say it again later on a different screen.

- [x] IT IS THE MIRROR OF `dislikedFoods`, DELIBERATELY, down to the `createFact` shape — same kind, same `source: 'onboarding'`, same hard dislike — so both paths land in exactly one place rather than creating a second concept for `compileExerciseExclusions` to disagree with.

- [x] **NEVER PROACTIVELY ASKED, unlike its food twin.** Onboarding had just been made conversational so it would stop marching through questions; fixing a capture gap by adding a new question would take that straight back. It sits in the catalogue (like `favoriteCuisines`/`breakfastStyle`) so the model records it the instant someone volunteers it, and the prompt rule explicitly forbids asking for it. The gate asserts both halves — that it's never asked, and that `dislikedFoods` still IS.

- [x] **THE HALF THAT IS EASY TO MISS: the first plan.** The `user_facts` rows are written AFTER the plan is generated, and `exerciseExclusions` is `[]` for a brand-new signup — so wiring only the database would have handed someone a first mesocycle containing the exact exercise they had just ruled out, with the exclusion silently taking effect weeks later. The in-memory answer is resolved and passed into BOTH `generateExercisePlan` and `generateMesocycle`. **This is the same half-landed shape the trace report had just flagged elsewhere, caught in the act of building it.**

- [x] AN UNRECOGNISED PHRASE EXCLUDES NOTHING, rather than excluding something approximate. "Those jumpy squat things" resolves to no catalogue name, so it contributes no exclusions — but it still gets a fact row, so the answer is never lost and shows on the Profile screen. Guessing an exercise from an unmatched phrase would remove work someone never asked to have removed.

- [x] THE GATE IS BEHAVIOURAL, NOT TEXTUAL: `test:onboarding-exercise-dislikes` generates a REAL plan with the dislike applied and asserts the exercise is gone — against a control run proving it was there to begin with, so the section cannot pass vacuously on an exercise the engine never picks. Proven to bite on four mutations, including both "plan not told" cases separately.

- [ ] **AND THE GATE'S OWN FIRST RUN WAS A FALSE POSITIVE I HAD TO FIX.** It reported that an unrecognised phrase changed the plan. It hadn't — plan generation draws on a random source, so two identical calls differ on their own, and I had compared one unseeded run against another. Seeded with a fixed key and it passes three runs running. Recorded because `test:injury-rebuild` misfired exactly this way once already: a coin-flip gate is worse than a red one, because it makes unrelated work look guilty.

- [ ] STILL OPEN from the same trace, and NOT built: soft preferences ("I prefer chicken to fish") are compiled into a list that **nothing calls** — the comment claims it feeds swap-candidate ranking and it does not; wire it or delete it, but "neither" is how the next person gets misled. The five untagged allergens (celery, sesame, mustard, lupin, sulphites) and injuries outside the eight body areas both remain remembered-only; by the repo's own rule those are safety-path work and need a plan before a build.

- [x] **THE ONBOARDING WAS A QUESTIONNAIRE BECAUSE THE CODE MADE IT ONE** — fixed. Ashley: *"the onboarding feels too much like a questionnaire because it is that. a real coach wouldn't be sending you buttons to click. they'd be waiting for a text reply."* She was right, and it was literal. **A second Gemini call, with function calling FORCED (`mode: "ANY"`), existed for the sole purpose of stapling chips onto any question the model had chosen not to put chips under.** Its own comment read *"chips must not depend on the model remembering to ask for them"* — exactly right for a form, exactly wrong for a conversation. Deleted. That also removes an entire extra model round trip from nearly every turn, so replies get cheaper and faster.

- [x] THREE FORCES, NOT ONE. The forced leg was the third; the other two were wording. `present_slot`'s own description said *"call this whenever you ask a closed-set question"*, and the SLOT MECHANICS rule said *"Closed-set question → ask it in your own words AND call present_slot"*. Between them, essentially every question in the flow arrived with a menu attached. Chips are now described as a **rescue** with exactly three cases: they said they don't know or asked what the options are, the answer was too ambiguous to map, or the same question has already been asked once and is still unanswered.

- [x] ASHLEY'S RULING WAS "ONLY WHEN YOU'RE STUCK", over two alternatives (never; or keep them for the list-shaped ones). Worth recording what that trades: the honest cost is that "which days can you train" now means typing rather than tapping seven weekdays, and onboarding may take a few more turns. What it does NOT trade is safety — see below.

- [x] **A PROMPT-ONLY VERSION OF THIS WOULD HAVE BEEN THE fa683fc MISTAKE AGAIN.** That commit loosened this same prompt, worked on its own terms, and made the model stop replying on 4 of 7 turns — an effect nobody predicted, and it was reverted. So the stuck case has a deterministic half that does not involve the model at all: `isStuckMessage` catches a bare *"I don't know" / "not sure" / "what are my options"* and the client renders that slot's card itself. Someone who types "I don't know" and gets another paragraph of prose back is worse off than before chips were removed — that is the one moment a list genuinely helps, and withholding it would be stubbornness rather than conversation.

- [x] THE DETECTOR IS DELIBERATELY NARROW, and that is the check with teeth. It must match the WHOLE message: *"I don't know"* is someone asking for help, *"I don't know, maybe three days a week?"* is an answer with a hedge in front of it, and burying that under a chip grid would throw away what they just said. `test:onboarding-conversational` asserts both directions on five hedged answers, and bites when the anchors are loosened.

- [x] **NOTHING ON THE SAFETY PATH DEPENDED ON CHIPS, which is what made this safe to do at all.** `detectAllergenTags` scans every typed message deterministically, independent of the model, and lands the matching "-free" tag on `dietaryPreferences` — the only thing meal generation actually filters against. `tryExactLabelMatch` records a typed answer that matches an option exactly, with the same certainty a tap gives. Both untouched, both asserted by the new gate. Every anti-stall path is untouched too: a value that fails validation still re-asks with chips, the dead-air guard still renders the next question with its card, `pickSlotToForce` still steps in on a stall. Those were rescues already — they were never the questionnaire.

- [ ] MEASURE IT BEFORE TRUSTING IT — needs Ashley's machine, since `*.supabase.co` is unreachable from the sandbox and the probe calls the DEPLOYED function against the real Gemini API. New fixed persona `scripts/probe-personas/conversational.json`: nine turns, every answer phrased the way someone would actually type it rather than as a chip label, with a deliberate bare "I don't know" at turn 6 which is the one turn that SHOULD produce chips. The probe now prints **turns that offered chips**, alongside the numbers it already printed — required slots still missing, and empty-reply turns, which is the metric fa683fc failed on.

- [x] **THE COACH NOW SAYS WHAT IT IS FOR, AND SHOWS IT RATHER THAN LISTING IT** — Ashley: *"I want it to clearly explain in the first few messages what it does, how it can help and how the user can use it."* Half A of `docs`-planned work, client copy only, no model involved. The first-ever chat now opens with four short messages — who this is, what to say to it, what it will not do without asking, then today — and three starter chips under the last one. **Deliberately NOT a feature list**: a brand-new user does not need an inventory of 22 tools, they need to know what KIND of thing to type, so the how-to-use-it half is carried by chips rather than prose.

- [x] A CHIP CANNOT OVERSTATE THE WAY A DESCRIPTION CAN, which is the whole reason they are chips. Each one is a sentence the user actually sends — `handleQuickReply` puts the text in the composer and hits send — so tapping it demonstrates the real thing instead of describing it. One per KIND rather than three of the same: *"Talk me through today"* (explain, answered from plan context), *"Swap an exercise"* (change, `propose_exercise_swap`, a card she confirms), *"There's a food I won't eat"* (remember, `record_fact`).

- [x] **NOTHING ABOUT THE SCHEDULE OR THE VOLUME IS OFFERED, AND A GATE NOW HOLDS THAT LINE.** `adjust_volume` and `update_workout_schedule` are stubs that reply *"I can't safely make plan changes yet"*. A chip is a suggestion in the app's OWN voice — it has no honesty rule the way the model does — so one landing on a stub would teach a new user, in their first-ever interaction, that the coach says no to obvious asks. `test:coach-promises` §6 makes every chip NAME the tool it routes to and proves that tool is declared, executed and not a stub; it also asserts both stubs still decline, so the day either becomes real, the gate turns red and points at the chip list. Proven to bite on five separate mutations.

- [x] **THE MIDDLE MESSAGE WAS A NINE-LINE WALL, AND ONLY `render:screens` COULD SEE IT.** Every check passed — types, gates, tool routing — while the message meant to replace "one block of text" was itself a block of text taller than the other two combined at 412px. Split at the natural seam (what you say / what I'll do with it) into 6 and 3 lines. The words moved to `src/lib/first-run-intro.ts` so the harness renders the REAL strings rather than a replica that drifts: the chrome around them is copied class-for-class and can drift, the copy cannot, because there is only one copy.

- [ ] **A CORRECTION: the intro is not localStorage-based and does not replay on a cleared browser.** I flagged that in the plan. `isFirstEverChat` is set from the Supabase `chat_messages` COUNT (`ChatAssistant.tsx:463`), not from the cache — so it follows the user across devices correctly, and replays only until they send their first message, which is arguably right (someone who never replied has not been introduced). I reached the wrong answer by reading the WRITE path — the intro is `setMessages` only, with no DB insert, which is true — and inferring the read path from it instead of looking at where the flag is set.

- [ ] OBSERVED, NOT CHANGED: the three chips stack one per row on a phone. Quick replies render inside the message's `max-w-[80%]` and behind the `pl-9` avatar offset, leaving ~294px, which no two of these fit in. That constraint is shared chat chrome — it applies to every model-supplied quick reply too, not just these — so widening it is a change I did not make unasked and cannot verify from here. Legible and tappable as it stands; worth a decision if the column starts reading as a menu.

- [x] **THE APP SAID "ACTION FAILED" ABOUT A CHANGE THAT HAD ALREADY LANDED** — fixed. Ashley, minutes after the migration went in: *"I missed the gym but I did muay thai instead"* → *"Done — that day is marked as Muay Thai instead of lifting."* followed immediately by *"Action failed — the change was not applied."* **The database write succeeded.** chat-gemini only emits an action when its own write returned `dbSuccess`, and it emitted one. `swap_session_for_activity` was missing from the client's `PlanAction` union, so `applyPlanAction` fell through to `return false` and the client appended the failure line.

- [x] THE EXACT INVERSE OF THE DEFECT THE TOOL WAS BUILT FOR. `swap_session_for_activity` exists because the coach used to *claim* a change it never made. It now made the change and claimed it had not — strictly worse than silence, because a user who believes it failed will do it again or give up.

- [x] **AND THE LESSON WAS ALREADY WRITTEN DOWN, IN THE SAME FUNCTION, TWELVE LINES ABOVE.** `log_workout_set` had this identical bug and carries a comment saying so: *"this type was missing from the PlanAction union, so applyPlanAction fell through to `return false` below and the user was told 'Action failed' about a write that had already landed."* Same file, same fall-through, one action type later. **A comment did not prevent the recurrence, so it is now a gate.** New `npm run test:chat-actions` parses every literal `action: { type: … }` the edge function returns and fails if any lacks a `PlanAction` interface or an `applyPlanAction` branch. Proven to bite: renaming the handler turns it red on two checks.

- [ ] SCOPE STATED RATHER THAN OVERCLAIMED: the gate covers the LITERAL action types — the `dbSuccess ? { type: "…" } : undefined` returns, which are precisely the "already written, now told it failed" class. It does **not** cover index.ts's final fall-through, `action: { type: name, ...args }`, which emits the tool's own name computed at runtime; those cannot be enumerated statically and most route through the proposal/confirm flow instead. A wider audit of that path is a separate piece of work.

- [x] **THE LOAD-CEILING FEATURE NAMED A TABLE THAT HAS NEVER EXISTED** — fixed. Migration `20260826140000` said `alter table profiles` and `load-ceiling-prompt.ts` said `supabase.from('profiles')`. **The table is `fitness_profiles`, and all 46 other migrations say so.** Both halves were mine, written in the same round. Found only because Ashley ran the migration against a real database and it threw `relation "profiles" does not exist` — on TEST, loudly, before production, which is exactly what the TEST project is for.

- [x] IT WOULD HAVE FAILED SILENTLY, FOREVER. The write path's `isMissingColumnError` guard matches `/schema cache/`, and PostgREST's reply for an absent TABLE is *"Could not find the table 'public.profiles' in the schema cache"* — so the guard I wrote to make a pending migration harmless would have swallowed a permanent bug and reported "needs migration" to a screen that shows nothing. A guard meant to tolerate a temporary absence was hiding a typo.

- [x] NOTHING COULD HAVE CAUGHT IT, so a gate now does. `tsc` cannot — a table name is a string. The other 55 gates cannot — they exercise the pure engine and never touch a database. New `npm run test:table-names` parses every migration for the tables it CREATES, then checks every `alter table` and every `supabase.from('…')` in `src/` and the edge functions against that set. 28 tables created, 28 altered, 24 queried, all resolve. Proven to bite: reintroducing the typo turns it red on two checks naming the file.

- [ ] **A CORRECTION: the Supabase dashboard's "LAST MIGRATION" is not the newest applied one.** Both projects displayed `allow_absent_body_metrics` (17 Aug), from which I told Ashley to expect FOUR pending migrations. The CLI then listed exactly ONE. The other three were already applied; the dashboard field means something else. **The CLI's pending list is the only reliable answer** — do not infer migration state from that panel again.

- [x] **`tier1_compound` MEANT "NEEDS A BARBELL", NOT "IS A MAIN LIFT"** — and that single conflation was the whole 37.5%. All 8 tier-1 entries are barbell lifts or the two pull-up-bar movements, so **Pistol Squat Progression, Nordic Hamstring Curl and Chest Dips — three of the hardest things a person can do without a gym — sat at tier-2 alongside Air Squat, Incline Push-Ups and Glute Bridge**, and Dumbbell Bench Press ranked level with a Push-Up. The catalogue was not missing the hard movements; it was mis-ranking them. That also exposed a limit in the promotion shipped an hour earlier: it broke ties on LIST POSITION, so it could not tell a Pistol Squat from an Air Squat.

- [x] RANKED WITHOUT RETAGGING — Ashley's call, taking the safe half of two options. New `movement-difficulty.ts` carries an anchor-only difficulty bump (2 = "this IS the main lift for whoever can only reach it", 1 = "a harder-than-baseline variant"), read by exactly ONE thing: which exercise a day nominates as its anchor. **`mechanics_tier` is untouched**, because it drives sets, reps, rest brackets, load ceilings and several quality checks — retagging would have moved prescriptions for gym users who never had this problem. Tier still multiplied by 10 so it always dominates: a tier-2 can never outrank a tier-1 however hard it is.

- [x] **MY OWN COMMENT PREDICTED THE BUG AND WAS WRONG.** The per-exercise floor reads an anchor computed from the day's BASE names, and I had written that this was safe because *"rotation is within-tier, so the RANK the anchor is chosen on cannot change — only the name."* True while the anchor ranked on tier alone; false the moment difficulty entered the ordering, since a rotation swapping Air Squat for Goblet Squats is within-tier AND changes which movement is hardest. **97 days landed the floor on the wrong exercise.** Fixed with one post-pass over the day as it finally stands — `trimWeekRestForBudget` re-derives correctly but only visits days that are OVER budget, so it could not cover this.

- [x] SPANISH SQUAT IS DELIBERATELY UNRANKED, and the gate says so by name. It is a patellar-tendon rehab tool the injury pass places on purpose; promoting it to a day's MAIN LIFT would turn a rehab prescription into the centrepiece of the session. **0 promoted across 1,088 anchored days.** The difficulty list is hand-maintained and unavoidably editorial — there is no property of an entry to derive "a Nordic curl is brutal" from — so the mitigation is a check that every one of the 39 ranked names still exists in the catalogue, making a rename fail loudly instead of silently dropping a movement back to baseline.

- [x] Verified: `test:audit` **0 / 13,967**. Sessions past the stated maximum **0 / 0 / 0**, under-minimum 5 / 18 / 6 — both unchanged from the promotion round. 1,088 anchored, **0 under 60s**, 0 in a superset, 55 already above 60s left alone. `test:main-lift-rest` sections 5-7, plus 24 further gates.

- [x] 37.5% OF TRAINING DAYS HAD NO MAIN LIFT — promotion built. Found because `render:screens` put a bodyweight day on screen with no MAIN LIFT label. Measured: **96 of 256 days contain no tier-1 exercise at all** — full_gym 0, home_gym 0, minimalist 48 of 64, bodyweight 48 of 64. The cause is arithmetic: **only 8 of 145 catalogue entries are `tier1_compound` and 6 of the 8 need a barbell**, so without one the only tier-1s reachable are Pull-Ups and Chin-Ups. A home trainee's pull day had a main lift; every squat, press and hinge day had none. Plan: `docs/PLAN-the-day-with-no-main-lift.md`. Ashley's ruling: **both, promotion first** — promote now, add real non-barbell tier-1 movements as a separate content round.

- [x] IT WAS BEHAVIOUR, NOT DECORATION. `MAIN_LIFT_REST_FLOOR_SECONDS` keys on `tier1_compound`, so on those days **nothing received the 60-second floor**. The round that shipped that floor was called "the day's hardest lift keeps its rest" and Ashley's ruling was *a full minute, every hard lift, bodyweight included* — it protected a bodyweight trainee's pull-up day and left their leg day on accessory rest. Now the day's hardest STANDALONE movement is promoted: 1,088 of 3,072 days had no tier-1, **all 1,088 got an anchor, 0 still rest under 60s**.

- [x] **THE FIFTH PATH — the same lesson, one round later.** Wiring the four call sites was not the job. Measuring the outcome found **227 promoted anchors still under 60s**: `trimWeekRestForBudget` trims last and floors anything not `isMain` at 30, so anchors were floored upstream and walked straight back down. The previous round's own comment reads *"a constraint asserted at three paths, missed at the fourth"*. Caught only because the verification asked what the plans actually contained rather than whether the edits compiled.

- [x] TWO INVARIANTS THAT STOP THE APP CONTRADICTING ITSELF. **No superset member is ever promoted** (0 of 1,088) — a superset prints *"alternate — no rest between"* directly under its members, so promoting one and giving it a 60s floor would print both claims in one card; 128 days whose hardest movement is paired keep no main lift instead. And **promotion is a FLOOR, never a ceiling**: 49 anchors already resting above 60s are left alone rather than pulled down, the same one-way rule the stated load ceilings follow.

- [x] **A REGRESSION I CLAIMED AND HAD TO RETRACT.** I reported that this broke `test:injury-rebuild`'s "the two modes genuinely differ" check. It did not. **That gate does not seed its RNG** — three runs of identical code gave 452, 452 and 436 rebuilt slots against a fixed 448, passing twice and failing once with nothing changed. I compared one run against one run and concluded a regression. Seeded, the answer is **436 / 448 both with and without the change** — no effect, failure genuinely pre-existing (and the profile is full_gym, where every day already has a tier-1, so promotion cannot fire there at all). Seeding added: a coin-flip gate is worse than a red one because it makes unrelated work look guilty. What it ASSERTS is untouched.

- [ ] FLAGGED, not fixed — **a carry or an isometric hold can now be labelled MAIN LIFT** (Loaded Backpack Walk 112 days, Farmer Squat Hold 80). On a day whose focus is literally "Squat & Carry" the carry genuinely is the hardest thing, so it is defensible, but no coach would call it a main lift. Also: the promoted anchor sits wherever the generator placed it, so MAIN LIFT can appear third in the list, below a superset. Both are consequences of "the day's hardest movement" being a simple rule, and inventing per-prescription-type exceptions is how a simple rule becomes unreviewable. Ashley's call.

- [x] ONE ICON MEANT BOTH "I EAT FISH" AND "I CANNOT EAT FISH" — fixed. The new screen-render harness put the real onboarding questions on screen for the first time, and 🐟 was serving **both Pescatarian and Fish-Free** in the same 17-option list, on the allergen path. 💪 was serving **both Shoulders and Elbows** in the list that drives injury filtering. `value` was correct in both cases — only the picture was wrong, which is exactly why no existing gate could see it: every other check in this repo reasons about tags and values. Ashley's call, offered against three alternatives: change the odd row out. Pescatarian → 🍣 (every other exclusion already wears the food it excludes, so Pescatarian was the row breaking the convention); Elbows → 🤜 (💪 is a flexed bicep, which reads as upper arm); Lower Back → 🧍, replacing 🔙, the "BACK" arrow — a pun on the English word that means "go back" everywhere else the user has seen it.

- [x] NO VALUE MOVED, and the gate asserts it. Icons are presentation; `value` is the enforcement contract (diet-rules' `FORBIDDEN_TAGS` keys, and the injury codes every filter matches on). New `npm run test:option-icons` checks no two options in one question share an icon, **exempting uniform lists by the property "all of them are identical"** rather than by naming slots — `trainingDays` is seven 📅 and `mealsPerDay` three 🍽️, where the label carries all the meaning and no one can be misled about which is which. A named allowlist would have to be edited to stay honest and would eventually be edited to stay quiet. Proven to bite: restoring 🐟 on Pescatarian turns it red on two checks, then green again. `test:diet-tag-sync`, `test:meal-roundtrip`, `test:injury-separation`, `test:injury-adaptation-safety`, `test:joint-tags` and `test:rehab-prescribed` all pass.

- [x] AND A SUSPICION THAT DID NOT SURVIVE LOOKING. From the contact sheet I thought the 17-option dietary list and the 10-option cuisine list would push their Done button below the fold on a phone. Rendered at full height: both fit, allergen disclaimer and all, with room to spare. Recorded because the harness is as useful for killing a plausible worry cheaply as for finding a real defect — the alternative was a speculative layout change to something that was never broken.

- [x] I CAN SEE THE APP NOW — `npm run render:screens`. Three UI defects this week were found the same way: Ashley opened the deployed app on her phone, hit something broken, sent a screenshot, and we spent a deploy round-trip on it. Two of them were sitting in the markup the whole time — the composer below the fold behind the keyboard, and a row reading **"B · 3&times;9-11 &middot; 25kg"** where "B" was Backpack Row. **Every gate passed while the app showed a one-letter exercise name**, because every gate reasons about DATA (is the load right, does the rest floor hold, is the audit clean) and a layout defect is invisible to an assertion about kilograms. This renders the REAL components with REAL generated plans at a REAL 412px and takes a picture. 23 screens.

- [x] IT FOUND A REAL ONE ON ITS FIRST RUN. **A peeked day never told you a superset was a superset.** Today's shows a glowing mint rail, `A1`/`A2` numbering, and the line *"alternate — no rest between"*; another day showed two plain rows both badged a bare "A", no rail, and **no alternate line at all** — which is an instruction for how to train the pair, not decoration. PeekPanel's own header comment promised the two surfaces "cannot drift apart again" because `ExerciseLine` is shared; that was true of the ROW and false of everything around it. Fixed by extracting `SupersetShell` — the chrome, taking a render function, since the two callers genuinely cannot share a row type (today's knows about logged sets; the peek's must not).

- [x] AND ITS FIRST FINDING WAS ABOUT ITSELF, which is worth more than the bug. The first run showed "Barbell Squats" labelled ACCESSORY with no MAIN LIFT anywhere — a convincing screenshot of a defect that does not exist. `generateExercisePlan` returns exercises with `tier` **undefined**; only `generateMesocycle` assigns them, and the app only ever renders mesocycle days. I was photographing a state no user can reach. **A screenshot of the wrong data is a confident picture of nothing** — recorded in the harness so the next reader suspects the props before the component.

- [ ] WHAT IT DELIBERATELY CANNOT DO, stated so the coverage is not overread: it uses `renderToStaticMarkup`, so **effects never run** and every component renders in its initial state — anything that only appears after an effect is out of scope, as is anything needing live session context (`RestDayCard` is skipped BY NAME rather than silently). No touch, no scrolling, no keyboard. It would not have caught either of this week's onboarding bugs. It is the cheap pass before Ashley's phone, not a replacement for it. Also **not** pixel-diffed against goldens on purpose: a golden gate on a UI still being designed fails on every intentional change and gets muted within a week.

- [x] AN EXERCISE ROW COULD CRUSH ITS OWN NAME TO ONE LETTER — fixed. Ashley, from a phone: *"you can't see the exercises until you click into it to display it."* Literally true — a row read **"B · 3&times;9-11 &middot; 25kg &middot; 2s down &middot; drive up"**, and "B" was Backpack Row; expanding the row was the only way to learn its name. CAUSE: the summary was `shrink-0` while the name was `min-w-0 truncate`, so 100% of any shortfall came out of the name. The summary is unbounded — sets, reps, load, assistance, added load AND a tempo phrase — so a tempo'd accessory ate the whole line. It looked fine on today's screen only because those rows happened to carry shorter summaries; **same component, same bug, and today truncated too** ("Single-Leg Dumbbell C…"). Now `flex-wrap` with a name box that is NOT `min-w-0`, so the name's nowrap text sets the row's min-content and the SUMMARY drops to a second line instead.

- [x] EVERY DAY GETS TODAY'S HEADLINE. Ashley: *"it shows the focus of the day ie squat and carry in nice big bold writing. I want all days to look like that but still make it obvious which is the current day's plan."* A peeked day was one `text-sm` line ("Friday · Full Body Power") against today's 36px bold hero. **What still separates today, now that the type matches:** the eyebrow is mint-with-glow reading "Today · Wednesday" versus a muted grey "Friday"; today has Start session where a peeked day has an X; today has the session-progress line and a day that is not today has no progress to show. The headline is 30px rather than 36px on purpose — identical size in a different colour reads as a bug, a clear step down reads as hierarchy.

- [x] VERIFIED BY RENDERING IT, not by reasoning about flexbox. No phone here and no Supabase, but Chromium is installed — built a static harness with the app's own compiled CSS, the real class strings and the real 412px width, and screenshotted before/after. It reproduced Ashley's exact "Bac…", "Archer Pus…", "Step-Up (E…" from the code alone, then showed them whole. Also settled a judgement call by rendering both: a wrapped summary right-aligned floats awkwardly mid-row, left-aligned it reads as a caption under the name. **Chose from the picture rather than from a guess.**

- [x] ONBOARDING WOULDN'T STAY AT THE BOTTOM OF THE CHAT — fixed. Ashley, testing the deployed keyboard fix on a real phone: *"the box stays above the keyboard but I have to keep scrolling to the bottom of the chat."* Auto-scroll was `useEffect(..., [messages, busy, reviewOpen])` — it scrolled when the message list changed, but in this screen the height keeps growing AFTER that: the question renders, THEN its `SlotChipsCard` renders the option grid beneath it, so **the buttons that answer the question land off-screen**. Multi-selects grow as options are toggled with no new message at all, the review card opens, and the keyboard both shrinks the container (`100dvh`) and grows its padding — none of which is a message.

- [x] AGAIN, THE FIX ALREADY EXISTED ONE COMPONENT OVER, and its comment says so in as many words: `ChatAssistant`'s reads *"ResizeObserver-based scroll instead of setTimeout/useEffect[messages]"*. Onboarding was still running the approach that had been tried and abandoned. Ported the same pattern — observe the CONTENT BOX rather than the data, `requestAnimationFrame` so the scroll runs after layout, `instant` rather than `smooth` so repeated fires don't fight each other, plus a near-bottom guard (120px) so someone re-reading an earlier answer is never yanked away. `sendMessage` forces past that guard: they just answered, so the reply is what they are waiting for.

- [ ] **THE PATTERN WORTH NOTICING: three bugs in a row were "the fix exists elsewhere in this app, onboarding didn't get it."** The keyboard inset, the auto-scroll, and (earlier) the composer's soft-disable. Onboarding is the first screen any user sees and the one with the least in-app traffic from us, so it drifts behind the screens we actually use. Worth a deliberate sweep comparing `ConversationalOnboarding` against `ChatAssistant` for anything else the latter learned and the former never heard about — rather than waiting for a fourth screenshot.

- [x] THE ONBOARDING TEXT BOX HID UNDER THE KEYBOARD — fixed. Reported by Ashley from a real phone while testing the live app: *"I have to scroll up to see the text input field."* Not a browser quirk. `ConversationalOnboarding` was wrapped in `min-h-screen` (100vh) with the composer as an ordinary last flex child, so when the soft keyboard opened the layout stayed full height while the visible area halved and the composer went below the fold. On Android 100vh also counts the collapsing URL bar, which is why it could need a scroll before anything was even focused. **This was the ONE screen in the app without the fix** — `useViewportInset` already exists and `ChatAssistant`, `BottomDock` and `BottomTabBar` all use it; onboarding, the first screen a new user ever sees, was missed.

- [x] FIXED WITH THE HOOK, NOT WITH `100dvh`, and the reason is written down in the hook already: iOS Safari resizes the VISUAL viewport but not the LAYOUT viewport, so a bottom-anchored element still ends up under the keyboard there. dvh alone would have looked right on Android and stayed broken on iPhone. The container moved to `min-h-[100dvh]` anyway (it fixes the URL-bar half), the composer is now `fixed` with a `useViewportInset` offset, and the scroll area yields `insetPx + 112` while the keyboard is up so the last chip card or the Generate button is never covered on iOS.

- [x] THE ONBOARDING FLOW — built. Ashley asked for an honest opinion of the whole thing: order, content, adaptivity, whether the quick-reply buttons are ugly. The verdict was that it is better than most fitness onboarding and had **one structural problem: the order**. Body metrics (age, height, weight, sex) sat at positions 22-25, behind NINE consecutive nutrition questions — the four values that drive every prescribed weight in the app, collected at the point of lowest attention. They now sit at 12-15, and the rule enforced is *everything the training half needs comes before anything the food half needs*. Plan: `docs/PLAN-the-onboarding-flow.md`. New gate `npm run test:onboarding-order`, new report `npm run report:slot-impact`.

- [x] THE ORDER GATE CAUGHT MY OWN PLAN BEFORE IT SHIPPED. `ONBOARDING_SLOTS` is an ordered array, and a slot whose `requiredIf` reads a LATER answer silently never appears — `isSlotApplicable` returns false rather than erroring. That defect has already happened once in this file (activityLevel sat last while gating the barbell chain, so an experienced lifter's volunteered numbers were discarded). The reorder plan's own written-out sequence **dropped activityLevel from before the barbell chain, recreating it exactly**. The gate derives each slot's dependencies by handing its predicate a Proxy and recording which keys it reads — a hand-written list would have been written from the same wrong mental model that produced the mistake.

- [x] **A CORRECTION: the plan's §4 was wrong and the file had already warned about it.** It proposed moving `favoriteCuisines` and `breakfastStyle` "out of onboarding" to first use. They were **already** in `NEVER_BLOCKING_SLOTS` and already filtered out of `trackedSlots`, so they were never proactively asked in the first place — §4 was solving a non-problem. Deleting them removed the only path by which a volunteered cuisine could be RECORDED, which the code's own comment warns about in as many words. It surfaced as a TypeError three files away; both are restored, and `test:onboarding-order` now fails BY NAME if a never-blocking slot is deleted rather than demoted.

- [x] NOT ONE QUESTION IS A CANDIDATE FOR CUTTING — measured, not asserted. New `report:slot-impact` varies each slot across all its values on a FIXED RNG seed (a per-value seed would measure the ±0.3 tie-break jitter, not the question) and counts distinct 16-week mesocycles, per equipment tier. Result: `fitnessGoal` 4/4, `trainingExperience` 4/4, `sessionDuration` 4/4, `trainingStyle` 4/4, `conditioningPreference` 3/3, `trainingDays` 5/5, `gender` 2/2, `weightKg` 3/3. The three that read inert for training — `activityLevel`, `heightCm`, `age` (bodyweight only) — all feed BMR/TDEE, so cutting one blanks somebody's calorie target. **Candidates after the nutrition filter: NONE.** That is the answer to "does it collect too much".

- [x] MEASURING KILLED BOTH OF MY "OBVIOUS" ADAPTIVE SKIPS. I proposed not asking a bodyweight trainee about training style (4 styles → **4 different plans**) and not asking a conditioning trainee about cardio (3 preferences → **3 different plans**). Both wrong, both refuted in two minutes. Which is why the deliverable is the report rather than a list of questions I felt confident about.

- [x] A STALE COMMENT WAS ARGUING TO GIVE TIRED PEOPLE MORE VOLUME. `recoveryCapacity` carried *"measured to have zero effect anywhere in the generated plan"* — true when written, false once `RECOVERY_SET_MULTIPLIER` landed. Re-measured on full_gym/intermediate/hypertrophy/4-day: base week **81 sets for all three** (that half still holds), 16-week mesocycle **low 912 / moderate 1125 / high 1125**. So "low" removes 213 sets, 19% of the block. NOTE THE DENOMINATOR CHANGE: an earlier note in this session quoted "58 vs 77 sets" from a different profile — those numbers are not comparable to these and should not be read as a regression.

- [ ] FLAGGED, NOT FIXED — **moderate and high recovery produce a byte-identical mesocycle** (1125 sets each) despite distinct multipliers (0.9 vs 1.0); the difference is absorbed by set-count rounding. So the question has three answers and two outcomes. Whether someone who reports high recovery should earn more volume than someone reporting moderate is a training call for Ashley, not a bug.

- [x] THE BUTTONS ARE NOT UGLY, and the "obvious" fix would have made them worse. `OptionCard`'s `compact` variant did two jobs at once — tighter padding AND no description — because every compact caller happened to have no descriptions. Adopting it on the four-option questions, as the plan proposed, would have silently deleted *"New to this, or coming back after a long break"* from the goal, experience and activity questions: the lines that let someone place themselves honestly instead of flatteringly, and which the same plan named as one of the three best things about the flow. Split into `compact` (size) and `showDescription` (content); four-option questions now tighten and keep their descriptions.

- [ ] FLAGGED, deliberately not decided — **the emoji**. 🌱📈🎯🏅 render differently on every platform and age badly. They do real work (fast visual scanning), so this is not a "rip them out" recommendation. It is the single thing most likely to make the app read as cheap beside a polished competitor, and it wants a designer and a deliberate call rather than a diff.

- [ ] FLAGGED — **every claim about WHERE people abandon onboarding is reasoning, not evidence.** The app has no funnel analytics, so "nine food questions is where drop-off happens" is a plausible hypothesis and nothing more. The reorder is defensible on its own terms (the most load-bearing questions should not be asked at the point of lowest attention), but it has not been validated against a single real user.

- [x] ASK WHAT YOU CAN ACTUALLY LOAD — built. The previous round stopped the app ASSERTING what it did not know; this one makes it ask. Ashley's ruling: **at first use, not onboarding** — someone who has never trained cannot answer "how much can you load", and onboarding is where people drop out. At most three questions ever exist (dumbbell, kettlebell, backpack) because `loadingMode` already classifies every exercise and a home trainee owns only those three; a barbell user is already asked their squat/bench/deadlift and a cable stack means a gym. Each appears ONLY when an exercise using that implement is in front of them, and at most one per session. Plan: `docs/PLAN-ask-what-you-can-actually-load.md`. New migration `20260826140000`, new gate `npm run test:load-ceilings` (28 checks).

- [x] DOWNWARD ONLY, and that asymmetry is the whole safety argument. A stated 15kg replaces the invented 50kg — a figure that described a *commercial gym rack*, which `LOADING_CEILING_KG_PER_HAND_OR_TOTAL`'s own comment already admitted and had deferred. A stated 200kg changes **nothing**, because those tables are also formula-regression backstops and this is the loading path. Same one-way rule the weigh-in offer used: new information may correct a load, never inflate one. PROVEN across 532 ceiling checks — **0 raised**. Verified end to end: 10kg dumbbells prescribe 10kg, an 8kg bag prescribes 8kg, and a claimed 99kg rucksack still prescribes 20kg.

- [x] THE PROPERTY THAT MATTERS MOST WHILE THE MIGRATION IS UNAPPLIED: **an unanswered profile prescribes identically to today, 0 differences across every loaded exercise x every equipment tier.** Ashley cannot run migrations from her machine right now, so this ships before the columns exist. Reads use optional chaining, writes fail soft through the same `isMissingColumnError` guard `added_load_kg` uses, and the prompt simply never appears. Nothing breaks; the app keeps guessing exactly as it always has.

- [x] DECLINING IS A VALUE, NOT AN ABSENCE — the body-metrics lesson, applied before it could bite again. `load_ceilings_declined` is its own column because null would otherwise mean both "not asked" and "refused", and the prompt would return forever. It silences **every** implement, not just the one on screen: someone who does not know what their dumbbells weigh does not know what their kettlebell weighs either, and asking again next session with a different noun is the same nag wearing a hat. Gated both ways, including that a declined trainee still gets a full, unchanged load — a refusal that degrades the product is not a real choice.

- [ ] FLAGGED, deliberately not built — **"which weights do you own", rather than "the heaviest"**. Far more useful: it would let the app round to a 10 or 15 the trainee actually has instead of a 12.5 they do not. Also a much harder question to answer and to build. The ceiling is the 80% version and choosing it was a decision, not an oversight. Likewise **re-asking when kit changes**: someone who buys heavier dumbbells has no way to tell the app.

- [x] THE APP TOLD PEOPLE TO ADD WEIGHT IT HAD NO REASON TO THINK THEY OWNED — fixed. Ashley's question: *"how do we know the user's backpack is 20kg or that they can add weight to it each week?"* **We do not, and we never ask.** Onboarding has one equipment question (four boxes) and the only weights a user can ever state are squat/bench/deadlift. The 20kg is `IMPROVISED_IMPLEMENT_CEILING_KG` — a hardcoded 8/12/20/25kg safety ceiling about straps and posture, not a fact about anyone's bag — and for most bodies the CLAMP is the number. Yet the guidance shown was identical to a barbell's: *"Starting estimate from strength standards for your bodyweight... too easy, add load next set."* Both halves untrue for a rucksack: the figure came from a ceiling rather than from them, and the instruction assumes weight that may not exist.

- [x] **A CORRECTION I OWED MID-ANSWER: I said "nothing checks". Wrong.** `getDoubleProgressionRecommendation` already reads the last logged sets and works from THAT number — `maxWorkingWeight(sessionSets)`, not the estimate — and `TodayPanel` calls it for every exercise carrying a load. So someone who logs 10kg is recommended 10kg next time, with a note saying why. The gap is far narrower than I first described: **session one only**, before anything is logged. Which is why Ashley's ruling was right and cheap — say the first number is a guess, and let the machinery that already exists do the rest.

- [x] TWO MESSAGES, SPLIT BY WHO ACTUALLY HAS A RACK. Improvised implements: *"A starting guess — I have no way to know what your bag actually holds. Load what you have, log it, and I will work from your number from next session."* home_gym/minimalist: *"...I do not know which weights you actually have. Use the closest thing you own..."* **full_gym is deliberately untouched** — there the original sentence is simply true. The second message closes a gap the code already documented and had deferred: `LOADING_CEILING_KG_PER_HAND_OR_TOTAL` records that its values *"are full-gym ceilings and do not scale down by equipment tier — a home_gym/minimalist trainee's actual adjustable dumbbells realistically top out well below 50kg"*. The app still prices home kit against a commercial rack; it now at least stops instructing them to add weight it has no reason to think they own. Verified reaching the screen: `basis` -> `load_guidance` -> `LoadChip`'s explainer, not a string that dies in the engine.

- [x] ONBOARDING IS ADAPTIVE, BUT BARELY — measured while answering Ashley's second question. Of 22 slots, exactly **4** branch, and they are one chain: equipment -> *"will you be lifting barbells?"* -> *"do you know your lifts?"* -> the three numbers. Goal, style, experience, cardio, recovery and diet change nothing about what is asked next. So the precedent for an equipment-conditional weight question already exists — which is why "ask in onboarding" was a real option, and why Ashley's steer that it need not go there was worth taking: the same fix lands with no new question at all.

- [ ] FLAGGED, still open — the app prices home dumbbells against a 50kg commercial rack. The wording is now honest but the NUMBER is still full-gym-shaped. Asking for a real ceiling (at first use, or in chat, rather than onboarding) remains the actual fix; this round only stops the app asserting something untrue while it does not know.

- [x] A BACKPACK AT ITS LIMIT — built. Someone whose only load is a weighted backpack runs out of progression and the app had nothing left to offer. MEASURED (`report:frozen-weeks`, 5,731 transitions): `Backpack Row` **91** and `Loaded Backpack Walk` **42** were 133 of 217 repeated weeks — 61%, the largest single contributor. Ashley's ruling: slow the movement down. Chosen over more sets (fights the time budget and the set-count rules), harder variations (the right answer in principle, but those movements do not exist yet — a content request wearing a progression hat), and telling the trainee to put more in the bag (honest, but reads as giving up on the person who picked bodyweight *because* they have nothing to add). Plan: `docs/PLAN-a-backpack-at-its-limit.md`.

- [x] THE CONDITION ALREADY EXISTED AND MEANT THE WRONG THING. `applyTempoPrescription` was built on the principle that *tempo is how a lift progresses when it has no load to add*, and it gated on `ex.suggested_load_kg != null` — "has no weight". A backpack row at its ceiling HAS a weight (20kg) and was skipped, even though 20kg is every kilogram it will ever have. Widened to "has no MORE weight": `isImprovisedLoadImplement(entry)` AND at/above `IMPROVISED_IMPLEMENT_CEILING_KG` (8/12/20/25kg by experience). **Ashley's earlier ruling survives untouched** — chin-ups and dips are still excluded because a belt can always take another plate, so slowing them would paper over a gap instead of closing it (and that gap was since closed by the added-load round). A backpack cannot. That is the whole distinction: one implement has more to give, one does not.

- [x] VERIFIED BY READING A REAL PLAN, not just a counter. bodyweight/upper_lower/intermediate `Backpack Row`: weeks 1-8 the weight climbs 12.5 -> 17.5 with NO tempo, week 9 it reaches the 20kg cap and tempo appears, `4-1-1` through the strength block and `3-0-1` through hypertrophy, absent on every deload. Gate extended with the over-fire check that matters: **675 backpack lifts at their ceiling get a tempo, 0 below their ceiling do**, and the original "no tempo on a lift that already shows a weight" keeps its teeth for every other lift (still 0). `test:audit` 0 / 13,967; `test:session-length` clean with no session past its stated maximum — the duration risk was real (the tempo round caused a regression once) and it did not recur.

- [ ] **THE METRIC CANNOT SEE THIS FIX, and the number is unchanged at 217.** `frozen_week` compares load AND reps; tempo is a third lever it does not read. So a trainee's backpack row genuinely gets harder from week 9 and the score is byte-identical — exactly the shape already flagged for added weight (`quality-score` reads `suggested_load_kg`, not `suggested_added_load_kg`). Two levers now progress lifts invisibly to the scorer. Worth fixing as one job rather than twice.

- [ ] FLAGGED, THE BIGGER HALF IS UNTOUCHED — `Loaded Backpack Walk` (42 of 217) gets nothing here and that is deliberate. It is a carry: both its levers are exhausted at once (weight at the improvised ceiling, distance at the 55m carry cap), and tempo is meaningless on a walk — you cannot take three seconds to lower a carry. So the honest ceiling on this round is **91 of 217 (42%)**, not 61%. Stretching tempo to cover it would have been worse than leaving it named.

- [ ] FLAGGED, a different defect found while tracing — `bodyweight/beginner` showed `Backpack Row` going **8kg -> 7.5kg while reps climbed**, a rep bought with a lighter weight. `test:frozen-weeks` already tracks this class (9 bad against a HEAD baseline of 10) and it is separate from running out of load.

- [x] A CORRECTION TO MY OWN FIRST READING, made before building rather than after. I reported these lifts as "pinned at the ceiling from day one" on the strength of one profile. Wrong: `Backpack Row` at bodyweight/intermediate climbs 12.5 -> 17.5 -> 20 with reps rising inside each block, and the novice cases (12kg flat, reps 8-10 -> 10-12 -> 12-14) are textbook double progression, not a defect. The genuinely stuck transitions are narrower and cluster at week 10->11, after the load has actually converged. Measuring one profile and generalising is what produced the wrong version.

- [x] TIME FIT WAS SCORED AGAINST A NUMBER NOBODY WAS PROMISED — fixed. **METRIC CHANGE, say it out loud: Time fit readings before this are NOT comparable with readings after.** A trainee says "30-45 minutes"; the generator aims at the 37-minute MIDPOINT, and the scorer judged every day against that midpoint — so a 45-minute session, which is exactly what they said they had, was reported as *"21% over budget (expected: 37min)"*. It was the largest single source of deductions after frozen weeks, and most of it was measuring against a target the trainee never agreed to. The promise is a RANGE, so that is what is judged now: anywhere inside [minimum, maximum] is full marks, overrun is measured from the MAXIMUM and underrun from the MINIMUM, both keeping their existing tolerance shapes. The generator still AIMS at the midpoint — that was never the wrong part, and aiming mid-range is what leaves room to land inside it. MEASURED: `time_fit` **2,749 (29.8%) -> 490 (5.3%)**, i.e. **82% of those flags were the ruler, not the plan**; Time fit **1.62 -> 1.92 / 2**; overall **11.28 -> 11.57 / 12**. The 490 that remain are genuinely outside what the trainee asked for. The deduction TEXT was fixed in the same pass — it reported "% over budget (expected 37min)" against a promise of 30-45, so the sentence contradicted the score.

- [ ] FLAGGED — **the same wrong denominator sits in a second place.** `dev-constraint-audit.ts` CHECK 3 uses `DURATION_BUDGET_SECONDS * 1.15`, which for a "30-45" trainee is 37 x 1.15 = **43 minutes**, so the audit rejects 43-45 minute sessions that the trainee explicitly said were fine. Deliberately NOT changed in this round: loosening a gate is a different risk from correcting a score, and this is the check that caught the Preacher Curl duration regression earlier today — which, on this reading, may not have been a regression at all. Worth deciding rather than assuming, and worth noting the honest implication for that entry.

- [ ] FLAGGED, attribution not isolated — `frozen_week` read **5,722** before this round's work and **5,855** after. The likeliest cause is the one-rep-target-per-lift-per-week fix: forcing a second slot to show the first slot's reps removes apparent week-to-week variation that was really just two slots disagreeing. If so it is the truth surfacing rather than a regression. NOT PROVEN — the catalogue additions landed in the same window and each sweep is ~26 minutes, so no clean single-variable measurement was taken. Named rather than attributed.

- [x] A GATE FOR THE UNIT OF EVERY LOAD CEILING — `npm run test:ceiling-units`, Ashley's call after the per-hand/total mix-up cost a correct exercise. Four properties, each of which would independently have caught it: every ceiling sits above the worst legitimate TOTAL in its category; with real headroom; the ceiling can actually FIRE; and each category's unit composition is printed so a category gaining a differently-united movement is visible in a diff. TEETH PROVEN by re-halving `isolation_shoulder` to 25 — **three checks fire**, each naming the cause in a sentence (*"ceiling is a TOTAL: 25kg >= 2x the 20kg worst per-hand value"*). That is the whole point: last time the same fault surfaced as 56 unexplained failures next to a stale comment blaming the loading formula, and the wrong explanation was believed. The gate reads the real exported table, not a copy — a gate duplicating the numbers it guards is the same defect class it exists to catch.

- [ ] FLAGGED BY THE NEW GATE, bigger than expected — **10 of 20 ceilings can never fire at all**, because the implement's physical clamp is lower and always gets there first (`deadlift` clamp 300 vs ceiling 325, `pulldown` 100 vs 180, `isolation_calf` 100 vs 140…). Half the load backstops are decorative. Not a defect today (the clamp is doing the work and warns when it fires) but it means the ceiling check is largely redundant with the clamp, and the two should probably be one mechanism. Reported per-category by the gate rather than failed, since it is a design question.

- [x] HOME-GYM AND MINIMALIST DEPTH — `Front Raises`, `Band Lateral Raise`, `Band Shrug`. MEASURED: `isolation_shoulder` at home_gym and minimalist went from **0 same-pattern alternatives (fallback-served) to 2**; full_gym 1 -> 4; `isolation_trap` 1 -> 2 or 3 at every tier. Across all four equipment tiers: **0 true dead ends, 0 patterns leaning on the nearest-pattern fallback, 0 patterns where the cap hides options** — the first time all three read zero. Both band entries are reachable at home_gym AND minimalist, which is why a band and a dumbbell were chosen over another machine. Neither carries a ceiling risk: a band is not in `LOADED_EQUIPMENT` so no kilogram figure exists, and Front Raises is a per-side dumbbell pair like the Lateral Raises beside it.

- [x] ONE REP TARGET PER LIFT PER WEEK — a pre-existing defect fixed to **0 from a HEAD baseline of 19**, not merely back to baseline. Observed directly: `Backpack Row` week 11, Monday 16-18 @20 / Tuesday 14-16 @20 / Thursday 16-18 @20 — same weight, same sets, same RPE, one rep bracket apart. CAUSE: the frozen-load rep bump memoised its SIZE per lift per week (and its comment claimed both slots would therefore agree) but the frozen VERDICT is resolved per SLOT, so a dissenting slot never entered that branch to read the memo. **The obvious fix made it worse** — memoising the verdict took the count 22 -> 37, because forcing a later slot into the frozen branch buys reps for lifts whose own figure genuinely moved. Measured, then reverted. What works is memoising the OUTCOME: the rep string itself, applied after every rep lever has settled and after the load is resolved, so it cannot move a single weight. `test:audit` 0 / 13,967.

- [x] **I SHIPPED A REGRESSION TO MAIN AND CAUGHT IT A ROUND LATE.** `test:band-slots` reads **340 against a 348 floor** — rehab-indicated bands reaching main slots — and it was already 340 at `083c749`, the machines commit that is now on main. That round ran twelve gates and this was not one of them, on a gate written earlier in this same session. VERIFIED BEFORE TOUCHING THE FLOOR, because lowering one is how a real regression gets hidden: `test:rehab-prescribed` passes in full, and **64 of 64** knee-injured profiles still receive a knee-indicated movement. Nobody lost their rehab; 8 instances stopped being the day's MAIN lift, from pool churn as the catalogue went 135 -> 145. Floor moved to 335 with the reasoning written into the file, AND a new property check added that catalogue size cannot move: every knee-injured profile gets something indicated for the knee (48 checked, 0 without, denominator asserted so it cannot pass vacuously).

- [x] THE SAFETY NET COMPARED PER-HAND WEIGHTS AGAINST TOTALS — fixed, and **this is a retraction of what I reported one round earlier.** I said the shoulder loading fraction (0.19 of bench) was over-prescribing and that `Machine Lateral Raise` had exposed it. **Wrong.** Swept across both sexes, 50-120kg, all four experience tiers and every rep bracket: the heaviest per-hand lateral raise the app ever prescribes is **20kg against a 25kg ceiling**. The fraction is fine. `prescribeLoad` stores a dumbbell pair PER HAND and a machine as a TOTAL, so the machine's honest 40kg total — the *identical real load* to the dumbbell's 20kg per hand — was compared against a ceiling calibrated in per-hand units. **The entry was correct and I deleted it as unsafe.** HOW I REACHED THE WRONG ANSWER: I found a comment in `dev-constraint-audit.ts` asserting the fraction was the real defect and the ceiling was deliberately left failing, it cited "40kg for a 120kg advanced male", I saw exactly 40kg, and I treated the match as confirmation instead of checking it. The per-hand sweep takes two minutes and says 20. The comment predates `isPerSideLoad` landing (the per-side round) — stale, not wrong when written. I also walked into the per-side defect class *while explicitly checking for it*: I verified `unilateral` against neighbouring entries and never against the ceiling's own unit.

- [x] `SAFETY_CEILING_KG` -> `SAFETY_CEILING_KG_TOTAL`, every value now a stated TOTAL, both sides normalised at the comparison. Ashley's ruling: teach it the difference rather than patch the one symptom. MEASURED: **10 of 20 categories were already mixed-unit**, so those ceilings had been total-shaped by accident (the largest observed value in a mixed category is always a total) while per-side entries went *under*-checked. Three categories contain only per-side movements and were per-hand-shaped: `isolation_shoulder` 25->50, `overhead_carry` 70->140, `single_leg_dumbbell` 60->120 — the same allowance, restated. PROOF NOTHING WAS LOOSENED, old rule vs new rule across the whole population: **14,789 verdicts unchanged, 0 now stricter, 11 now looser — and all 11 are `Machine Lateral Raise` itself.** No existing exercise's allowance moved at all. Headroom after: `isolation_shoulder` sits at 80% of its ceiling, exactly the 20-of-25 ratio it had before. `test:audit` 0 / 13,967 with the entry restored; catalogue 142.

- [ ] FLAGGED, pre-existing, not chased — `leg_press` sits at **exactly 100%** of its 400kg ceiling, because that ceiling and the leg-press implement clamp are both 400. The audit uses `>` so it never fires, which means the backstop is decorative for that one category: the clamp gets there first, every time. Not a defect today (the clamp is doing the work) but a ceiling that cannot fire is not a ceiling, and it should either move above the clamp or be deleted as redundant.

- [ ] FLAGGED — **there is no gate on the ceiling table's units.** The audit proves today's numbers pass; nothing would catch someone re-halving a ceiling, or adding a machine to a pure-per-side category, except 56 mystery failures and (as this round showed) a plausible wrong diagnosis. The transferable lesson from the rest-floor round applies here too: a constraint needs a check at every path, and "the unit of this number" is a constraint.

- [x] THE CATALOGUE RAN OUT IN THE GYM — first round built. Ashley, testing live: a machine was busy, she looked for another machine physically in front of her, and the app didn't offer it. TWO defects, failing in different situations. (1) The swap CAP: `MAX_SMART_REPLACEMENTS = 8` while full-gym chest had 14 legitimate alternatives. **This was the third round of the same defect** — the cap's own comment records it being raised from 5 to 8 after an earlier user dead-ended (flat bench busy, only incline offered, also busy). Raising it again would have been the same mistake a third time, so the COUNT is gone as a mechanism: the ranked list is now unbounded and `SwapDialog` keeps the "don't overwhelm" job it already did properly (top 4, then "Show N more", which now reaches everything). (2) The CATALOGUE: no flat chest press machine, no shoulder press machine, no plate-loaded row, no hip thrust. MEASURED, full gym: chest swap options **8 -> 14**, rows **8 -> 11**, overhead pressing **4 -> 5 and its first machine ever**. New `npm run report:swap-coverage`. Plan: `docs/PLAN-the-catalogue-runs-out-in-the-gym.md`.

- [x] I GOT THE HEADLINE WRONG FIRST AND CORRECTED IT BEFORE BUILDING. I reported that `isolation_shoulder` had **zero** alternatives at home_gym — *"a trainee prescribed Lateral Raises has nothing to swap to"*. **False.** `getSmartReplacements` falls back to `NEAREST_PATTERN_FALLBACK` when same-pattern comes back empty, so it really offers 5 (all vertical_push). **How I reached it:** I wrote my own same-pattern filter and read "0 same-pattern" as "0 offered", and I only ever ran the REAL function against full_gym — where same-pattern is never empty, so the fallback never fired and I never saw it. I had even read that fallback's code earlier (it carries a long comment about a neck-injury shrug regression) and still didn't connect it. `report:swap-coverage` now prints SAME and OFFERED as separate columns for exactly this reason, and flags fallback-served patterns as a quality signal rather than a dead end. **True dead ends across all four equipment tiers: 0.**

- [x] TWO ENTRIES WERE WRITTEN, MEASURED, AND PULLED BACK OUT. Both by gates, neither by reading. **(a) `Machine Lateral Raise` put the audit 56 failures into the red** against the 25kg `isolation_shoulder` safety ceiling — and it did NOT create that defect, it removed the cover from one already documented. `dev-constraint-audit.ts` says outright that the ceiling is *"DELIBERATELY NOT RAISED... the shoulder isolation fraction (0.19 of bench) is what's wrong, not this ceiling."* Every existing lateral raise is a dumbbell or a per-hand cable and BOTH are clamped by a separate implement ceiling first; a machine has neither clamp. Measured on a 120kg advanced male: dumbbell 20kg, cable 20kg, **machine 40kg** — 40kg being the exact figure that comment predicted. Fixing the fraction changes every trainee's lateral raise, so it is its own round with its own plan; the entry came out rather than the ceiling going up. **(b) `Preacher Curl` cost 3 of 13,967 audit combinations**, all the same 30-45min bodybuilding "Back & Biceps" day landing exactly on the +15% boundary. Traced: that day went from FIVE exercises to SEVEN. **Catalogue depth is not free** — a deeper pool lets the day-filler fit one more distinct movement, and biceps already had six entries and no swap problem at all. The additions that earn their duration cost are the ones in THIN patterns or the ones a trainee physically cannot substitute for. Recorded in the file so the next batch measures instead of assuming.

- [x] I CHANGED A TEST AFTER IT WENT RED, AND MADE IT STRONGER RATHER THAN GREENER. `test:added-load` failed on *"reps still climb under the constant weight (none seen)"*. Added load had NOT broken: Chin-Ups (3-5/4-6/5-7) and Tricep Dips (5-7/6-8/7-9) both carried it correctly. The check pinned itself to **Pull-Ups by name** in one seeded plan, and the shifted selection put Pull-Ups outside a strength phase while the other two still worked. The name was never what it meant. It now asserts climbing reps for **every** lift carrying added weight rather than one, plus a separate check that some lift carries it at all — so it can no longer pass by the feature silently vanishing, which is the failure it was really guarding. Verified against the injury-rebuild precedent: that gate is still red and still NOT rewritten, because there the premise was invalidated and needs a decision; here the coupling was incidental.

- [x] THE SEARCH BOX I OFFERED TO BUILD ALREADY EXISTED. I put "let me search the whole catalogue" to Ashley as a fourth option without checking. `SwapDialog` has had `searchExerciseCatalog` the whole time, unfiltered by pattern or equipment, placeholder "e.g. Decline Bench Press". It also explains her report completely: she could search, and `Chest Press Machine` genuinely was not in the app to find. Verified state, not an assumption — and a reminder to read the component before offering to build what is in it.

- [x] QUALITY **11.25 -> 11.28 / 12**, and `main_lift_short_rest` is **553 -> 0** — zero occurrences anywhere in a 9,216-combination sweep, not merely below a display cutoff (grepped the whole report). **Structure reached a perfect 2.00**, which is the dimension that rule lived in. Nothing else moved materially: frozen_week 5725 -> 5722, time_fit 2762 -> 2749, load_incoherent 693 and push_pull_imbalance 566 both unchanged. NOTE ON THE RUN: the first sweep was killed and restarted, because I edited the engine after it started and its number would have described code that was never committed. It produced no summary before being killed, so nothing was lost — but the number reported here is the re-run, against the exact tree in `36018f5`.

- [x] THE HISTOGRAM COULD NOT REPRESENT ITS OWN SCALE, found by disbelieving the report rather than by a gate. `formatHistogram` was hardcoded to 0-10 while the score is out of 12 (six dimensions x 2): `Math.min(10, Math.floor(s))` clamped every plan scoring 10+ into the top bucket, and the render loop then LABELLED that bucket `[9-10)`. So the report read **"9214 plans in [9-10)"** directly beneath **"Overall average: 11.28"** — arithmetically impossible, which is the only reason it was caught. The data was always right; the axis was two points short and the label was actively wrong. Almost certainly introduced when `primerFit` took the total from 10 to 12 and the histogram kept its old constant — the same shape as the rest-floor defect two entries up: a constant updated in one place and not another. Now derived (`DIMENSION_KEYS.length * 2`) so it moves by itself. `OVERALL_FLOOR` deliberately stays a literal — 7.2 is a RATIO decision (60%) that should be re-argued, not drift with the dimension count. `belowFloor` is counted separately and was never affected, so "0 plans below the floor" was true throughout.

- [x] THE DAY'S HARDEST LIFT KEEPS ITS REST — built. **553 of 9,216 combinations (6.0%)** had the day's main lift resting under a minute; every observed case a bodyweight pull-up at 42s or 57s. TRACED, arithmetic and all: hybrid's tier1 base 90s, × conditioning's 0.8 multiplier = 72s, − `stageTimeCap` Phase 2's blanket −15s = 57s, − anatomical adaptation's −15s `rest_adjust_seconds` = **42s**. Ashley's ruling: a flat 60-second minimum under any main lift, bodyweight included, and nothing may take it below — not the time squeeze, not the phase. Chosen over a 45s bodyweight floor (still too short for a set of pull-ups, and would have left the app's own quality check disagreeing with the rule) and over protecting the prescription from the squeeze only (lands at 57s, still under the line). 60 is not a new number: it is the floor `trimWeekRestForBudget` already used and the line `quality-score`'s `main_lift_short_rest` already drew. **Before this the rule and the check disagreed.** New `MAIN_LIFT_REST_FLOOR_SECONDS`; `test:main-lift-rest` gains section 4.

- [x] **THREE PLACES WAS WRONG — IT WAS FOUR, and the entry below saying "three" is the reason I found it.** That lesson (rest is set independently at prescription, at the per-week `adjustRest`, and at `trimWeekRestForBudget`, so a rest constraint must be re-asserted at each) was correct and was followed. What it missed: `stageTimeCap`'s **Phase 2** (`exercise-plan.ts:815`) takes 15s off every non-cardio exercise with a 30s floor and **no main-lift gate at all**. Phase 1 immediately above it does exempt main lifts, and its comment claims *"main lifts keep full rest even under time pressure"* — true only of Phase 1, and I had read that comment as covering the function. The write-up of a lesson is not the same as an inventory of the paths. There is now ONE `mainLiftRestFloor(entry, policy?)` helper and all five sites call it, so a sixth path fails loudly instead of silently undercutting the other five.

- [x] THE OLD RULING STILL HOLDS, ABOVE THE LINE — and this is the half that was easy to break. The three existing floors were all gated on `isExternallyLoaded` because of Ashley's earlier ruling that *"the risk this guards is a bar, not a tier"*, deliberately keeping a conditioning chin-up denser than a conditioning squat. That is preserved: conditioning's 90s floor is still loaded-only, and its bodyweight main lifts land in the **60–90s band, 106 of 108** — asserted as a band in `test:main-lift-rest` §3, not just "still under 90s", so the new floor cannot quietly sweep them up to 90. The universal floor stops being a density question below 60, where it becomes "can the trainee finish the next set".

- [x] THE GATE HAD THE HOLE THE ENGINE HAD. `test:main-lift-rest` §2 asked *"nothing anywhere puts a **bar** under 60 seconds"* and passed green (0 of 1,944) the entire time a bodyweight pull-up sat at 42s. Same blind spot, same `isExternallyLoaded` gate, in the test as in the code — which is why it never fired. New §4 asks the question of bodyweight main lifts and of deload weeks. TEETH PROVEN by stashing the engine change and re-running: **60 of 648** bodyweight main lifts under 60s before, **0** after; **4 of 864** deload cases before, **0** after. The `isDeload` exemption the loaded floor carries was deliberately NOT copied — a light week does not make a set easier to finish 42 seconds later.

- [x] THE FLOOR COST ESSENTIALLY NOTHING, AND ONE TIER GOT BETTER. Mean session duration per equipment tier over 1,728 loading days: bodyweight 52.07 → 52.11min, minimalist 52.82 → 52.84, full_gym 53.45 → **53.40**. Days past the **stated maximum** (not the midpoint — that denominator has misled once already): bodyweight 2 → 2, minimalist 6 → 6, full_gym **12 → 7**. The improvement is the estimator fix, not luck: `computeDurationTopUp` and `sizeBlockToRestBudget` now apply the same floor when forecasting, so top-up stops re-adding sets the day cannot afford. Before-numbers taken by stashing the change and re-running, not from an older log. `test:audit` **0 / 13,967** and `test:session-length` byte-identical (6 / 21 / 7 under-minimum, 0 past maximum on its own grid).

- [x] THE SET LOG CAN RECORD ADDED WEIGHT — built end to end, migration AND write AND read-back in one round. The loop was open at BOTH ends and neither was what I assumed. (a) The log had no column, and `SetGrid` already let you type 15 on a pull-up — writing `weight_kg: 15, is_bodyweight: false`, i.e. *"the pull-up weighed 15kg"*, a row indistinguishable from an ordinary 15kg lift. **Not a missing feature, a wrong one.** (b) `TodayPanel:152` filtered `.filter(ex => ex.suggested_load_kg != null)` before asking for a recommendation, and a pull-up's is null — so `getDoubleProgressionRecommendation` was **never called for these four lifts at all**. The read-back was not merely unwired, it was unreachable. Plan: `docs/PLAN-the-set-log-can-record-added-weight.md`. New migration `20260825120000`; `test:logging-roundtrip` gains sections 8-11 (16 checks).

- [x] I DID NOT REPEAT THE `assistance_kg` MISTAKE, which is the precedent that set the shape. That column was added by migration `20260811100000` and its own comment says the write path "is NOT built in this migration — see the round's decision log for why that's scoped as its own pass". **That pass never happened**: two weeks on, the column exists and nothing writes it. A column with no writer is not a foundation, it is a loose end, so this round shipped the column, the write, and the progression read-back together or not at all.

- [x] THE PRE-MIGRATION SAFETY PROPERTY, and it is the part that could have hurt live users. Both write paths spread a fixed object, so adding `added_load_kg` unconditionally would make **every set write fail** with "column does not exist" until `db:push-both` runs — on the one path where losing data is unforgivable, for a feature nobody is using yet. Two rules instead: the key is **omitted entirely when null**, so an ordinary set's payload is byte-identical to what shipped before and 99.9% of logging cannot be affected; and a write that does fail on the missing column **retries once without it**, so the trainee keeps their reps and loses only the belt figure, only until the migration runs. `test:logging-roundtrip` §9 asserts the key is ABSENT rather than null (on the payload, not the outcome) and §10 drives the degradation through a fake PGRST204.

- [x] SEPARATE COLUMN, NOT A REINTERPRETATION OF `weight_kg` — which means "the weight of the thing you lifted". Overloading it to mean "the weight you hung off yourself", switched on `is_bodyweight`, would be one field answering two questions: the defect class this repo has now hit five times (`loads_joints` answering "is this dangerous", the shrug `isolation_trap`, the slot-inheritance leak). A weighted pull-up is `weight_kg 0, is_bodyweight true, added_load_kg 15`, so `isMalformedZeroWeight`, `maxWorkingWeight`, the ghosts and the PR cache all keep seeing exactly what they saw before — asserted in §8 both ways round.

- [x] TWO DEVIATIONS FROM MY OWN PLAN, both deliberate. (1) The plan specified a new gate `test:added-load-log`; I extended `test:logging-roundtrip` instead, because a new file would have meant duplicating ~200 lines of fake Supabase client and two sources of truth for how the store behaves. (2) `SetGrid`'s PR paths (`checkForPR` and `getTopPRSet`) both read the STORED weight, so a weighted pull-up keeps exactly today's behaviour (bodyweight, PR by reps) rather than half-adopting added weight as the PR metric — passing the typed figure to one and the stored 0 to the other would have left the badge and the check disagreeing inside one function.

- [ ] FLAGGED — **PRs do not yet account for added weight.** A +20kg chin-up and a bodyweight one are the same PR as far as `pr-engine` is concerned. Real, deliberately not half-done in this round, and now a small change rather than a blocked one.

- [x] A CARRY THAT ACTUALLY GETS FURTHER — built. Carries were the largest remaining block of repeated weeks: **100 of 265** frozen week-to-week transitions. Someone carries 22kg for 40m and it is still 22kg for 40m four months later. TRACED, not assumed: the distance is a hardcoded `'40m'` in `fixedUnitPrescription`, and the comment above it stated the design outright — *"a 40m farmer's walk is 40m for everyone; **only the load moves**"*. Coherent until the load converges on its ceiling, after which **nothing** moves: carries are excluded from `rampReps` AND from the frozen-load rep bump, correctly, because `shiftReps` passes `'40m'` straight through. Ashley's ruling: when the weight can't move, walk further. MEASURED: carries **100 → 52**, total **265 → 217 (4.6% → 3.8%)**, with loaded non-carry (128) and bodyweight (37) unchanged. New `FROZEN_CARRY_DISTANCE_STEP_M = 5`, capped at 3 steps.

- [x] QUALITY **11.19 → 11.25 / 12**, the largest single move of the session, and it lands exactly where the change was aimed: `Progression` **1.69 → 1.75**. `Time fit` pays 1.62 → 1.61 for the longer carries, which is the honest cost and is measured against the MIDPOINT budget; against the number the trainee actually gave us, the stated maximum, the change is one day in 6,912. Every other dimension unchanged.

- [x] THE RESIDUAL 52 ARE AT THE CAP, VERIFIED RATHER THAN INFERRED FROM THE EXAMPLES. Counted by the distance they are stuck at: **52 at exactly 55m** (40 + 3×5) and 2 at 40m (no previous loading week to compare against). That is a ceiling reached, not a lever missing — materially different from before, when nothing ever moved. Cap kept at 3 steps deliberately: 40 → 55m is already +37.5%, and without a cap a sixteen-week plan walks to 100m, which is a different exercise.

- [x] THE DURATION MODEL HAD TO LEARN DISTANCE FIRST, and that was the real risk. `computeSetWorkSeconds` returned a flat `fallbackSeconds` for ANY distance — **a 50m carry cost exactly the same as a 40m one**. Fine while distance was a constant; a silent way to blow a session budget the moment it isn't, and `duration` is the audit check that has now bitten three times. Fixed WITHOUT a new pace constant: each entry's own `avg_duration_seconds` already implies one over the default distance (Farmer's Walk 35s/40m = **1.14 m/s**; Loaded Backpack Walk 40s = **1.00 m/s**), so every carry keeps its tuned pace and scales. Measured cost, isolated from the tempo round: mean +0.1min, days over the STATED MAXIMUM 14/48/95/88 → 14/48/95/**89** — one day, in full_gym.

- [x] MY OWN GATE WOULD HAVE PASSED A REAL DEFECT, and finding that was worth more than the feature. The first cut of the carry over-fire check ran ONE profile and asserted *"distance never moves in the same week the load did"*. It passed. Swept across every combo the true figure is **8 of 272**, and reading a real plan showed why: `Suitcase Carry w5:45m@12 → w6:40m@14` — the distance visibly goes BACKWARDS. Measured all 8: **every one has the load going up**, zero with the load flat, so it is the deliberate reset (a carry that earned a heavier weight returns to base distance, exactly as double progression resets a rep range). The check was wrong on both counts — wrong number, and looking for the wrong property. Rewritten to sweep `everyCombo()` (260 transitions) and assert what actually matters: **distance never shortens unless the weight went up** (0 of 260), and never grows in the same week the weight did (0).

- [x] TWO OTHER ASSERTIONS CHANGED MEANING WITH THIS, both deliberately and both recorded rather than quietly flipped. (1) `test:frozen-weeks` §4 read *"carries keep their fixed distance"*, written so the exclusion "can't quietly rot into an accident" — Ashley has now answered the question it was holding open, so its teeth move to the other side. (2) §5 read *"carries are still frozen — the exclusion is real, not accidental"*; it now asserts the halving with a floor under it, so neither a regression nor a silent walk past the cap passes. (3) The rep-ceiling check in §3 started failing because `repLow('45m')` reads 45 and compared it to a 40m baseline as "+5 reps" — correct arithmetic, wrong unit; carries are now excluded from a check that was only ever about reps.

- [x] `fixedUnitPrescription`'S DOC COMMENT SAID SOMETHING THAT STOPPED BEING TRUE the moment this shipped, so it was rewritten rather than left. It still returns the DEFAULT distance, which is what a carry rotated into a slot by swap/ban/injury/equipment must start from — `test:slot-replacement` green.

- [x] THE COACH CAN LOG ADDED WEIGHT — built, and **I had the mechanism wrong when I flagged it.** I wrote that "chat-gemini's tool schema has no field for it" and that it needed an edge-function deploy. Neither is true: `log_workout`'s contract is *"the server does NOT parse anything here — it forwards the model's raw text spans untouched"*, and `src/lib/set-parse.ts` is **the only place a number becomes a number**. So the fix is entirely CLIENT-SIDE and ships with the Vercel push — **no chat-gemini deploy required**. I said the opposite when recommending this work.

- [x] AND THE DEFECT WAS WORSE THAN "NO FIELD FOR IT" — the stated weight was **silently discarded**. MEASURED before: `"3x5 @15kg"`, `"3x5 with 15kg"`, `"3x5"` and `"3x5 @bodyweight"` on Chin-Ups all produced the **identical row**, `weightKg 0`. The cause is one line: `isBodyweight = parsed.isBodyweight || (entryEntry != null && !entryIsLoaded)` — a chin-up is not externally loaded, so the override fired and threw the figure away. **Silently is the worst version**: the check directly above it in `test:pending-actions` shows a LOADED lift blocks on a weight ambiguity and asks rather than guessing. These four just dropped it. Now `@15kg` on a chin-up carries `addedLoadKg 15` with `weightKg` still 0, and the receipt reads **"BW +15kg"** rather than a bare figure. `test:pending-actions` gains 8 checks including the over-fire (a barbell lift's number still goes to `weight_kg`).

- [ ] FLAGGED, PRE-EXISTING, NOT WIDENED — `WEIGHT_RE` requires a literal `@`, so **"3x5 with 15kg" still loses the weight**. That is not specific to these four: `"bench 3x8 with 80kg"` fails to parse too, and for a loaded lift it degrades correctly (asks "what weight did you use?") while for these four it stays silent, because we cannot tell "no weight stated" from "weight stated in a form we can't read". Widening the regex touches every exercise's parse and wants its own round with its own measurement — deliberately not done inside this change.

- [ ] **`assistance_kg` IS NOW A STRICTLY SMALLER JOB.** This round built the exact plumbing its migration comment said it needed — an optional-kg column threaded through `SaveSetInput`, `PendingSet`, both write paths, the server row shape, and a progression read-back. Assistance is the mirror (less over time = stronger) and still wants its own pass, but the pattern to copy now exists and is gated.

- [x] A DIP IS NOT A PULL-UP — built. New `added_load_scale` on the exercise entry, relative to a weighted pull-up at 1.0: **Chest Dips 1.4, Tricep Dips 1.2**, absent (=1.0) on Pull-Ups and Chin-Ups so the change could not move them even by rounding. An 80kg advanced trainee in a strength block now reads `pull-up +15kg / upright dip +17.5kg / chest dip +20kg`; a 110kg one `+20 / +25 / +27.5`; a 62kg woman `+10 / +12.5 / +15`. Ashley chose this over matching the strength charts outright (which would have given the 80kg man +25 / +27.5 and the 110kg man **+37.5kg**).

- [x] I CORRECTED TWO THINGS I HAD WRITTEN ABOUT THESE EXERCISES. (1) I described Tricep Dips as **"bench-style"** — it is not. It is the same dip bars as Chest Dips with an UPRIGHT torso (`form_cues: 'Upright torso (more tricep)'`), `joint_stress: 'moderate'`, and it is the only one of the four reachable at NOVICE (`minExperience: 'novice'`, vs 'intermediate' for the other three). The difference between the two dips is torso angle, not apparatus, which is why 1.2 vs 1.4 rather than something wider. (2) The earlier entry called the single fraction table "deliberately conservative" as though that were a finished decision; it was an unexamined default, and reading the entries is what showed the two dips differ.

- [x] WHY NOT THE STRENGTH CHARTS, with the number that decided it. At a 2.0x chest-dip scale a **50kg trainee's chest dip lands on exactly 17.5kg — the 35% safety cap itself**. A formula that reaches its own ceiling is no longer prescribing; the ceiling is. `test:added-load` now asserts the cap NEVER binds across seven bodies (50-110kg) and all four lifts, so it stays a backstop rather than quietly becoming the prescriber. The shipped 1.4x leaves margin at every body measured.

- [x] ASHLEY ASKED WHAT THE NUMBERS ARE BASED ON, and the answer is now in the code rather than only in chat. It is a percentage of the trainee's OWN BODYWEIGHT — because a dip or a pull-up IS lifting your bodyweight, so what you can add on top is proportional to what you are already moving. Three inputs: bodyweight, experience tier (intermediate 10% / advanced 18% at the pull-up baseline), and which movement (the new scale). **What it is NOT based on: anything the app has watched them do.** That limit is the reason week one of every block is bodyweight-only, the reason the guidance says "drop the weight and keep the reps if it costs you form", and the reason the 35% ceiling exists at all. `ADDED_LOAD_BODYWEIGHT_FRACTION`'s doc comment now says all of this in place.

- [x] A CHIN-UP THAT ACTUALLY GETS HEAVIER — built, closing the gap Ashley named. `report:added-load` BEFORE, across 8,928 slots (4 equipment × 4 experience × 4 goals × 3 splits × 4 bodies): Pull-Ups 3,960, Chin-Ups 3,912, Tricep Dips 608, Chest Dips 448 — and **zero carrying a weight**. The defect that produced, traced on a full-gym advanced 80kg male: `Pull-Ups w1:9-11 w2:10-12 w3:11-13 | … | w9:3-5 w10:4-6 w11:5-7` — **week 9 prescribed THREE reps to someone the plan believes can do eleven, with nothing added.** That is not a strength block, it is less work; the phase note promises *"Heavier and lower rep"* and delivered only the second half. AFTER: **801 of 8,928 slots (9%)** carry weight — the strength and power blocks and nothing else — reading `w9:3-5@+15 w10:4-6@+15 w11:5-7@+15`. Plan: `docs/PLAN-a-chin-up-that-actually-gets-heavier.md`. New gate `npm run test:added-load` (22 checks).

- [x] THE DESIGN I BUILT FIRST WAS WRONG, CAUGHT BY LOOKING AT THE OUTPUT RATHER THAN THE GATE. The first cut scaled the added weight DOWN as reps rose (1.0 at ≤5 reps, 0.6 at 6-8, none above) with a 0.7/0.85/1.0 step across the block. It passed a typecheck and produced numbers, and the trace showed what it really did: `w9:3-5@+12.5 w10:4-6@+15 w11:5-7@+10` — **weight going up then DOWN inside one block**, and vanishing entirely at `w7:8-10`. Cause: reps climb within a block (that IS the within-block lever) and my rep scale cut the weight as they climbed. Two levers fighting, producing more reps at less weight — a deload wearing progress's clothes, the exact failure the frozen-week round already documented and which I walked into anyway. Rewritten to key on the **phase**, which cannot change inside a block, so the weight is constant by construction. The gate now pins both halves — weight constant within a block, reps still climbing under it — so the rejected design cannot come back.

- [x] FOUR REFUSALS, each for its own reason: no added weight on a **deload** (recovery is doing less, not carrying the same belt for fewer reps — same reason tempo skips it), none on the **calibration week** (the app has never seen this person do a single pull-up), none **below intermediate** (unreachable today since Pull-Ups/Chin-Ups/Chest Dips already carry `capability_requirement: intermediate` — measured 0 slots — but asserted so relaxing that gate can't silently load a novice's dips), and none above a **10-rep target** (a guard against an odd phase config, not a scale; never fires today).

- [x] IT IS ADDED WEIGHT AND THE SCREEN SAYS SO. New `suggested_added_load_kg`, deliberately NOT `suggested_load_kg`: putting 15 in the ordinary field would render "15kg" beside "Pull-Ups", which reads as *lift 15kg* — the class of untrue statement `LoadChip`'s own header exists to prevent. New `AddedLoadChip` renders "+15kg added / on top of bodyweight", mirroring how `AssistanceChip` already inverts the framing for assisted pull-ups. Keeping it a separate field also means every consumer of `suggested_load_kg` — `enforceLoadCoherence`, quality-score's `load_incoherent`, the frozen-week report, `isLoadlessWeek`, the tempo exclusion — carries on treating a pull-up exactly as before. No silent reinterpretation of an existing field.

- [x] TWO CEILINGS, and no third discount. 35% of bodyweight absolute (a backstop in the spirit of `SAFETY_CEILING_KG`; max actually seen is 15kg against a 28kg cap), plus `IMPROVISED_IMPLEMENT_CEILING_KG` for a bodyweight-tier trainee since with no gym the added weight IS a loaded backpack — **currently unreachable and kept anyway**, because `BODYWEIGHT_ALLOWED_PHASES` excludes strength and power so a bodyweight plan never enters a phase this loads. Deliberately NO extra conservatism multiplier for an assumed body: `load-prescription.ts`'s calibration comment records that as tried and measured — *"the fix for an unknown body has to be the body… stacking a discount on top of a conservative body compounds to absurdity without adding safety."*

- [x] QUALITY IS UNCHANGED AT **11.19 / 12**, and every rule count is byte-identical to the pre-change run (frozen_week 7102, time_fit 2732, load_incoherent 693, …). That is not a null result, it is a measurement: `quality-score.ts` reads `suggested_load_kg`, and this change deliberately writes `suggested_added_load_kg` instead, so **the scorer cannot see the added weight at all**. No regression and no credit. Also means the added weight costs nothing in session time, which was the risk worth checking — a belt does not make a set longer.

- [ ] FLAGGED, a gap the above exposes — **quality-score has no visibility into whether these four lifts progress.** Its `progression` dimension reads load and reps; a pull-up shows null load, so a strength block that now goes `3-5@+15 → 4-6@+15 → 5-7@+15` scores exactly the same as one that went `3-5 → 4-6 → 5-7` with nothing on the belt. Teaching it `suggested_added_load_kg` is a small change to the scorer, and it should be done before the next round claims a progression number for these lifts.

- [ ] FLAGGED, the tightest number here — the declined-metrics case **ties rather than clears** at the bottom. `ASSUMED_BODY` is 50kg, so a stated 50kg trainee and a declining one both land on 10kg, and below 50kg the declining one would get MORE. That boundary is `ASSUMED_BODY`'s, not this function's: it applies to every load in the app, and `test:assumed-body`'s own comparison set stops at 50kg for the same reason. Pinned in `test:added-load` so the tie is deliberate rather than discovered later.

- [x] ~~STILL OUT OF SCOPE~~ **BOTH NOW DONE.** (1) dips take more than pull-ups — see the `added_load_scale` entry. (2) the logged-history path — see the set-log entry above.

- [x] MAKE THE TEMPO REAL — built. The loadless coach note shipped in `074ad9d` tells a bodyweight trainee *"the difficulty comes from how you move: about three seconds lowering, a pause at the bottom"* — and **that sentence was the only place tempo existed**. No field anywhere in the app, nothing prescribed, nothing tracked, nothing progressed; the session still said `Air Squat 9-12` and stopped. A promise with no delivery, same shape as `update_workout_schedule` and the Muay Thai swap, and worth fixing precisely because we had just made the promise. Tempo now becomes **the phase's voice for a lift that cannot take weight**: `2-0-1` in anatomical adaptation, `3-0-1` in hypertrophy, `4-1-1` in maximal strength; none in power (explosive intent) or metabolic (short rest is the stimulus). One lever at a time — tempo is the BLOCK's and constant within it, reps stay the week's, following `loadStepUnaffordable`'s precedent. Plan: `docs/PLAN-make-the-tempo-real.md`. New gate `npm run test:tempo-prescription` (21 checks).

- [x] ASHLEY'S CORRECTION, and it changed the design: **"Pull ups can add weight."** She is right — a chin-up or a dip takes a belt or a loaded backpack, so *"there is no weight to add"* is simply false for them. Showing no weight on a chin-up is **a gap in this app, not a fact about the exercise**, and a slow eccentric there would paper over the gap rather than close it. New `accepts_added_load` on the exercise entry, set on exactly four — Pull-Ups, Chin-Ups, Chest Dips, Tricep Dips — and excluded from tempo. Everything else on the bodyweight list progresses by LEVERAGE (a deficit push-up, a straighter hanging leg raise) and an assisted pull-up loads the other way entirely. Coverage after the exclusion: bodyweight 30.1%, minimalist 20.2%, home_gym 11.5%, full_gym 6.8% of working sets.

- [x] ~~THE REAL FIX FOR THOSE FOUR, NOT DONE~~ **DONE in `583718a` (`prescribeAddedLoad`), `2398d0c` (per-movement scale) and `484772d` (the set log).** Left open here for three rounds after it shipped — my own note drifting from the code, the same shape as the migration mistake. Original text kept below. ~~if a chin-up can take weight, the app should be PRESCRIBING that weight rather than only cutting reps in a strength block.~~ Needs strength-standards handling for weighted pull-ups/dips (assisted pull-ups already have `suggested_assistance_kg`, so the machinery is half there). Until then those four lifts still show reps falling with nothing to explain it — the original defect, unfixed for that slice, and named here rather than quietly counted as solved.

- [x] I CAUSED A DURATION REGRESSION AND FIXED IT AT THE CAUSE. `test:session-length` went red: **2 of 576 "30-45" sessions ran to 45.8 minutes**, 48 seconds past a ceiling that read 0 before. Both were Maximal Strength weeks with a `4-1-1` push-up at 4 sets. Cause: `enforceWeeklyPatternBalance` is documented as needing to run LAST — after every duration pass — and it BUMPS SETS to balance push against pull, a known couple-of-minutes drift (see the older entry below). Tempo made those minutes cost more, since a `4-1-1` rep is 6s against `SECONDS_PER_REP`'s assumed 3.5. Fixed by a **final safety trim against the STATED MAXIMUM** (new `getSessionMaximumSeconds`), not the midpoint budget the earlier pass aims at — so it is a backstop, not a second opinion: every day already inside what the trainee asked for is skipped untouched, and only a day that broke the ceiling gives up rest. Runs inside the existing `if (!isDeload)` guard, honouring `trimWeekRestForBudget`'s contract, and respects `minLoadedMainLiftRestSeconds` so it cannot erode the conditioning rest floor (`test:main-lift-rest` re-verified green).

- [x] THE DURATION MODEL CAN SEE THE TEMPO, which is the whole risk. `SECONDS_PER_REP` is 3.5 and its own comment says it already assumes a controlled rep, so a `4-1-1` set costs 71% more working time. `DurationSlot` now carries tempo and `computeSetWorkSeconds` REPLACES the generic pace rather than adding to it (adding would double-count). MEASURED across 6,912 days per tier, before → after: mean 48.4→48.5 / 49.4→49.6 / 50.2→50.3 / 50.2→50.3 min, and days over the stated maximum 14→14, 51→48, 97→95, 92→88 — flat to slightly BETTER. Worst-case ratio unchanged at 104/111/109/107%.

- [x] QUALITY COST, NAMED RATHER THAN WAVED AT: **11.20 → 11.19 / 12**. The whole of it is `Time fit` (1.63 → 1.62), with `timeFit/time_fit` firing on **2,645 → 2,732 combinations (+87, 28.7% → 29.6%)**. Every other dimension is unchanged to two decimals. That is the expected and honest cost: a slower rep makes a session marginally longer, and quality-score's `time_fit` measures against the MIDPOINT budget. Against the number the trainee actually gave us — the stated maximum — the same change is flat-to-better (days over: 14→14, 51→48, 97→95, 92→88). Both readings reported because they answer different questions and only one of them is what a trainee asked for.

- [ ] FLAGGED, PRE-EXISTING, NOT CHASED — that same wider grid (4 goals × 3 experience × 3 durations × 3 splits, a 62kg female) finds **~1% of loading days over the stated maximum in every tier**, at HEAD as well as after. `test:session-length` reads 0 because its own grid is a different profile set, so "0 over budget" is true of that grid and NOT of the app generally. The trimmer only pulls rest and gives up once rest is at its floor, which is the likely cause — inferred from its documented behaviour, NOT verified case by case. Worth a round of its own.

- [x] "ADD LOAD NEXT WEEK" — TO SOMEONE WITH NO WEIGHTS. Fixed. A trainee whose equipment answer was **bodyweight** opened a week of Box Squat (Bodyweight), Deficit Push-Ups, Table Row and Low Box Step-Up and was told *"with **load climbing week to week**… **find the weight** where the last rep feels like RPE 6"*, *"**Baseline week — this sets the working weight** every later week adds load on top of"*, and *"**Drop the load** if you need to"*. Every one is an instruction about equipment she does not own — same class as the assumed-body work. MEASURED: **369 of 576 bodyweight week notes → 0**; full-gym weeks still carry the load coaching (381 of 576), so the new wording did not leak to people who do have weights. Ashley's ruling: fix the words first, then look at the training. Plan: `docs/PLAN-dont-tell-someone-with-no-weights-to-add-weight.md`. New gate `npm run test:loadless-notes` (20 checks).

- [x] THE NUMBER THAT STARTED IT, AND THE WRONG VERSION OF IT I ALMOST REPORTED. Measuring "does a 16-week plan actually progress" first gave **46.5% of exercises end lower than they started** — a scary figure that is simply wrong. It was measuring PERIODIZATION: `anatomical_adaptation` carries `rep_shift +3` and `strength` carries `−3`, so any plan opening in one and closing in the other shows reps falling by six regardless of progress. Re-measured phase-neutrally (Epley e1RM): loaded lifts **83.5% up / 8.6% lower**. Bodyweight lifts are 84.7% lower AS PRESCRIBED but **77.2% up with the block's own rep shift removed**, 4.9% genuinely regressing. So the engine is fine and the defect was entirely in what the trainee is TOLD — a loaded lift has a weight column to carry "fewer reps but heavier", and a bodyweight lift has no column at all (`LoadChip`: *"Bodyweight renders no chip and no ⓘ"*).

- [x] TWO THINGS I REPORTED AS PART OF THIS DEFECT THAT ARE NOT. (1) *"Heavier and lower rep"* and *"Move the weight fast… bar speed"* never reach a bodyweight trainee — `BODYWEIGHT_ALLOWED_PHASES` already excludes 'strength' and 'power'. The loadless variants cover those two phases anyway so relaxing that restriction can't silently reintroduce it. (2) The per-EXERCISE guidance was already correct: *"Progress by adding reps or slowing the tempo before adding load."* The defect was confined to the WEEK note, assembled from three independent sources (`phaseConfig.coach_note`, `policy.coachNote`, `buildProgressionNote`) — all three needed changing, the same "re-assert it at every path" shape the main-lift rest floor taught.

- [x] THE PREDICATE KEYS ON THE PLAN, NOT THE EQUIPMENT ANSWER, and that distinction is the point. `EQUIPMENT_SETS` includes `'weighted backpack'` at EVERY tier deliberately — it is the one progressive load available with no gym — so a bodyweight-tier plan does contain a couple of real numbers (Backpack Row, Loaded Backpack Walk). Keying on `equipment_access === 'bodyweight'` would have declared those weightless. `isLoadlessWeek` instead asks the week it just built: at most 25% of working sets carrying a load. MEASURED loaded share: **bodyweight 12.2%, minimalist 52.2%, full_gym 77.0%** — the threshold sits in the gap rather than on top of either number, and the gate prints all three every run so it can't drift unseen.

- [x] NO PRESCRIPTION CHANGED, PROVEN RATHER THAN ARGUED. Generated 432 plans (4 equipment × 4 goals × 3 experience × 3 splits × 3 injury sets) with `coach_note` stripped, against both engines: **identical md5 over 163MB**. `test:quality` therefore cannot have moved and was deliberately NOT re-run — a checksum over every prescription is a stronger statement than a score. (First cut of that comparison was a FALSE pass: both sides came back empty because the `grep -v '^\['` I use to strip debug lines was deleting every data line, each plan being a JSON array. Caught because "0 lines vs 0 lines" is not evidence of anything.)

- [ ] SECOND HALF, PARTLY BUILT — Ashley asked for the words first "and then the training". The training half arrived as TEMPO (`c343ca4`), not as a harder movement. Progressing the MOVEMENT (Air Squat → a harder variation) was measured and is **blocked on content, not code**: inverting `capability_requirement.regression` gives 15 entries forming 10 links, so only **53 of 731 (7.3%)** of a bodyweight trainee's weightless working slots have a harder version they can reach, and Air Squat, Box Squat, Plank, Side Plank, Low Box Step-Up and Step-Down have **no rung up at all**. Needs ~15-25 new exercise entries written before the engine change is worth anything.

- [x] A BAND WHERE A BARBELL SHOULD BE — fixed. A **full gym** user was prescribed `Band Shoulder Press` for a tier2 compound slot. The training objection an LLM review raised (bands lose tension at the bottom) is fair but not the one that bites: a resistance band is not in `LOADED_EQUIPMENT`, so the app shows **no weight at all** — the weight column that carries a lift's whole progression story is blank and the lift can only progress by reps, while a barbell sits unused in the same pool. MECHANISM, traced: `scoreCandidate` weighed five factors plus a tier bonus and **not one of them looked at the implement**. `Dumbbell Shoulder Press` and `Band Shoulder Press` are both tier2_compound vertical_push and scored IDENTICALLY, so the winner was decided by the ±0.3 tie-break jitter — a coin flip, every time. MEASURED (`npm run report:band-slots`, 18,909 main/secondary slots): bands holding a main slot with a loaded same-pattern/same-tier peer available **637 (3.37%) → 319 (1.69%)**; `Band Shoulder Press` **123 → 0**. All four ranking call sites funnel through one `orderCandidates`, so unlike the rest floor this was a single choke point. Plan: `docs/PLAN-a-band-where-a-barbell-should-be.md`. New gate `npm run test:band-slots`.

- [x] THE RESIDUAL IS NOT UNFIXED WORK, and was chased rather than waved at. Of the 95 surviving full-gym cases: **90 had the loaded peer's movement family already claimed by that day** (the duplicate-family guard runs ahead of scoring, so the peer was never eligible for the slot), **4 had it already used elsewhere that week**, and **1 is a genuine miss** (`Kneeling Band Lat Pulldown` over a free `Close-Grip Lat Pulldown`). The gate's ceiling is 10, not 0, for exactly that reason. NOTE ON THE NUMBERS: the first cut of `report:band-slots` asked the looser "did the day contain ANY loaded main lift", which counted a band as a defect whenever an unrelated slot elsewhere in the session held a dumbbell. Both figures above are the tightened definition measured against BOTH engines — the looser one read 906 → 615 and is not comparable.

- [x] THREE GUARDS, each load-bearing, because the easy way to drive this number to zero is to break one. (1) The penalty only applies when **another candidate in the same ranked list carries a real load** — a minimalist trainee whose only `vertical_pull` is a band keeps it; 378 such placements survive by design. (2) A band **indicated for a flagged joint is exempt**: `Spanish Squat` is a patellar-tendon rehab tool and the rehab pass puts it there on purpose — measured 348 → **352**, i.e. it went UP, because an exempt band now outranks the non-indicated bands it used to tie with. (3) The penalty is **12**, above the jitter (0.3) and above anything the five factors can swing together (~5), and far below the 30-point gap between mechanics tiers — so it reorders implements within a tier and can never reorder the tiers. The gate reads all three out of the source, so raising the constant past the tier gap fails a check instead of silently restructuring sessions.

- [ ] FLAGGED, PARTLY EXPLAINED — `test:quality` is **unchanged at 11.20 / 12** and `Selection` is unchanged at 1.94, but one rule moved: `selection/load_incoherent` **651 → 693 combinations (+42, +0.46pp)**. Direction of travel is "newly visible, not newly caused": ~318 main/secondary slots that previously showed NO weight now show one, and every branch of `load_incoherent` requires a non-null `suggested_load_kg` — a lift with no weight cannot trip a rule that compares weights. **My first, more specific theory was wrong and is recorded because it was nearly reported as fact**: I assumed a band standing as the day's main press had been silently disabling the rule's whole isolation-vs-main-press branch (which needs `mainPress.suggested_load_kg != null`). Measured instead of asserted — of 855 days whose main press carries no weight, **0** have a band in that position, at HEAD and after, because a band tier2 is almost never the day's FIRST main-press match. The remaining +42 is consistent with the newly-loaded lifts simply being checkable at all, but that has not been proven exercise by exercise. Small, flat headline, flagged rather than claimed.

- [ ] FLAGGED, deliberately not done — the rule is "a real weight beats a **band**", not "a real weight beats anything unloaded". `Pull-Ups`, `Dips` and `Push-Ups` are legitimate main lifts with their own progression (harder variations), and demoting them for a full-gym trainee would be a much larger change than the one asked for. Named here rather than assumed either way.

- [ ] **`test:injury-rebuild` IS RED, AND HAS BEEN SINCE BEFORE THIS CHANGE — I previously reported it fixed and that was wrong.** Bisected across nine commits: it fails at `6646854`, `a6f8e79`, `b3bb0b6` on *"a thinning injury does not trigger a rebuild"* (the wiped-pattern cause I diagnosed and fixed last session), then from `f7939ef` onward on a **different** check in the same file — first *"rebuild beats what substitution would have left"*, then *"the two modes genuinely differ"*. **How I got it wrong: I fixed the check I had bisected to, confirmed that one check passed, and did not re-read the rest of the gate's output.** The failing assertion moved; the gate never went green. CAUSE, and it is not a defect: the six shoulder-friendly variants plus `NEAREST_PATTERN_FALLBACK` made **pointwise substitution stop being lossy** (shoulder substitute slots 388 → 446), so `rebuiltSlots > subSlots` no longer distinguishes the two modes — the gate's premise was invalidated by the content work that fixed the underlying problem. Deliberately NOT rewritten to go green: whether the rebuild path still earns its place now that substitution covers shoulders is a real question, not a test-maintenance chore. Band change measured against it either way: HEAD 448 vs 450, with the change 436 vs 446 — both fail, neither for band reasons.

- [x] A LOADED BARBELL WITH 42 SECONDS REST — fixed. Someone whose goal is **conditioning** was prescribed Barbell Squats and Barbell Bench Press with 42s between sets. Nothing was broken: conditioning's `restSecondsMultiplier` is 0.8 on tier1, and on a short session that lands at 42s — the goal working exactly as designed, on the one exercise where it should not. Ashley's ruling: *the session still conditions, the part with a bar on your back does not.* Short rest stays everywhere else (accessories, machines, bodyweight, carries); a LOADED tier1 lift gets a 90s floor. MEASURED: conditioning's loaded main lifts under 90s **391 → 0** on loading weeks; nothing in any goal now rests under 60s (0 of 1,944). `test:audit` still 0/13,967 and `test:session-length` unchanged — the extra rest did not push sessions over budget. New gate `npm run test:main-lift-rest`.

- [x] SCOPED TO THE GOAL, DELIBERATELY, after measuring the alternative. A blanket 90s floor would have changed **~20% of loaded main lifts for every other goal too** (hypertrophy 87/432, fat_loss 118/432, strength 84/432…), which is far wider than what was approved — and those are not dangerous: conditioning was the **only** goal that ever went below 60s at all, and a hypertrophy bench at 75s is a deliberate, normal prescription. So the floor is a named field on the goal policy (`minLoadedMainLiftRestSeconds`), set only on conditioning, rather than a global rule. Gated both ways: the floor holds for conditioning, and no other goal has one.

- [x] ~~THE FLOOR HAD TO BE ASSERTED IN THREE PLACES~~ **FOUR — see the rest-floor entry at the top of this file.** THE FLOOR HAD TO BE ASSERTED IN THREE PLACES, which is the transferable lesson. Rest is set independently at prescription (`assignSetsRepsFromConfig`), at the per-week phase adjustment (`adjustRest` — a metabolic block's negative `rest_adjust_seconds` took a squat from 75s to 55s), and at the duration trimmer (`trimWeekRestForBudget`, which carried its **own hardcoded `isMain ? 60 : 30`** and dragged the 90s straight back to 60). Fixing only the first left 288 of 432 still under the floor; fixing the first two left 285. Each path sets rest without consulting the others, so a constraint on rest has to be re-stated at each — worth remembering before adding any other rest rule.

- [x] Gated the half of the ruling that is easiest to lose: conditioning's **bodyweight** main lifts keep their short rest (106 of 108 still under 90s). Had the floor been keyed on TIER rather than on "is there a bar", a conditioning trainee's chin-up day would have slowed down too and the goal would have quietly become hypertrophy. `isExternallyLoaded` is the discriminator, matching where the floor is set at prescription time.

- [x] THE COACH PROMISED SOMETHING IT COULD NOT DO — fixed. Plan: `docs/PLAN-swap-a-session-for-what-you-actually-did.md`. Ashley told the coach in advance she was skipping her weights day for Muay Thai; it replied *"I'll make sure today is marked as a rest day for lifting so we stay on track"* and did nothing. It could not have: of 22 tools, **none touched a day's status**. Worse than a no-op — `classifyDay` ends `dateStr < todayStr ? 'missed' : 'due'` with nothing between, so the day she announced IN ADVANCE would show as **missed** the next morning and the Muay Thai was recorded nowhere. Same shape as the pre-plan-days bug: the reward for engaging with the app is being told you failed. **The lesson was already written down and ignored** — `update_workout_schedule` is disabled with this in its own description: *"It used to write to a profile field the app doesn't actually render from, so schedule 'changes' looked applied in chat but never showed up on the Exercise tab."* Ashley's ruling: swap the day AND record what replaced it, over merely not counting it against her (leaves real training invisible) and over merely muzzling the coach (leaves her to do it by hand). Built: additive migration adding `swapped_for_activity` to `workout_sessions` (it is already one row per profile per date, so the fact belongs there, not in a third table); new `'swapped'` day state; new `swap_session_for_activity` tool writing to `workout_sessions` AND `cardio_logs` — the latter meaning the activity counts toward the streak for free, since `streak.ts` already reads it. New gates `test:coach-promises` and six cases added to `test:training-week`.

- [x] THE GENERAL GUARD, which is the more valuable half: `test:coach-promises` asserts **every declared tool has an executor branch**. A tool the model can see but nothing implements is a promise with no delivery — it gets called, falls through, and the reply describes an action that never happened. Checked at the time: all 22 are wired, so this was the only instance. It also asserts the new tool writes ONLY where the app reads (`workout_sessions` for the week strip, `cardio_logs` for the streak) and **never** to `fitness_profiles` — the field `update_workout_schedule` died on — and that a failed write produces a failure sentence rather than a success one, which is the original defect one layer down. Prompt gains NEVER CLAIM AN ACTION YOU DID NOT TAKE: don't speak in past tense about a change until the tool has run; when there is no tool, say so and point at the Exercise tab.

- [x] Ranking decided deliberately: `'swapped'` sits BELOW the logged-work checks and above the date judgement. Someone who announced a swap and then trained anyway has earned the tick — the hook's own comment says logged work outranks every date judgement, and *"anything else would erase real work to make a tidier calendar"*. Asserted both ways, plus the over-fire check that an ordinary unlogged past day is still `missed`.

- [ ] FLAGGED, one judgement call — a swapped day **drops out of the weekly tally entirely** rather than counting as done. Reasoning: the tally counts sessions OF THE LIFTING PLAN, so counting a deliberately-skipped day as done would inflate lifting adherence, and counting it as missed is the bug being fixed; dropping it mirrors `before_plan`. The alternative — count it as done, since Muay Thai is real training — is defensible and is one entry in one predicate if Ashley prefers it. What she actually did is not lost either way: it is in `cardio_logs` and the streak reads that.

- [x] **RESOLVED 27 Aug 2026 — both commands run.** `npm run db:push-both` reported *"Remote database is up to date"* on BOTH projects, so the `swapped_for_activity` column had already been applied at some earlier point and this item had been carrying a stale claim; and `chat-gemini` is now deployed to production. Verified from the command output, not inferred. Original text follows.
- [x] ~~**INERT UNTIL TWO COMMANDS RUN ON ASHLEY'S MACHINE** (both 403 from the sandbox): `npm run db:push-both` for the `swapped_for_activity` column, and `npx supabase functions deploy chat-gemini` for the tool. Until the migration runs the column does not exist and the write fails; until the function deploys the coach still has no tool. The frontend half ships with the Vercel push and is harmless alone — a state that simply never occurs.
~~
- [x] SESSIONS THAT FINISHED BEFORE THE TIME WAS UP — fixed. `applyDurationFiller` triggered on a flat 15-minute gap below the MIDPOINT budget, and that number is right for exactly one tier by coincidence: 75 − 15 lands precisely on the "60-90" tier's 60-minute minimum, so it looked correct. Every other tier had a hole beneath its own minimum — "45-60" (52min budget) topped nothing up between 37 and 45 minutes, so a session could ship shorter than the range the trainee actually chose and nothing noticed. Filler now triggers on the stated MINIMUM as well as the flat gap, via new `getSessionMinimumSeconds`. MEASURED on loading weeks, "45-60": sessions below the 45-minute minimum **77/576 (13%) → 19/576 (3%)**, mean fill 93% → 95%, worst case 69% → 80% of budget. Other tiers unchanged at 1%. `test:audit` still 0/13,967. New gate `npm run test:session-length`.

- [x] **A CORRECTION I HAD TO MAKE TO ASHLEY MID-TASK, and the reason this entry leads with "loading weeks".** I first reported this as "one session in four when you set aside an hour or more, worst case 31 minutes of a 75-minute budget" and recommended it on that basis. That was inflated: I measured every week against the midpoint budget without separating **deload weeks**, which are half volume BY DESIGN and deliberately never filled. On loading weeks alone the real figures were 13% of one tier ("45-60") and 1% of the other two, worst case 69% not 42%. The worst-case example I had eyeballed from the quality report was almost certainly a deload too. The code comment beside the filler says deloads are meant to run short — I should have read it before quoting a number. Ashley was given the corrected figures and chose to proceed anyway. `test:session-length` now asserts on loading weeks only and separately asserts that **no deload is ever topped up**, so the same mistake can't be made silently again.

- [ ] FLAGGED — the residual 3% are all within ~3 minutes of the minimum, and the cause is ordering: `enforceWeeklyPatternBalance` trims sets AFTER `applyDurationFiller` has already sized itself, so a day that was long enough at filler time lands a couple of minutes under afterwards. Not chased: that pass is documented as needing to run last, after rotation, periodization and duration trimming have all had their say, and the gain is minutes.

- [ ] FOUND, NOT CAUSED — 69 "30-45" sessions run past 105% of their midpoint budget (worst 121%). Verified identical at HEAD. Not a defect: 121% of a 37-minute midpoint is 44 minutes, comfortably inside what a "30-45" trainee asked for. Recorded because the first version of `test:session-length` asserted against the midpoint and flagged all 69 — measuring against a number the trainee never saw. The gate now checks the stated maximum instead, and reads 0 across every tier.

- [x] THE WEEKS THAT REPEAT THEMSELVES — **built, on the sixth attempt.** Frozen week-to-week transitions **518 → 264 (9.0% → 4.6%)**; the bucket Ashley's ruling targets, loaded non-carry, **380 → 126 (−67%)**; tier1 main lifts 44 → 14, tier3 isolation 77 → 6. `test:audit` still **0/13,967** and `test:assumed-body` passes — the two gates that decided it. Plan + all six attempts: `docs/PLAN-the-weeks-that-repeat-themselves.md`. THE FIX IS AN ORDERING: **observe first, then pin.** The load is resolved once with BASE reps and no pin, which makes it an honest probe of the underlying prescription — *would this weight have moved on its own?* Only if the answer is no do reps go up (one more than last week's accumulated total) with the weight pinned to the lift's own previous natural figure so the extra rep can't drag it down. The probe must use base reps, never bumped ones: extra reps lower a standards estimate, so probing with them makes a lift look like it dropped rather than froze. Verified by reading a real 16-week plan, not just counters — squats run 52.5 → 55 → 55 with reps 4-6 → 5-7, then 50 → 52.5 → 52.5 with 6-8 → 7-9. New gate `npm run test:frozen-weeks`, measurement `npm run report:frozen-weeks`.

- [x] FOUR CORRECTIONS THE SIXTH ATTEMPT NEEDED, each caught by a gate rather than by reasoning: (1) **the pin must respect the divergence backstop** — the second `prescribeLoad` call passes `forceStartingWeightKg` directly and bypasses the 1.25x clamp every other forced weight goes through, so a rotated-in Walking Lunges held at 8kg against a 6kg fresh estimate (133%) tripped `rotation_relative_load`; when the pin would breach the band the right answer is to **decline the rep**, because buying a rep must never cost weight. (2) **One decision per lift per week** — the streak is name-keyed but was applied per slot, so a lift on two days incremented twice and the second slot came out a rep above the first: same exercise, same weight, "4-6" Monday and "5-7" Thursday. Lift-weeks showing two rep ranges at one weight went 19 → 108; now back to **19, exactly the HEAD baseline**, so this adds none. (3) **Deloads are not recorded as the comparison basis** — a deload is meant to differ, so using it would make every first week of a block look like it moved and no freeze could be detected across a block boundary; week 9 compares against week 7. (4) **Both sides of the comparison are pre-coherence** — mixing bases is what broke attempt 2. Measured to justify it: `enforceLoadCoherence` leaves only 4 of 2,406 loaded exercise-weeks above a clamp, so the bases barely differ, but consistency is the point, not closeness. Also corrected a wrong instruction in my own earlier write-up: "compare post-coherence on both sides" is unachievable, since a probe is necessarily pre-coherence for the current week.

- [ ] FLAGGED — **carries are 100 of the 254 remaining freezes and are left alone on purpose.** `shiftReps` passes '40m' straight through, so a rep bump there is silently inert; a carry's lever is distance, and whether carrying further should count as progress is a product question Ashley has not been asked. The gate asserts carries are STILL frozen, so the exclusion can't quietly rot into an accident. Bodyweight is another 38 — already reps-led, so something downstream flattens them; uninvestigated.

- [ ] FOUND, NOT CAUSED, NOT FIXED — **ten transitions raise reps while the weight drops**: "Lateral Raises wk9→10: 12-17@6 → 13-18@4". A reps-led lift crossing into a new block re-derives a lighter estimate while its reps climb, which is more reps at less weight — a deload wearing progress's clothes. Verified pre-existing by running the new gate against HEAD's engine: the **identical 10 cases**. `test:frozen-weeks` pins the count at 10 so it can't grow while someone decides what to do about it.

- [ ] THE WEEKS THAT REPEAT THEMSELVES — **diagnosed and measured, NOT BUILT.** Full write-up incl. five failed attempts: `docs/PLAN-the-weeks-that-repeat-themselves.md`. Ashley picked this off a measured shortlist and ruled: when the weight can't move, ask for an extra rep instead (6-8 → 7-9), over forcing the weight to the next plate (~5% jumps where the design wants 3.5%, compounding over 16 weeks, in the direction that costs an injury) and over leaving the numbers and just explaining the week. **That ruling stands; it is not what failed.** SCALE, measured — the quality report's 84.1% is NOT the useful number (it counts "at least one instance anywhere in a 16-week plan"); the real rate is **9.0% of week-to-week transitions** identical in load AND reps, worst in tier2 (14.3%). By cause: **73% loaded non-carry** (Ashley's ruling), 19% carries (`shiftReps` passes '40m' straight through — their lever is distance, and whether distance should progress is an unasked product question), 7% bodyweight (already reps-led, so something downstream flattens it — uninvestigated). MECHANISM, traced and NOT what I first assumed: not rounding (`UNVERIFIED_RAMP_STEP_KG` is 5kg vs a 2.5kg plate step) but `load-prescription.ts:1264` — the unverified ramp is a **ceiling approach**, `Math.min(previous + step, estimate)`, deliberately, because prescribing past the standards estimate for someone we've never seen lift would be inventing strength data. Once it converges on that ceiling the load pins there for good, and reps are held flat *because load was supposed to be the lever*. New gate-free measurement `npm run report:frozen-weeks`, split by cause so a fix that drives the total down while leaving a bucket untouched can't read as success.

- [ ] WHY IT WASN'T BUILT, and what the next attempt needs. Best result reached was **518 → 359 frozen transitions (9.0% → 6.2%)**, audit still 0/13,967 — and it **broke `test:assumed-body`**, the item-2b safety invariant that a trainee who declined their body metrics is never prescribed more than one who gave them. A stated 55kg/52/female profile's Romanian Deadlift collapsed to **6kg, below an empty bar**, ending up lighter than the same lift for the declined profile at 16kg — a 2.7x gap between near-identical bodies, so structural, not physiological. Root cause is a flaw in my own design: **the pin makes the freeze self-perpetuating** — pinning this week's load to last week's means this week equals last week, so the lift is still "stuck", so it pins again, and the weight can never escape even once the estimate would afford a real step. The pin is not optional either: reps feed `prescribeLoad` as `repRangeLabel`, so bumping reps WITHOUT a pin lets the weight fall, and more reps at less weight is a deload wearing progress's clothes — invisible, because the frozen-week counter falls either way. Both pinned and unpinned versions fail `test:assumed-body`, for opposite reasons. THE SIXTH ATTEMPT should compute the load first with unbumped reps and only bump-and-re-derive when it actually resolves equal to last week's post-coherence load — the one ordering where the pin cannot manufacture a freeze. Four traps now named and avoidable: compare post-`enforceLoadCoherence` on BOTH sides (comparing pre- against post- made it *worse*, 380 → 272); pin to the NAME-keyed previous load, never `Math.min(name, slot)` (the slot value belongs to last week's variation — pinning to it held a rotated-in lift at the outgoing variation's weight and produced 9 `rotation_relative_load` failures); don't age a lift's streak once per slot (an accessory on two days aged twice, hit the cap in half the time and froze outright — tier3 went 77 → 191); and `shiftReps` has a floor but NO ceiling, so any accumulating bump needs its own cap or a strength block's 4-6 quietly becomes an endurance block.

- [x] THE QUALITY SCORE HAD TO BE TOLD THAT REHAB IS A LEGITIMATE SECOND WARM-UP. **METRIC CHANGE — say it out loud: Structure readings for INJURED profiles before this are NOT comparable with readings after it.** Prescribing rehab in every session cost **0.11 overall (11.05 → 10.94)**, and Structure was the whole delta (**1.95 → 1.81**); every other dimension was flat or up. Attributed by stashing the work and re-running the sweep, not by guessing. One rule did it: `primer_not_first` flagged any primer past position 0, encoding "a session has exactly one warm-up" — true until rehab existed, wrong for injured trainees after, and at 0.4 per distinct rule type roughly a third of combinations newly tripped it. **Ashley's ruling: keep both warm-ups and fix the check.** The rejected alternative (rehab REPLACES the day's primer) would have restored 11.05 with nothing measured differently, at the cost of a shoulder-injured trainee having no leg preparation before squatting on leg day — a metric problem traded for a training one. The rule now accepts a non-first primer only when every exercise before it is also a primer AND it is indicated for a joint **this trainee actually reported**; deliberately NOT "any primer carrying `indicated_joints`", which would pass rehab to someone who never reported the injury — the same tag-answering-the-wrong-question shape as the five defects before it. Teeth asserted in `test:rehab-prescribed` §7: a non-rehab second warm-up still flagged, a rehab warm-up after the main lift still flagged, an uninjured profile with the identical day still flagged. The 1.81 was never plans getting worse and the recovery is not plans getting better; uninjured profiles are unaffected in both directions, so their figures stay comparable.

- [ ] FLAGGED by the new frequency table, pre-existing, NOT from this round — `selection/duplicate_movement_family` fires on **40 of 9,216** combinations (0.4%). Checked rather than assumed: generated the same sweep against the pre-rehab engine (`f7939ef^`) and got the identical count — **2 repeated-family days per 192 for a shoulder-injured trainee, 0 for uninjured and 0 for knees, before AND after**. The repeated family is `hip_hinge`, nothing to do with rehab: a shoulder injury thins the upper-body pool, so a bodyweight full-body session reaches for a second hinge. Small and long-standing; noted because the frequency table made it visible for the first time.

- [x] `run-quality-score.ts` NOW PRINTS A PER-RULE FREQUENCY TABLE across all 9,216 swept combinations, not just the ten worst-scoring plans it samples. Added because attributing that 0.11 drop cost stashing the work and running two 14-minute sweeps to discover a single rule was responsible — the worst-10 section answers "what does a bad plan look like", which is a different question from "what changed", and the second one gets asked far more. Counted per combination rather than per occurrence, to match how `scoreFromViolatedRules` actually penalises. Additive output; no scoring change.

- [x] REHAB IS ACTUALLY PRESCRIBED NOW, not merely permitted. Plan + what actually happened: `docs/PLAN-actually-prescribe-the-rehab.md`. `exercise-db.ts` has always documented `indicated_joints` as "the prep/rehab work a physio would prescribe FOR that joint... **the plan should deliberately include them** when the matching injury is present." Nothing implemented that sentence: `isIndicatedFor` was read in exactly two places, one to keep rehab ELIGIBLE and one to label a movement "Chosen to help your shoulder" — a sentence that was false, because nothing had chosen it. **MEASURED FIRST, and the number was not what I told Ashley:** I said the app "does nothing at all to help them", which described the mechanism right and the outcome wrong. Rehab already arrived on **51.0%** of a shoulder-injured trainee's days and **50.7%** of a knee-injured trainee's, by luck of the shuffle. The defect was never "no rehab" — it was rehab as a coin toss, plus **40 of 144 knee plans containing none at all across sixteen weeks**. AFTER: **100% / 100%**, uninjured control **0% before and after**, knee plans with none **40 → 0**. New `pickRehabMovement` picks the gentlest indicated movement — warm-up tier first, then isolation by ascending duration, **never a tier2 compound** — which is Ashley's ruling ("every session, gentlest option") made mechanical: it excludes Wall Sit and Spanish Squat from a guaranteed slot while leaving them available through ordinary ones. "Gentlest" had to be tier-then-duration because every knee-indicated entry is `joint_stress: 'low'`, so the obvious field cannot discriminate and using it would only look principled. Protected via the EXISTING `requiredNames`/`protectedNames` mechanism rather than a second one invented beside it — "in every session" and "first thing dropped when time runs short" cannot both be true. Deliberately bypasses `primer_pattern_affinity`: a shoulder needs its band pull-aparts on leg day too. New gate `npm run test:rehab-prescribed`, new measurement `npm run report:rehab-coverage`. `test:audit` **still 0/13,967** — the duration risk the plan flagged did not materialise.

- [x] A STYLE PREFERENCE WAS DELETING A SAFETY RESPONSE — found because the guarantee stalled at exactly 75% for knees. `stageStyleFilter` stripped every knee-rehab drill from anyone who picked **'bodybuilding'**: full gym left one survivor (Lying Leg Curl), home gym / minimalist / bodyweight left **zero**, because a seated short-arc quad set is tagged functional/hybrid and nobody tags a rehab drill 'bodybuilding'. `MIN_VIABLE_POOL` never fired — the pool stayed large, just missing the one category that mattered. That function's own comment already drew the line it was crossing ("style is a preference, not a safety constraint — unlike equipment or injury"). Indicated movements now survive the style filter; the exemption is gated to rehab only and asserted to be, so no other off-style movement rides in.

- [x] A REGRESSION I SHIPPED FOUR COMMITS EARLIER, found by running a gate I had not run that round. `test:injury-rebuild` was ALREADY failing before this work — verified by stashing, then bisected to **`6646854` "Shrugs are not shoulder work"**, my own commit, which I reported green because the gate list I ran did not include this one. It has been red on `main` since this morning's merge. Mechanism is that plan's own stated risk: splitting `isolation_trap` out of `isolation_shoulder` left traps holding exactly two movements, both shrugs, **both contraindicated for a NECK injury** — so a neck-injured trainee's 32 shrug slots had no same-pattern replacement, were dropped outright, and that reads downstream as "a whole pattern was wiped", forcing a full rebuild over a genuinely thinning injury. Before the split they fell back to lateral raises, which a neck injury allows. I verified the split did not orphan shrugs for an UNINJURED trainee and never checked an injured one; the plan named the invariant and my verification covered half of it. Underlying bug is general: `getSmartReplacements` demanded an exact `movement_pattern` match **with no fallback at all**, while plan-build has had `NEAREST_PATTERN_FALLBACK` for exactly this since before traps existed — any thin pattern could hit the same wall. Map re-homed to `exercise-db.ts` beside `MovementPattern`, both readers importing it, swap path now falls back instead of dropping. Over-fire check holds: a shoulder injury, which genuinely wipes `vertical_pull`, still forces a rebuild.

- [x] TWO WARM-UPS DOING THE SAME JOB — caught by reading a generated plan, not by a gate. A session opened "Scapular Push-Ups, Scapular Push-Ups": seven of nine shoulder-indicated movements are primers, so the ordinary primer pick and the rehab pick drew from the same small set, and `usedGroups` stopped `main` colliding with rehab while saying nothing about the primer. Where the day's primer is already indicated for the injured joint, no second rehab movement is added — the guarantee is that every session carries rehab, not that every session carries a rehab SLOT. Gate extended: no exercise appears twice in a session, across 384 injured days.

- [ ] FLAGGED — **five of the eight injury codes get nothing.** `getFlaggedJoints` maps only five to a joint at all (hips/ankles/elbows are collected at onboarding and map to nothing), and of those five only shoulder and knee have any `indicated_joints` entries. Someone reporting a hip, ankle, elbow, wrist or back problem sees no change from the work above. Whether the app should SAY so rather than stay quiet is a user-facing question for Ashley, not a code decision.

- [x] SHIPPED TO `main` — 24 commits, 22-24 Aug, merged fast-forward. `origin/main` had not moved since **17 Aug** (`66aeb26`), so everything from the coach-voice round onward was finished, gate-verified and invisible to users: the shrug placement fix, the one-arm press fix, honest loads without a body, the Home-tab spinner, the matching workout-day layouts, and the four onboarding phases. Ashley's ruling when asked: ship the fixes now rather than hold 23 unrelated ones behind a database command. Re-proved `tsc -b` and `npm run build` clean on the exact commit being shipped rather than trusting the note from when it landed.

- [ ] STILL OWED, NEITHER RUNNABLE FROM THE SANDBOX (`*.supabase.co` and `vercel.com` both return `CONNECT tunnel failed, response 403` — network policy, not credentials; re-verified 24 Aug):
  - **`npm run db:push-both`** for `20260824090000_create_weight_basis_offers.sql`. Until it runs, the weigh-in rebuild offer is live but **inert, and silently so**: `weight-basis-offer.ts` discards the read error (`:249-257`) and the insert error (`:341`), so a missing table means the offer simply never appears — nothing crashes and nothing complains. That is the right failure mode for shipping ahead of the migration and the wrong one for noticing it never landed. Checked before shipping, not assumed.
  - **`npx supabase functions deploy onboarding-chat`** for the coach-voice work (`1aa451e`, `5f2eb90` and the onboarding commits). Gate-verified against a scripted fake model, **never once measured against the real one** — the tone probe is still outstanding and must not be reported as done until it exists.
  - **Possibly `vercel --prod`.** PROJECT-LOG §7.4 records that this project's shipping practice has been `git push origin main` **and** `vercel --prod` run together at the end of nearly every round. Whether the git integration alone deploys is not verifiable from here. Assume the push may not be enough until the live site is seen to have moved.

- [x] SHRUGS ARE NOT SHOULDER WORK — fixed, audit still 0/13,967. Plan: `docs/PLAN-shrugs-are-not-shoulder-work.md`. Ashley ran a real generated session past Gemini, which flagged **Dumbbell Shrugs on a "Push & Press" day**. Right, and the cause was a tag: both shrug entries carried `movement_pattern: 'isolation_shoulder'`, a pattern whose four members were two lateral raises (deltoid) and two shrugs (trap), and `'Push & Press'` has an `isolation_shoulder` accessory slot. NOT one unlucky roll — swept over 288 training days, shrugs appeared on 59 and **31 of those were a pressing day**. The codebase already knew the tag was wrong: `categorize()` has carried a name-matched `shrug` special case, with a comment explaining it ("shrugs track deadlift; lateral raises track bench"), for as long as both shared the pattern — that workaround fixed the WEIGHT and never the PLACEMENT. **Third defect of this exact shape in as many rounds** (loads_joints for injuries, vertical_push for the landmine, now isolation_shoulder for placement): a tag answering one question used to answer another. Fix: new `isolation_trap` pattern, both entries retagged, `tier3_isolation` slots on `'Pull & Hinge'` and `'Upper Pull & Core'`, the three pattern-keyed maps updated deliberately (all `Partial<Record<>>`, so the compiler forced nothing), and `categorize()`'s switch taught the value. MEASURED: pressing-day shrugs **31 → 0**; still reachable on 54 of 288 days across two tracks (a fix that orphaned them would be worse than the bug); `categorize()` still `shrug` and the load **byte-identical at 28kg/hand** — this was placement, not load. New gate `npm run test:pattern-tags`, named for the class not the instance. `test:quality` over 9,216 combinations: **Selection 1.94 → 1.95**, every other dimension unchanged — small, but Selection is exactly the dimension a placement fix should move, and it was the only one that did.

- [x] TWO THINGS THE NEW GATE AND THE COMPILER CAUGHT, fixed with it: (1) a tier3 trap movement whose NAME lacks "shrug" resolved to **`isolation_chest`** — trap work priced as a cable fly. `categorize()` checks isolation tier first and that block's own switch has a chest `default`, so the fallback case at the bottom of the cascade is unreachable for isolation-tier entries; both switches now know the pattern. Nothing moved today (the two shrugs are name-matched) but the next trap movement added would have been mispriced silently. (2) `patternLabel` lost exhaustiveness — it is the one place a pattern name reaches a trainee (`pattern_gap_note`), so a missing case would have printed "undefined" into user-facing copy.

- [ ] FLAGGED — `'Back & Biceps'` is the textbook home for shrugs and deliberately does NOT get the trap slot. Swapping its duplicate `isolation_bicep` slot for one put that track's 30-45min sessions at **43min against a 37min budget** — 4 `duration` failures in an audit otherwise at zero, verified as caused (stashing the change returned it to 0), not merely revealed. That track is at its time ceiling at the short tier; shrugs have two other homes carrying all 54 placements. Revisit if the budget ever loosens.

- [ ] FROM THE SAME REVIEW, NOT DONE — a full-gym user was given **Band Shoulder Press** for a tier2 compound slot. Gemini's reason (bands lose tension at the bottom) is a fair training point but not the one that bites: a resistance band is not in `LOADED_EQUIPMENT`, so the app shows **no weight at all** and the lift can only progress by reps. Nothing ranks candidates by implement, so with dumbbells and a barbell available it is a coin flip. Changes candidate ranking rather than one tag, so it wants its own measurement pass. (Third point of that review — session length — is NOT a defect: the app reserves warm-up time before allocating exercises, `budgetSeconds = totalBudgetSeconds − warmupReserve`, and `estimateDaySeconds` counts the warm-up. Noted in passing: `getWarmupReserveSeconds`' comment says 15%/5min/12min while the code is 20%/6.5min/14min — doc drift only.)

- [x] A ONE-ARM PRESS SCALED OFF THE BENCH PRESS — fixed, audit still at 0/13,967. Plan: `docs/PLAN-a-one-arm-press-off-the-bench.md`. `Landmine Press` was prescribed **132.5kg** to a 120kg advanced male whose two-arm barbell overhead press is 80kg — a ONE-ARMED press at **1.66x the two-arm press**. Never the exercise: `categorize()` picks a standard by matching words in the NAME and then falls back on `movement_pattern`, and the fallback collapsed `horizontal_push` and `vertical_push` into one `return 'bench'`. Right for horizontal push; wrong for vertical, which has its own materially lighter standard (0.65 vs 1.0 x bodyweight at intermediate). Only ONE exercise was reaching it, because the other three vertical pushes are caught by the name rules above — correct **by their names, not by construction**, so any future overhead movement missing that substring list fell through the same trapdoor. Nothing caught it either: `SAFETY_CEILING_KG` is keyed by category, so sitting in `bench` gave it a 220kg ceiling instead of overhead's 140kg. Fix is one line (split the case); the landmine now resolves to `overhead` directly with no new scale entry and inherits the tighter ceiling. **132.5 → 80kg** / 75 → 47.5 / 45 → 30. DELIBERATELY NOT halved despite `unilateral: true`: one arm handles ~half what two do, but the floor pivot carries part of the load so the bar-end number reads higher than the hand feels — modelling each separately means inventing a lever coefficient, so the overhead standard asserts only that they cancel. `test:per-side-load` extended with the invariant that needs no coefficient — **a one-arm press never exceeds the same trainee's two-arm press** — plus the trapdoor itself (every `vertical_push` must resolve to `overhead`), the over-fire check (`horizontal_push` still `bench`), and byte-identical numbers for the three lifts that were already right.

- [ ] FLAGGED — the landmine now sits **level with** the two-arm press, not below it: the gate's closest case is 87.5kg vs 87.5kg, holding with zero margin. Least certain number in that change. Conservative alternative is `{ parent: 'overhead', scale: 0.8 }` → 64 / 38 / 24 / 20kg. Revisit if a real trainee finds it heavy; the file's doctrine is that too light costs a boring set and too heavy costs a shoulder.

- [x] THE CONSTRAINT AUDIT IS AT ZERO — 54 failures → **0**, all 13,967 combinations passing. Two defects, one found by fixing the other. Plan: `/root/.claude/plans/help-me-plan-how-declarative-parrot.md`.

- [x] (1) ONE ARM, BOTH ARMS' WEIGHT. `prescribeLoad` decided per-side vs total with `isDumbbell || (mode === 'single_implement' && entry.unilateral)`, written out by hand at three call sites. A cable lateral raise is `unilateral: true` but its equipment lands in `loadingMode()`'s `stack` bucket, so one arm was handed the number derived for two: **37.5kg for a 120kg advanced male against a 25kg category ceiling**, 51 of the 54 remaining audit failures, all one exercise. Same defect already fixed once for one-arm kettlebell carries; the fix never covered stacks. Now one named `isPerSideLoad` covering all three shapes — dumbbell pair, unilateral single implement, unilateral weight stack — keyed on real machine equipment (`'cable machine' | 'machine' | 'leg press machine' | 'hack squat machine'`) and NOT on the `stack` bucket, which is a fallback holding 17 bodyweight/band unilateral entries. `labelModeForEntry` and `estimateEffectiveTotalKg` both route through it: the first so the caption can't say "total" over a halved number, the second so the newly-halved lift isn't read as half its real demand by the rotation guard (that would have traded one audit failure for another). Measured after: heaviest cable lateral raise in the whole sweep **17.5kg**, `load_cap` 51 → **0**.

- [x] (2) A BLOCK BASELINE SURVIVING A ROTATION — found because (1) made it fire more (rotation_relative_load 3 → 9 before this). `blockBaselineKg`/`blockWeek3Kg` are keyed by SLOT POSITION, but an accessory's variation rotates on a two-week sub-cycle WITHIN the block, so from week 3 the slot builds on a stranger's week-1 number. The file already states the rule ("a capped or broken anchor must never propagate through a rotation") and implemented it as a 25% divergence backstop — porous in both directions: the comparison is exclusive so exactly 125% passes, and plate rounding then rounds that value UP after the check. TRACED LIVE (temporary instrumentation, reverted): a 50kg advanced female carried a 5kg Cable Lateral Raises baseline into Lateral Raises, passed the backstop at exactly 5 against a 5.0 limit, and rounded to 6kg — 150% of a fresh estimate. Now name-keyed, the same treatment `lastUnverifiedLoadingWeekKg` needed one tracker over: a rotated slot has no baseline and falls through to a fresh estimate for the exercise actually in it. rotation_relative_load 9 → **0**.

- [x] New gate `npm run test:per-side-load`: the rule on all three shapes and its non-firing on four (bodyweight unilateral, band unilateral, bilateral cable, bilateral single implement); label and rotation-comparison agreement across EVERY database entry; the cable number under its ceiling and still a real prescription; and 286 real mid-block rotations checked for an inherited load. Seconds, versus the full audit's 75.

- [x] ~~FLAGGED, NOT FIXED — **Landmine Press is prescribed 145kg for ONE ARM** (120kg advanced male). Larger than the cable defect and nothing catches it: `categorize()` routes it to the `bench` family, which has no low-enough `SAFETY_CEILING_KG`. Halving would give 72.5kg, but a landmine is a loaded bar END, so "halve it" may be the wrong answer even though the movement is one-armed — and the `bench` scaling is itself suspect. `test:per-side-load` asserts the current deliberate state so a later change is a decision, not a drift. Needs its own plan.~~ — done, see the top entry.

- [x] WEIGH-INS NOW REACH THE LIFTING WEIGHTS — offer built, Ashley's "ask first" ruling. Plan: `/root/.claude/plans/help-me-plan-how-declarative-parrot.md`. Weighing yourself updated food targets and nothing else: `computeTargets` prefers the latest `daily_metrics` reading, the training side reads only the immutable signup `weight_kg`, and `generateMesocycle` runs once at onboarding — so the 16 weeks of loads written at signup were the loads forever. Item 2b made that cost MORE (a declining 100kg man went from 0.76x to 0.35x of his real loads), so closing it was the completion of that work, not an extra. A weigh-in (or a weight typed into Profile) now produces a durable offer — new `weight_basis_offers` table modelled on `load_suggestions`, NOT `pending_actions` (10-minute expiry; a one-shot that vanished would restore the very problem the ruling avoided) — naming the largest real change ("would take Trap Bar Deadlift from 47.5kg to 95kg"). Confirm rebuilds the live week onward, never a past week, through the injury rebuild's machinery (extracted as `rebuildAgainstProfile` so week-identity preservation isn't re-derived per caller). Decline is permanent. MEASURED (`run-assumed-body-report.ts`, new third column, via the real rebuild path): declined WEIGHT only **0.89x → 0.98x**; declined all three **0.35x → 0.57x** — NOT the ~1.00x the plan predicted, because a weigh-in supplies one of three metrics and sex is the larger term (female standards are 0.53-0.67x male). Supply sex later and the offer fires again, which is why no wording claims the weight was the missing piece. New gate `npm run test:weight-basis`. **MIGRATION NOT APPLIED — `npm run db:push-both` still owed; `*.supabase.co` is 403 from the sandbox.**

- [x] Three bugs the new gate caught, fixed with it: (1) "the plan holds `assumed_body` loads" looks like the eligibility rule and is NOT — someone who declined weight, age AND sex still has an unknown sex after a weight rebuild, so the flag stays true and the offer would re-ask on every app load; `rebuildChangesAnything` is the real test. (2) The headline change was "largest INCREASE", silently skipping the one case with a real safety cost — a trainee LIGHTER than the light stand-in, carrying numbers too heavy for them. Now largest absolute change, either direction. (3) Preview and confirm each called `generateMesocycle` with the default RNG, so they selected different exercises — the offer could name a lift the applied rebuild never contained. Surfaced as a flaky assertion that passed or failed on what `Math.random` returned; both runs are now seeded on profile id + basis weight.

- [x] BUG (prerequisite, found while browser-verifying the above): the Home tab spun on "Loading your day…" FOREVER for anyone who declined a body metric. `computeTargets` returns null for them and `Dashboard`'s load effect required `macros` while `loading` initialises true. It also made the weigh-in card unreachable (it lives inside `Dashboard`) — so the weight-basis offer's main trigger could not fire for the persona it was built for. **Correction to a previous session's claim:** I saw this same spinner during the 2b browser run and attributed it to the local mock returning empty rows. That was wrong — it reproduces with a real database and the mock was irrelevant. `loadDashboardData` now takes `MacroTargets | null` and reports `hasNutritionTargets`; the calorie tile renders "0 kcal · no target yet" with no ring rather than "of 0 kcal" (the placeholder `MissingBodyMetricsNotice`'s own doctrine forbids). Gated in `test:dashboard`. Also fixed in the same run: the weight-basis confirm handler posted "Done — the rest of your plan now uses your real weight" even when the rebuild returned null and nothing was written.

- [x] Item "2b" — HONEST LOADS WITHOUT A BODY, built. Plan: `docs/PLAN-honest-loads-without-a-body.md`. `resolveParentOneRepMaxKg`'s three `||`s (`weight_kg || 75`, `gender === 'female' ? … : 'male'`, `age || 30`) turned a refusal into a 30yo 75kg man for every prescribed kilo in the app. MEASURED FIRST (`npm run report:trim-magnitude`'s sibling, new `scripts/run-assumed-body-report.ts`, matched pairs, same seed): a 55kg 52yo woman who declined all three was prescribed a mean **2.01x** her own week-1 loads, **2.09x** by week 5, 18 of 18 shared lifts >15% too heavy, worst 3.00x — every one labelled "suggested". AFTER: **0.94x / 0.94x, zero lifts over-prescribed**. THE FIX THAT WORKED WAS NOT THE PLANNED ONE: damping-while-assumed was built first and measured to move the week-1 ratio not at all (2.01x → 2.01x — a stated profile's week 1 is *also* a calibration week, so the same multiplier hit both sides and cancelled). The fabrication is in the body, so the body changed: `ASSUMED_BODY` is now 50kg/female/60 — deliberately the light end of the adult range, because prescribing a light trainee 2x what she can lift is an injury and prescribing a heavy trainee half is one boring set he corrects by logging. No second multiplier on top (a conservative body AND a discount compounds to a 100kg man on 2kg dumbbells). Also: new `load_source: 'assumed_body'` (chip reads "starting light", not "suggested"; no migration — JSONB), `isUnverifiedLoadSource()` so adding the state couldn't silently break every `=== 'estimate'` comparison, calibration can no longer be skipped on an assumed body OR on a "yes I know my numbers" with zero numbers entered (that path measured **2.22x → 0.93x**, and now gets a calibration week at all), the basis string stops claiming it came "from strength standards for your bodyweight, sex and experience", `MissingBodyMetricsNotice` stops claiming "your training plan is unaffected" when weight/age/sex is what's missing, and Ashley's ruling 1 — one more ask at the review, with an inline "Add it now". Exercise selection was contaminated by the same substitution (`isolationTargetBelowFloor` handed a 50kg woman a 20kg barbell curl); fixed for free at the chokepoint. New gate `npm run test:assumed-body`. Verified live in the browser against the local mock: review line renders, plan opens on "Wk 1/16 · B1 Calibration", Deadlifts show 22.5kg under STARTING LIGHT.

- [x] BUG found by 2b's own gate, fixed with it: the unverified per-week ramp tracker (`lastUnverifiedLoadingWeekKg`) is keyed by SLOT POSITION, and variations rotate at block boundaries — so a lift returning to a slot it left weeks ago stepped up from whatever had been sitting there. Observed: Romanian Deadlifts 8kg (wk3) → absent a block → **16kg** (wk9), a doubling on an unverified lift inherited from a stranger. Breaks the invariant `UNVERIFIED_RAMP_STEP_KG` is written around, and is the same contamination `forceStartingWeightKg` already guards against. Now also keyed by lift name; the slot value stays as the fallback for a genuinely new variation, since discarding it would reintroduce the block-boundary snap the ramp exists to prevent. Measured effect on `test:quality` across 9216 combinations: overall 11.04 → 11.05 / 12, Progression 1.65 → 1.66 (fewer `frozen_week` findings), every other dimension unmoved.

- [x] PHASE 4 (final phase of the onboarding plan): a plan can't have been missed before it existed. `classifyDay` (useTrainingWeek.ts) ended `dateStr < todayStr ? 'missed' : 'due'` with no plan-start notion — observed live: a profile created MINUTES earlier, opened on a Saturday, showed Mon/Wed/Fri all ○ (missed) and "0 of 3 sessions done". DECISION (Ashley): pre-plan days are not part of the plan and don't count — new `before_plan` state renders a faint "·" (not a rest day the plan chose, not a missed one), and the week tally excludes it via the new exported `countsTowardWeekTally`, so onboarding Thursday with Mon/Wed/Fri gives "0 of 1". SCOPE CHECKED FIRST: the hook is UI-only (TodayPanel/WeekStrip/WeekContextRow; NOTHING in src/lib imports it), so accountability, streaks and block review — which read logged sessions — are untouched. This is display, not progression. No new plumbing needed: App.tsx already threads `planCreatedAt={mesocycleCreatedAt ?? profile.created_at}` to ExerciseTab, and mesocycleCreatedAt is set on restore (from mesocycle_weeks.created_at) AND on generation, so it survives reload and moves on regeneration; converted to a LOCAL date before comparing, since everything else in the hook is getLocalDateString space and a UTC instant would misjudge the plan's own first day around midnight. MISTAKE CAUGHT BY THE NEW GATE: I first put the pre-plan check BEFORE the completed/part-logged checks, which would have displayed a session someone actually logged on a pre-plan day as "not part of your plan" — erasing real work for a tidier calendar. Logged work now outranks every date judgement. New gate `test:training-week` exists precisely because the calendar can't reproduce this on demand (a plan created today only has pre-plan days if the run happens Tuesday or later) and asserts BOTH directions so the guard can't over-fire. Also fixed: WeekStrip's aria-label interpolated the raw state name (screen readers announced "partial", would have announced "before_plan") — now a spoken-English label map. The "0 of 0 sessions" case needed nothing: RestDayCard already guards on planned > 0. All client-side, no deploy.

- [x] PHASE 3 of the onboarding plan: honest progress, and a way back. (1) THE BAR WAS LYING — `requiredCount` counted required slots only while `readyToGenerate` also waits on `unconfirmedOptional`, so after Phase 2 it filled completely with ~7 ask-anyway questions still to come. Denominator is now every slot that can actually hold a plan up (applicable AND not never-blocking = exactly required + ask-anyway), and the gate asserts that identity against its own blocking count so it cannot drift back. Numerator now counts `confirmed` not `values`, which fixes a SECOND bug free: `handleToggleMulti` writes values on every chip tap BEFORE Done, so merely touching one training-day chip nudged the bar and dropped the slot from the missing list. Browser-measured: 0/17 at start → 18/18 exactly when the review opens (max moves 17→18 as knowsWorkingLifts becomes applicable — the requiredIf-aware denominator working). Bar also gained role=progressbar + aria-valuenow (was invisible to screen readers); kept WORDLESS per the existing comment recording why "12 of 18 answered" was removed. (2) THERE WAS NO WAY TO CHANGE AN ANSWER — review rows are now buttons that re-open that question's card. Deliberately WITHOUT clearing `confirmed`: clearing flips readyToGenerate false, so anyone who opened a row then changed their mind would face a disabled Generate with nothing saying why — a NEW dead end in the pass meant to remove them. Leaving it confirmed is safe because applySlot has no confirmed-guard on write and commitWorkingState resolves every card carrying that slot key. The model-side guard in executeActions is untouched, so the coach still can't re-ask an answered question; only the user can. Verified live: Fat Loss → Muscle Growth, Generate enabled before/during/after. SlotNumericCard needed an explicit `editing` mode (it hides confirmed fields, so a re-opened numeric would have rendered empty), and the flag persists on DraftMessage so a refresh mid-edit doesn't leave an empty card. (3) Generate greyed out silently when readyToGenerate flipped — now reachable via an edit (equipment → barbell tier makes working-lifts newly required) — it now names what's outstanding. (4) Composer was hard-disabled while busy, dropping the phone keyboard EVERY turn; now readOnly so focus and keyboard stay (the Enter handler already refused to send while busy), matching the softer treatment chips already used. Opening message now says once that any answer can be changed later. All client-side — no deploy needed.

- [x] The name is now genuinely optional, end to end (Ashley's follow-up to Phase 2). Two of the three properties were ALREADY true and are now verified + gate-locked rather than assumed: continuing without a name works (never-blocking + nullable column), and nothing addresses a nameless user by name — `greetName()` returns plain "Hey", and chat-gemini interpolates the whole "Their name is X, use it occasionally" clause ONLY when a name exists, so the model is never told to use one it hasn't got (browser-confirmed: "Hey — I'm your coach. Good to meet you."). A name offered later DURING onboarding was already capturable because buildSlotCatalog filters on applicability only — now gate-asserted, since demoting a slot without keeping it in the catalog would silently break exactly that. THE REAL GAP, now fixed: after onboarding there was NO way to capture a name at all — ProfileScreen had no name control (while APP_REALITY tells the coach the Profile screen has "editable identity fields", so it could have sent someone to a control that doesn't exist), and chat-gemini had no tool writing display_name (record_fact's kind enum is food/exercise/timing/hard_constraint). Added `set_display_name`, dispatched exactly like record_context_fact — verbatim-quote gated so a name can't be invented, forwarded not written (I1 holds), executed client-side via the existing updateProfileField with onProfileChanged keeping App state in step. DECISION (Ashley): save it and say it back, NO confirmation card — a name mutates nothing about the plan; the receipt is what keeps "no silent writes" true. Deliberately no undo token (correcting a name is saying another one). ProfileScreen gains a Name row — savePatch was already generic, but the input is new because the existing `EditableTextField` is misleadingly named and is actually a NUMBER field. NEEDS `npx supabase functions deploy chat-gemini` for the chat tool; the Profile row works without a deploy.

- [x] PHASE 2 of the onboarding plan: trimmed the ask set to what shapes the first plan (Ashley's ruling). Questions that can block a plan 23 → 18, MEASURED by the gate rather than asserted (17 fresh / 18 for a barbell lifter, was 22/23), and the same scripted persona through the real UI now reaches the same 16-week plan in 10 conversational turns instead of 13. REMOVED: the time-of-day question — onboarding-slots.ts already documented every answer producing a byte-identical plan and mesocycle, its only consumer is a chat greeting that already reads `preferred_time || 'morning'`, and it was lossy by design (5 answers → 2, with 3 of the 5 branches in the greeting map unreachable); preferred_time now written as that same default. DEMOTED to never-blocking (still asked when the conversation goes there, all editable later in Profile, just unable to hold the door shut): cookingTime (was required:true for ONE sentence of a meal prompt), favoriteCuisines, breakfastStyle, displayName (was required:true for a greeting). UNTOUCHED deliberately: goal/experience/activity/equipment/days/session length/style/recovery/conditioning/meals-per-day (each measured to move the output) plus injuries and dietaryPreferences (safety path, stay blocking per CLAUDE.md). DEVIATION from the approved plan: it also proposed folding includeSnacks into mealsPerDay "to save a question" — NOT done, because includeSnacks is already in NEVER_BLOCKING_SLOTS so it was never one of the 23, and folding it would conflate meal count with snacks for zero reduction. The plan's "~15" estimate leaned on that; 18 is the real, measured figure. Observation, not fixed: with displayName never-blocking the model has less pressure to capture a name — in practice the opening message asks for it first, so it costs no turns either way, but a user who opens with something else could finish nameless (the greeting falls back to no name).

- [x] PHASE 1 of the onboarding plan (docs — see the approved plan): the refusal trap, round two. Age/height/weight/sex were `required: false` but NOT in NEVER_BLOCKING_SLOTS, so `unconfirmedOptionalSlots` held them until `confirmed`, which only came from a value passing `validate` — `isNumberIn` rejects '', gender had no third option, neither card had a skip. A user who wouldn't give a weight could answer all 22 other questions and never reach Generate. Same trap the absence work fixed in the calculation layer, still sitting in the completion gate. Fixed with `canDeclineSlot` (exactly "not currently required" — goal/days/equipment and the injuries/diet safety path stay unskippable), `declineSlots` writing null EXPLICITLY (inferring from "confirmed but empty" was tried and is wrong: numerics initialise to ''), a "Prefer not to say" button on both cards, and a new `decline_slot` tool so a TYPED refusal works too — the client re-checks canDeclineSlot so the model's instruction is enforced, not trusted. Also fixed in the same pass: the insert-error message was UNREACHABLE (setupError set, then setProfile runs, and that screen lives inside `if (!profile)`) — now a dismissible banner, so a user whose profile didn't save is actually told; and SlotNumericCard's all-blank Save was a silent no-op directly under a comment promising it never is. Verified in-browser both ways (button + model tool): reaches a 16-week plan, four rows read "Not given", and NO daily_metrics weigh-in row is seeded — no fabricated weight. Gate `test:onboarding-slots` extended with what can/cannot be declined. NEEDS EDGE-FUNCTION DEPLOY (`npx supabase functions deploy onboarding-chat`) for the decline_slot tool to exist live; the button works without it.

- [x] Onboarding free-flow + nonsense handling. Measured defect (browser, real UI, 10 unusable answers in a row): the goal question appeared 3x with a duplicate chip grid each time and the same "Let's lock this one in" sentence every time. Three causes, all fixed. (1) The stuck-slot breaker had no already-live-card guard — `executeActions` has one (`alreadyLive`) precisely because duplicate cards were a bug there before, and the breaker was written without it, so every stalled turn pushed a fresh identical card. (2) It always forced `remaining[0]`, hammering one question; it now rotates past a slot already forced twice, prefers a tappable question over a typed one, and says so ("we'll park that one"). (3) Every app-authored fallback line was a fixed string; breaker leads and the resolver's floor opener now CYCLE (not clamp — clamping repeats the last variant forever, which was the first cut of this fix). STALL_TURN_LIMIT 6→4. Prompt side: explicit unusable-answer handling (record nothing, never reuse your last deflection, make it easier instead; two strikes then move on; "I don't know"/"you decide" get a recommendation to confirm, not the question again), and STILL UNKNOWN restated as explicitly NOT an order. Verified in-browser same script: goal question 3x→1x, identical repeated leads 4→0, four different questions offered instead of one; happy path unchanged at 13 turns to a 16-week plan, no console errors. NOTE: verified against a LOCAL MOCK of the model and DB (this session's egress blocks *.supabase.co), so the app-side behaviour is proven and the model-side prompt rules are NOT — they need the live probe alongside the still-pending warmth measurement.
- [x] DECISION (Ashley, asked 2026-08-23): a required question the user keeps dodging is never skipped or defaulted — the app keeps asking and will not build a plan without it. Options put: (a) keep asking, never build without it; (b) offer a sensible default they confirm after several dodges; (c) let them skip and mark the plan provisional. Chose (a): injuries and equipment genuinely change what's prescribed, and a shrugged-through "no injuries" would be indistinguishable from a real one in the safety filter. This is what the client already enforces (complete_onboarding is refused until every required slot validates and every ask-anyway slot was answered or skipped), so the ruling is a confirmation, not a change — the parking behaviour above defers a question, it never drops one.

- [ ] Coach warmth, take 2 — reply guarantee BUILT and gate-verified, prompt loosened, NOT deployed and NOT live-measured (this session's sandbox egress policy blocks *.supabase.co, so neither the tone probe nor a deploy could reach TEST). The earlier finding — "react every turn" was load-bearing for RELIABILITY, not voice — is now answered in code: onboarding-chat's recovery chain moved to reply-resolver.ts and closes the four holes that let silence through (an entirely-empty model turn skipped recovery; sanitize ran after the legs so leak-shaped replies shipped as ""; the text-only leg was allowed to fail; no deterministic floor). A 200 response can no longer carry empty reply text — proven by construction and by the mocked-model gate `test:reply-guarantee` (47 checks: every measured failure shape, ≤3 extra model calls per turn, 0 on turns that already spoke), NOT yet proven live. TO FINISH (any session with Supabase access): (1) probe the currently-deployed function for the baseline: `npx tsx scripts/probe-onboarding-tone.mts scripts/probe-personas/warmth-measure.json baseline.json` — expect empty-reply 0/7 and the graded-every-answer voice; (2) `npx supabase functions deploy onboarding-chat` at the guarantee commit (1aa451e) → re-probe, expect empty-reply 0/7 and voice unchanged; (3) deploy the prompt commit (5f2eb90) → re-probe, expect graded-answer turns ~0 and empty-reply STILL 0/7 (the guarantee earning its keep — this is the exact configuration that previously produced 4/7 empty). The probe now prints `empty-reply turns: N/M` itself, and counts transport errors separately so an unreachable function is never misread as model silence. Client dead-air guard kept as defense in depth.
- [ ] BUG, exposed (not caused) by the equipment-quality preference: rotating a cable isolation exercise to its dumbbell sibling at the week-3 sub-cycle boundary re-estimates the load ~50% high — "Cable Curls -> Dumbbell Curls at 6kg, 150% of a fresh 4kg estimate", same for Cable Lateral Raises -> Lateral Raises. Already failing 3× before the equipment change; now 5×, purely because more profiles correctly land on the cable version in week 1 and therefore reach the faulty rotation. The `rotation_relative_load` check's own 125% band is what catches it. No new defect class — the same shape, wider exposure. Worth fixing at the rotation load-estimate, not by widening the band.

- [x] Injury rebuild WIRED into the live path (was built-but-inert). Both cards call assessAdaptation at propose time and carry mode:'substitute'|'rebuild'; both executors branch on it. Rebuild card no longer claims "I can adjust 146 exercises" — it says the injury rules out too much to patch exercise by exercise and the programme is rebuilt around it, session count kept. Time-bounded path rebuilds only its own week window, so a temporary rebuild is exactly as temporary as the adaptation (pre_image restores it). test:injury-rebuild drives the real executor: 432 slots rebuilt vs 320 substituted, zero contraindicated either way.
- [x] Six shoulder-friendly variants added (Ashley-approved scope, no more): Barbell/Dumbbell Floor Press, Neutral-Grip Dumbbell Press, Landmine Press, Chest-Supported Row, Neutral-Grip Seated Cable Row. All LOAD the shoulder honestly but are not contraindicated by it; none marked indicated (tolerated, not rehab). Closes the content gap: shoulder-injury pool horizontal_push 0→3, vertical_push 0→1, and a shoulder rebuild now actually contains pressing. Uninjured plans verified byte-identical — no regression to the normal case. Gate test:joint-tags [6] locks it.
- [x] Audit CHECK 2 (injury) repointed at contraindication. It still read loads_joints, so after the tag split it flagged 23,768 "failures" that were the correct output — every rehab movement prescribed FOR a shoulder injury and every shoulder-friendly press. Now 0. Same conflation, one more place.
- [x] RESOLVED, verified 25 Aug — `pattern_coverage` is now **0** in `test:audit` (whole sweep 0 / 13,967). It was logged as a live defect and had already been fixed by the six shoulder-friendly variants: the generator gave up on a pattern that had only ONE eligible candidate, and horizontal_push for a shoulder-injured trainee went 1 → 3. Original entry kept below for the record. ~~NEW, newly-visible (not a regression): pattern_coverage 276 → 683.~~ Every added failure is "plan has zero <pattern> despite the pool having N eligible candidates" for INJURED profiles — previously N was 0 so the gap was honest and silent; now a press exists and the generator still doesn't select it. Dominant case: 432× "zero horizontal_push despite 1 candidate". The plans are no worse than before (uninjured plans unchanged, injured plans strictly better), but the generator's slot allocation gives up on a pattern when only one candidate exists. Real defect, not yet fixed.
- [ ] NOT DONE from the queue: knees/lower-back/wrists tag audit (knees still has ZERO indicated movements, so a knee rebuild contains no rehab — same defect shape as shoulders had); rest of BACKLOG below.

- [ ] **JOINT-LOAD TAG AUDIT (shoulder) — done, decision needed before the rebuild.** Ashley's challenge was right: the 146 is *partly* an over-broad tag. How the filter decides: `injuries:['shoulders']` → flagged joint `shoulder` → exclude every exercise whose `loads_joints` array contains it. 42 of 105 exercises carry that tag. THE STRUCTURAL PROBLEM: `loads_joints` answers "does this joint participate?", and it is being used to answer "is this dangerous for an injured joint?" Those are different questions, and conflating them is what makes the filter blunt. Breakdown of the 42: **~29 genuinely contraindicated** (all 11 horizontal_push, all 3 vertical_push, all 5 vertical_pull, dips, lateral raises, overhead carry/tricep work) — a cuff injury really does rule out pressing and overhead pulling, so the plan being gutted is mostly real, not an artefact. **~13 wrongly excluded**, and these are the damning ones: the 9 `activation` movements are Band Pull-Aparts, Wall Slides, Prone Y-T Raises, Band Dislocates, Band Face Pulls, Arm Circles, Scapular Push-Ups — *these are rotator-cuff rehab exercises, the things a physio actually prescribes FOR this injury* — plus Face Pulls and Rear Delt Flyes (standard shoulder-health work), and Hanging Leg Raises / Ab Wheel Rollout (core movements where the shoulder is a passive hang, not loaded through range). So the app currently responds to a shoulder injury by removing shoulder rehab. SECOND FINDING: there are **zero shoulder-friendly pressing variants in the DB at all** — no floor press, no neutral-grip press, no chest-supported row. That is a content gap, not a filter gap; even a perfect filter can't substitute what doesn't exist. Fixing the tag would take the exclusion from 42 → ~29 and, critically, would let a rebuilt plan CONTAIN the rehab work. It would not restore pressing. Ashley's chosen direction (rebuild the plan around the injury) still stands, but should be built on the corrected tag, not the current one.
- [ ] **DECISION NEEDED — injury adaptation removes instead of substituting (the "146 removals" / "drops 10 of 13 slots" bug).** ROOT CAUSE FOUND, not fixed (injury path = plan first). `getSmartReplacements` requires the replacement share the outgoing exercise's `movement_pattern`; the injury filter removes conflicting exercises from the candidate pool BEFORE that. When an injury wipes out an entire pattern, the same-pattern candidate list is empty **by construction**, so every slot in that pattern is dropped — 100% drop rate, not a ranking problem. Measured, full_gym pool 102 exercises: **shoulders wipes out horizontal_push (11→0), vertical_pull (5→0), vertical_push (3→0)** — i.e. all pressing and all vertical pulling, which is exactly the 146 slots. knees wipes knee_dominant/single_leg/isolation_quad/isolation_hamstring (13 exercises). lower_back, wrists, neck wipe nothing (they only thin patterns, which is why those injuries have never looked broken). So this fires hard for shoulders and knees only. What a correct adaptation should look like is a coaching decision — see the question put to Ashley (options: regenerate the program around the injury / allow cross-pattern substitutes / drop-and-rebalance volume / keep dropping but be honest in the card).
- [x] Slot-replacement data inheritance — CLASS fixed (3rd instance of this shape). Audited every path that changes which exercise occupies a slot. Load: recomputed correctly on ALL of them (swap, ban, injury adaptation, equipment adaptation, block/weekly rotation, floor-clamp substitution) — so "load inheritance" was the wrong diagnosis. What actually leaked was everything ELSE: swap/ban/injury/equipment all funnel through `applyReplacement`, which shallow-spread the outgoing slot, carrying over `prescription_type` + `reps` (units — "40m" left on a rep lift and vice versa), `ramp_up` (**the outgoing lift's per-set warm-up kg ladder, displayed under the incoming lift's name** — the actual wrong-exercise-load leak) and the assistance-chip fields. Fixed at the choke point; canonical non-reps prescriptions extracted to `fixedUnitPrescription()` so generator and edit path can't drift. New gate `test:slot-replacement` asserts the invariant (no outgoing-exercise data survives) rather than the three instances — it caught a real gap in the first cut of the fix. Rotation is NOT a leak: its carry-forward is bounded by the incoming exercise's own fresh estimate, plus a 25% divergence guard on the weeks-2/3 path.
- [x] SAFETY_CEILING_KG recalibrated across 50-120kg, both sexes, all experience tiers, all rep/RPE brackets (was: one 80kg male). 7 categories were breached by a legitimate 120kg-advanced-male estimate (carry, isolation bicep/tricep/chest/quad/hamstring, deadlift marginally) — each raised to ~25% above its highest legitimate observed value. 2 categories had NO ceiling at all (`overhead_carry`, `single_leg_dumbbell`) — a missing entry silently exempts a category from the check entirely; both now bounded. **isolation_shoulder deliberately left at 25kg and still failing**: a 40kg Cable Lateral Raise is not a real prescription at any bodyweight, so the 0.19-of-bench shoulder-isolation fraction is the bug, not the ceiling — raising it would silence a genuine defect. load_cap failures 706 → 55, and all 55 residual are that one category. Total audit failures 530 → 343.
- [ ] **Pre-ceiling loads persisted in the DB — count blocked, backfill proposed, NOT run.** The per-implement ceiling only landed 2026-08-12, so every plan generated before then has uncapped numbers already written to `mesocycle_weeks`; a code fix does not clean stored rows. Built `scripts/count-preceiling-loads.ts` (SELECT-only, validated on TEST: 4 profiles / 64 week-rows / 1040 loaded slots → 3 over-ceiling slots on 1 profile, worst 52kg Romanian Deadlifts vs a 50kg ceiling). **Cannot run against PRODUCTION**: prod credentials are not in the repo (`.env` = the old pre-split project `aoksyzjrrikvuhxatljy`, `.env.local` = TEST). To get the real number: `AUDIT_SUPABASE_URL=<prod> AUDIT_SUPABASE_KEY=<prod anon> npx tsx scripts/count-preceiling-loads.ts`. PROPOSED BACKFILL (do not run without a decision): for each affected slot, re-run `prescribeLoad` for that exercise/profile and write the clamped value back, preserving everything else — but note this silently changes numbers a user may already have trained against, so the honest alternative is to surface it in-app ("some loads in your plan were above what that equipment can hold; we've corrected them") rather than mutate quietly. Prefer the in-app route.

- [x] VERIFIED: live chat click-through for the injury-persistence build (Gemini key now set on TEST). Real conversation against the deployed chat-gemini function: reported a lasting rotator-cuff injury → model correctly asked one clarifying question ("is this ongoing... would you like the plan to just work around it") before proposing anything, second turn confirmed duration → `propose_injury_as_lasting` fired (`affected_area:"shoulders"`) → executed via the real client executor → `fitness_profiles.injuries` confirmed `["shoulders"]` in the DB. Said "my shoulder's back to normal now" → `propose_injury_recovered` fired on the first turn → executed → `injuries` confirmed back to `[]`. Both tool choices, both DB writes, verified live and by direct query, not just mechanically. One side observation, not a defect: the lasting-injury substitution mostly REMOVED shoulder-pattern slots rather than swapping in an alternative (Bench Press, DB Shoulder Press, Cable Lateral Raises, Cable Flyes, Close-Grip Lat Pulldown, Pull-Ups all show "→ (removed)") even on full_gym — may be legitimate (nothing safe fits those slots) or may be a replacement-candidate gap; not investigated further, flagging in case it's worth a look. Test profile created and fully cleaned up afterward.
- [ ] Item 2 (repeat instruction): base single-week audit grid also extended with bodyweight/gender (6 combos, matching the mesocycle sweep) — total combos 2367→13967, ~38s runtime (not the blow-up worried about). Failures 48→530. Breakdown: `load_cap` 0→706 (brand new — see below), `pattern_coverage` 46→276, `duration` 1→11, `rotation_relative_load` 0→3 (unchanged, mesocycle-sweep-only). Headline finding: `load_cap` checks against SAFETY_CEILING_KG, a CATEGORY-level backstop (isolation_chest 60kg, isolation_bicep 45kg, isolation_quad 90kg, carry 90kg, etc.) explicitly documented as "generous enough that a correct estimate for ANY profile should never approach them" — turns out they regularly ARE exceeded at 100-120kg male / 50kg female extremes, since that ceiling table was only ever checked against the one 80kg-male body the grid used to run. This is a DIFFERENT bug from the per-implement `loading_ceiling` mechanism reported below — that one is caught by a real clamp; this one is a category-level backstop that itself needs raising or rethinking for extreme bodyweights. Report-only, no fix, per the load-prescription plan-first rule.
- [ ] BUG, ROOT CAUSE CONFIRMED, NOT FIXED — real week-by-week reproduction added. Built a live trace (temporary instrumentation, fully reverted after) reproducing the exact 62kg-advanced-female mesocycle-safety-sweep combo. For THIS specific profile, Goblet Squats' own trajectory stayed sane (10→16kg over 16 weeks — its 0.35 squat-scale keeps the fresh-estimate ceiling low enough that the ramp never gets dangerous) — could not reproduce the cited 126-154kg figure for Goblet Squats specifically under the current code; if that number came from watching the app live, the exact profile/exercise would help chase it further. But the underlying mechanism is real and dangerous elsewhere: a weekly-ROTATING accessory slot (e.g. Romanian Deadlifts ↔ Good Mornings ↔ Kettlebell Swing (Heavy), all sharing one `lastUnverifiedLoadingWeekKg` tracker per SLOT, not per exercise) climbed a clean, unbroken +2kg-or-so-per-loading-week line for all 16 weeks regardless of which exercise was actually in the slot that week — confirmed empirically: 30→32→37.5→(deload)→42.5→47.5→48(clamped)→(deload)→50(clamped)→50(clamped)→55→(deload)→60→65→48(clamped)→(deload), for an 80kg advanced male. The ceiling clamp fired 4 times in that one slot alone. Answering the original question directly: this is mechanism (a), "progressive overload applying repeatedly without a reset at block boundaries" — confirmed definitively by the code (`Math.min(previous + step, freshEstimate)`, additive, never a percentage) and by every observed trajectory (clean, constant-kg steps, never a multiplicative pattern). Separately reproduced actual raw (pre-clamp) computed values reaching 108-118kg on 48-50kg-ceiling implements (Romanian Deadlifts, Kettlebell Swing) in the wider 96-combo sweep — all safely caught by the clamp before display, but confirms the raw numbers this mechanism can produce are exactly as dangerous as flagged. Report-only, no fix, per the load-prescription plan-first rule. Also still true from the categorize() angle: `n.includes('squat')`/`n.includes('deadlift')` fire before the later `'bulgarian'/'split squat'`/`'romanian'` checks, so Bulgarian Split Squats/Romanian Deadlifts get the FULL barbell standard instead of their scaled-down derived category — confirmed via `cat=deadlift` on Romanian Deadlifts in this round's trace too.
- [x] SUPERSEDED by the item above — earlier pass (16 fixed-80kg-male mesocycle-safety cases → 96 across 50-120kg/both genders) is now folded into the base-grid extension.
- [ ] Report request (blocked, needs explicit authorization or the user to run it): "New Plan" data-loss + production-profile-pattern audit. What's lost on New Plan: everything tied to the old profile_id becomes unreachable (not deleted) — logged sets/sessions (exercise_set_logs, workout_sessions), weigh-in history (daily_metrics), goals (user_goals), saved facts/context (user_facts), and any plan_adaptations — because New Plan inserts a brand-new fitness_profiles row rather than updating the existing one (confirmed: App.tsx's handleReset clears local state only; the actual regeneration happens in handleOnboardingComplete, which always `.insert()`s). The "~37 profiles, one person resetting repeatedly?" half of this couldn't be checked — a read-only production query was blocked by the permission classifier both as a direct HTTP call and would need `db:link-prod`-style explicit authorization to retry the same way the migration push did.
- [x] DECISION: Profile changes and regeneration — injury persistence fix, built. Part 1: chat now distinguishes a temporary niggle (propose_injury_adaptation, unchanged) from a lasting injury (new propose_injury_as_lasting) by listening for durational language, asked as one combined question. Part 2: propose_injury_as_lasting writes fitness_profiles.injuries for real (executeLastingInjury) AND substitutes the rest of the current program — the exact gap that let a lasting injury vanish on regeneration; new propose_injury_recovered removes it via chat (manual removal via the Profile screen already worked). Part 3: the New Plan confirm dialog now warns by name ("active shoulder adaptation running until Aug 20") when an unexpired plan_adaptations row would be silently orphaned. Verified mechanically end-to-end on TEST (add → confirms in profile.injuries → fresh regeneration for the same profile correctly avoids the joint → remove → mesocycle left untouched) — all three safety regression tests (test:injury-separation, test:plan-adaptations-separation, test:injury-adaptation-safety) still pass. Now ALSO verified live (see the entry above) — the model correctly chooses propose_injury_as_lasting vs the time-bounded tool from the new prompt wording, and both DB writes land as designed.
- [ ] Implement load-ceiling ordering by equipment tier — home_gym/minimalist adjustable dumbbells realistically max ~32kg (lower still for minimalist), not the 50kg full-gym ceiling now enforced flat across all tiers.
- [ ] AUDIT: load-prescription.ts routing that keys on exercise-name substrings rather than structured fields (categorize()'s "swing" → hinge_accessory is one confirmed case) — report every other substring rule and what it wrongly catches.
- [ ] AUDIT: every path that adds/replaces an exercise (swap, ban, injury adaptation, equipment adaptation, gap-fill, floor-clamp substitution) — matrix of path × which guards (primer, duplicate-family, etc.) it applies. Second bug of this exact shape found (applyReplacement missing primer awareness; substituteFloorClampedIsolation missing duplicate-family check earlier) — swap/adaptation paths look systematically less guarded than generation paths.
- [x] BUG: Audit every implement for a maximum realistic load, not just a floor. A dumbbell, kettlebell or machine has a real-world ceiling. — fixed: LOADING_CEILING_KG_PER_HAND_OR_TOTAL in load-prescription.ts, warns + clamps, gate check in dev-constraint-audit.ts (base plan + full mesocycle).
- [x] BUG: Primer prescribed 88kg Kettlebell Swings, labelled "Light — movement prep". No kettlebell exists above ~48kg. Check whether primers should carry load at all, and whether a bodyweight figure is leaking into implement load (88kg vs an 87kg profile — a lead, not a conclusion). — root cause: applyReplacement (mesocycle-edit.ts) had no primer guard on the live swap/ban path. Fixed. categorize()'s "swing"→hinge_accessory mismapping (the bodyweight-scaled magnitude source) tracked separately in the substring-routing audit item below, not fixed here since primers can no longer reach it.
- [x] DECISION: Silent recalculation — built. Targets now anchor to a 7-day rolling average (getEffectiveTargetWeightKg) and only move once it's shifted ≥1kg from the average that last set the target; a real move surfaces a one-time dismissible notice. Goal proximity: a dismissible ask ("keep going / hold here / new target?") when within 2kg of or past the stated goal weight — the calorie math itself is untouched, deliberately (ask-first, not auto-hold-at-maintenance). Migration (calculated_weight_kg column) applied to both TEST and PRODUCTION.

- [x] ~~PLAN THEN BUILD (item "2b", split out of the refusal-trap slice on 2026-08-17): bring TRAINING under the same absence rule as nutrition. `resolveParentOneRepMaxKg` (load-prescription.ts:844-846) computes `bodyweight * STRENGTH_STANDARDS_1RM_PER_BW[lift][gender][experience] * ageAdjustment`, with `profile.weight_kg || 75` and `profile.age || 30` — so a trainee who declines a weight gets every prescribed load derived from a fabricated 75kg body, and is never told. Mitigating (and why this was NOT rushed into the nutrition slice): for anyone without verified working lifts those loads are already treated as unverified and clamped by the calibration-week machinery, then corrected from what they actually lift — so training genuinely works, it just isn't honest about what it assumed. Ashley's ruling on the nutrition slice was explicitly (a) "keep weight out of training's honesty scope for now"; this is the deferred (b). It is a load-prescription change and gets its own plan before any build, per CLAUDE.md. Open question for that plan: whether the honest signal is a load shown as unanchored until calibration resolves it, or simply a stated assumption — the former is truthful but touches the load path, the latter is cheap but keeps a fabricated number on screen.~~ — done, see above.

- [x] MACRO AUDIT (item 1 of Ashley's four-item meal queue), and the one real finding in it. **Audited, found no outright bug**: `computeBMR` is Mifflin–St Jeor and correctly implemented; `DYNAMIC_CSCS` is internally consistent (0 mismatches across 324 day-types); the apparent calorie "overshoot" was rounding — only **1 of 303** cases exceeded 10 kcal and that one is the disclosed calorie floor engaging; and the hardcoded 2.2g/kg protein in dynamic mode is deliberate, with the split control disabled and a `disabledReason` saying so. Measuring before claiming is the whole point of that paragraph — three of those four looked like bugs until counted.
  The real finding was the **flat 500 kcal fat-loss deficit**. The same arithmetic for everyone is a very different diet for everyone: a 45kg woman training moderately was cut **28%** below maintenance and a 100kg active man **14%**, so the harshness fell entirely on lighter people. Put to Ashley because it changes what live users eat; she chose **scale it, capped at 500**. Built as `FAT_LOSS_DEFICIT_FRACTION = 0.20` with `FAT_LOSS_DEFICIT_CAP_KCAL = 500` in `applyGoalAdjustment`.
  **THE CAP IS THE SAFETY PROPERTY**, and it is what made this shippable to people already on a plan: anyone whose 20% exceeds 500 keeps the 500 they had, so no existing target can move DOWN. Measured across 672 bodies (14 weights x 4 activity levels x 4 ages x 3 heights): **0 targets cut deeper**, deficits over 25% of TDEE **129 -> 0**, pinned to the calorie floor **135 -> 65**, and 375 of 672 (56%) move, all upward. NOTE THE DENOMINATOR CHANGED — the earlier "6 of 36" figure swept weights x activity only; this one adds ages and heights, so the two counts are not comparable.
  Hypertrophy's +300 was deliberately NOT scaled. The same disproportion exists (17% of a 45kg woman's maintenance, 10% of a 100kg man's) but Ashley's ruling covered the cut, and a surplus that runs slightly large costs a little unwanted weight where a deficit that runs too deep costs muscle and adherence. Flagged in a comment at the site rather than changed silently.
  **The gate was a liar on its first draft and mutation-testing caught it.** `test:fat-loss-deficit` recomputed the target from the two exported constants instead of calling `applyGoalAdjustment`, so every section proved the gate agreed with itself and would have stayed green through any mutation of the function. `applyGoalAdjustment` is now exported and the gate calls it, plus a new §7 that ties it to what the Nutrition tab actually displays via `getMacroDerivation` — because sections 2-6 prove the function, not the app. Five mutations confirmed biting: flat-500 restored (§3 red), cap removed (§2 red), fraction deepened to 30% (§3 red), `computeStaticMacros` bypassing the function (§7 red), and a stray `tdee - 500` left elsewhere in the file (§7 red).
  `test:macro-split` §5 went red at 58kg and it was **the gate, not the change**: its "old hardcoded formula" baseline re-encoded `Math.max(1500, tdee - 500)`, making that section a second, undeclared assertion about the deficit while its actual subject is the SPLIT. It now takes its calorie target from `applyGoalAdjustment` and reallocates within it; re-mutation-tested (default protein 2.0 -> 2.1) to confirm it still bites. Only 58kg failed because the other three weights sit above the cap and genuinely did not move — the cap working, visible in a gate that wasn't looking for it.

- [x] "ADD THIS MEAL TO MY PLAN" (item 2 of the meal queue) — the confirmed missing capability, now built. The coach had `propose_meal_swap`, `log_meal`, `add_to_grocery_list` and `check_off_grocery_item` and **nothing that put a new meal into the plan**, so asking for one landed where every request the app can't honour lands: a warm reply and nothing else. Planned first (docs/PLAN-add-a-meal.md) because it opens a new route into what someone eats, and CLAUDE.md puts dietary enforcement in the always-plan-first set.
  **The whole design is one decision: it adds no verification of its own.** A user-named dish is text the model wrote; had it reached `meal_plan_slots` on its own path, the app would have a second, unguarded door onto the allergen path — the "constraint asserted at N paths, missed at N+1" shape this codebase keeps producing. So `buildMealAdditionProposal` routes the dish into `verifyProposal`, the exact function every generated meal already passes: food-DB resolution, the 80% coverage floor (**a dish the app can't measure is refused, not guessed at**), the disliked-foods filter, `validateMealAgainstDiet`, rescaling to the slot's macro budget, and a re-measure afterwards. One enforcement point. It lives client-side because `verifyProposal` is a `src/lib` module Deno can't import; `chat-gemini` is a courier for this kind and writes nothing.
  Deliberate shapes worth keeping: the payload carries the **verified** option so confirm writes exactly what the card showed and the model's own quantities never reach the database; the executor **never deletes** (`persistPools` wipes a slot's pool before inserting — right for a regenerate, catastrophic for an add: asking for one curry would have silently deleted the other four dinners); a failed pick rolls the pool write back rather than leaving a meal the receipt is about to say couldn't be added; and undo removes the option AND clears the pick, since an undo that only dropped the pick would leave the meal in the options forever.
  Refusals say **why**, translated from `verifyProposal`'s own reject log rather than replaced — "I can't add X, it clashes with what you avoid" beats "I couldn't add that", which is the reply this feature exists to stop the app giving.
  Decisions taken without asking, all mechanical or already-settled policy: the dish is **re-portioned to the slot budget** (not new policy — `scaleToTarget` already does it to every meal, and one exempt meal would hole the day-total guarantees); a dish that can't be made to fit is **refused with the reason** (consistent with generation); an added meal **joins the pool AND becomes that date's pick**, because a meal that silently joined a pool of five alternatives nobody opens is a feature that looks built and does nothing — both effects are named on the card and reversible.
  `test:meal-addition`, safety section first: for **every one of the 21 preferences that forbid tags**, a dish built around a food carrying that tag is refused, and refused AS A DIET CLASH — not incidentally on an earlier check. `mediterranean` is exempted **by its empty tag set, never by its name** (it is a documented style preference, diet-rules.ts:53); key the exemption on the name and the gate becomes one that asserts a real gap as correct, which is what the five untagged allergens had going for them until earlier this session. Give mediterranean a forbidden tag tomorrow and the loop starts demanding a fixture for it.
  Four mutations confirmed biting: dietary preferences not reaching `verifyProposal` (**21 checks red**), the payload carrying the model's raw ingredient strings (the stored-shape check, added after the first draft caught it only statically), scaling skipped so the model's portions survive, and the executor deleting the slot's pool before inserting.

- [x] BUG, found while scoping item 3 of the meal queue: **"swap this for something else" could hand back the meal just rejected.** `swapPoolMeal` picked uniformly at random from the alternatives, so with the default pool of five, saying "no, something else" had a **one-in-four chance** of returning the option turned down a moment earlier — and nothing distinguished that from the app ignoring the request. Now rotates from the current option's own `pool_index` position, so N-1 swaps show N-1 different meals and the order is stable across calls and reloads. Mechanical, so built without asking. `test:meal-swap-rotation` (offline, stubbed client — a swap rule this simple shouldn't need a live TEST project that pauses after a week idle). Mutation: putting the random draw back went red on every run, and "4 swaps produced 4 different meals" failed on 2 of 3 — the repeat rate, visible.
  Its `Math.random` text check strips comments before scanning: the rotation's comment quotes the draw it replaced, which is worth keeping and is not code. Caught by the check failing on its own explanation.

- [x] **CORRECTION to the entry above, and the real bug underneath it.** I reported the swap fix as user-visible. It was not: `chat-gemini` fetched the pool itself and passed `chooseName` on every call, so `swapPoolMeal`'s no-`chooseName` branch — the one carrying the random draw I "fixed" — **was unreachable from the app**. How I got it wrong: I read the random draw in `meal-store.ts`, saw `executeMealSwap` calling `swapPoolMeal`, and stopped, without tracing that the payload's `chooseName` had already been filled in server-side. The same shape is worth checking wherever a client function takes an optional "or pick one for me" argument that a server always supplies.
  **The live bug was worse.** The server picked `alternatives[0]` — the first pool option that isn't the current one — so chat swaps ping-ponged: A → B → A → B, forever. With `DEFAULT_POOL_SIZE = 5`, **three of every five generated meals were unreachable through the chat entirely**. And because the server always chose, the rule existed in two places with the live copy being the broken one — the N+1 shape again (two food databases, four places a dietary preference must be listed, and now two meal choosers).
  Fixed by removing the duplication rather than patching both: `nextPoolOption` in `meal-store.ts` is now the ONLY chooser, and `propose_meal_swap` became a courier like `propose_exercise_swap` and `propose_meal_addition` — the client rotates and builds the diff. An edge function cannot import `src/lib`, so a chooser living there is a second copy by construction; that is the argument for moving it, not tidiness. Gated: 4 chat swaps must reach 4 distinct meals and cover the whole pool, and the edge function must no longer read `meal_plan_slots` or filter the options at all. Reintroducing `alternatives[0]` drops it to **1 distinct meal reachable**.
- [x] ASHLEY'S RULING (asked, because generating costs money per use): when someone has been through all their options, **offer to find new ones** — "that's everything I've got for lunch, want me to find you some new ones?" — rather than always generating, or leaving it bounded. Built exactly that way: nothing is generated unless they confirm, and **their existing meals are kept**.
  The offer is a confirmable CARD, not plain text, deliberately: an offer with no `pending_actions` row means a later "yes" has nothing to resolve and goes back through the model, which can misclassify it again and loop — the trap already documented here for `record_fact`. `propose_meal_pool_refresh` resolves client-side.
  "They've seen the lot" is a fact, not a guess: `mealOptionsSeenRef` records every option the conversation actually put in front of them, per slot. A ref rather than state because the swap builder reads it inside the same async turn that writes it — with `setState` the offer would arrive one swap late. Per-conversation on purpose: someone returning a week later should be offered their own pool again before being asked to spend on new ones.
  `generateMealPools` gained `appendToExisting`, which routes to a **separate `appendPools`** rather than a flag inside `persistPools` — the difference between them is a DELETE, and a flag deciding whether a user's meals survive is one misread argument from wiping them. Existing options are loaded up front so the duplicate-name and cuisine-coherence checks see them, or "find me something new" could hand back the five already sitting there. Four mutations confirmed biting, including append-becomes-replace and the dedupe losing sight of the existing pool.

- [x] SOFT FOOD PREFERENCES ARE READ BY SOMETHING (item 4 of the meal queue, and the other half of the finding the exercise side already fixed). `compileSoftFoodPreferences` had **zero call sites**, so "I love salmon" was recorded, compiled, shown back in the memory screen, and read by nothing. VISION-ARCHITECTURE.md §1.2 had already named `assembleDay`'s candidate scoring as the consumer, so wiring it executes an existing decision rather than inventing one.
  **A lean, not a filter**, the same line the exercise half draws. A day that works in nothing they said they liked takes `SOFT_FOOD_MISS_PENALTY = 0.01` — the same magnitude as the existing day-to-day repeat penalty, and a fifth of what a 5% calorie miss already scores. Both options stay in the pool; hard dislikes are a different channel entirely (filtered out at generation time, never reaching here).
  **Where it stops mattering is measured, not asserted.** Calories carry weight 1.0 in `macroDistanceScore`, so on a 1200 kcal day the crossover is ~12 kcal: a liked meal **8 kcal** worse still wins, one **16 kcal** worse already loses. Gated at both sides of that boundary, because "doesn't win when 200 kcal worse" is an easy bar that proves nothing about a nutrition path.
  At least ONE liked thing per day, not as many as possible — someone who loves salmon wants salmon once, not at every meal.
  Matching deliberately covers the dish NAME as well as ingredients, which is **wider than the hard dislike filter** (ingredients only). The asymmetry is the point: a false positive on a nudge costs nothing, a false positive on a filter takes food off someone's plate they'd happily have eaten — and a like far more often names a dish ("I love a curry") than an ingredient, so ingredient-only matching would leave most likes unable to match anything.
  Threaded through the GROCERY path too, and that is a correctness fix rather than tidiness: `assembleDay` is deterministic, and the shopping list is built by assembling the same days the Nutrition tab shows. Give one the preferences and not the other and they assemble different days — a shopping list for meals the app never serves. Gated that App passes the identical value to both.
  Four mutations confirmed biting: the bias computed but never added to the score, the penalty cranked to 0.5 so a preference beats macro fit, name-matching dropped, and the grocery horizon quietly not passing them.
  `memoryFacts`' state declaration moved above the assembled-day derivation — it was declared after the line that now needs it. Moved, not duplicated.

- [x] DEVIATION, flagged unprompted: **I committed a regenerated `audit-report.txt`** in the soft-food-preferences commit. A background `test:audit` run rewrote the file inside the window where `git add -A` picked it up. PROJECT-LOG is unambiguous — "Never commit a regenerated report — `git checkout --` it back after running" — so it is reverted here to the committed snapshot.
  Worth recording *why* the convention exists, because the diff looked like good news and wasn't: the audit is **deliberately non-deterministic run-to-run** (PROJECT-LOG §"exercise-plan.ts uses Math.random() for exercise variety by design"), so the snapshot moving from 54 failures to 0 is not a measurement of anything. It is a snapshot for diffing *shape*, and it is meant to be compared by failure CATEGORY, never by exact count. Committing a regeneration turns a diagnostic into a claim.
  The live run is the number that counts, and it was taken this session: `test:audit` **0 / 13,967**, 71s.

- [x] **WEIGHTS THAT DON'T EXIST, defect 1: four routing rules were written and never reached.** Found in `test:quality`'s console output, not in any gate — a completed sweep logged **574,937** instances of `prescribeLoad`'s own warning: *"computed 78kg, above the 48kg realistic ceiling — clamping. This is a safety net, not a fix: something upstream produced a wrong number and should be traced."* Nobody had traced it. Plan: docs/PLAN-weights-that-do-not-exist.md.
  `categorize()` matches names by substring **in order**, and four rules sat AFTER the generic `deadlift`/`squat` matches — unreachable for any name containing those words. **Romanian Deadlifts** was priced as a full conventional deadlift, **Bulgarian Split Squats** and **Split Squat (Bodyweight)** as a bilateral barbell back squat, and **Farmer Squat Hold** as a back squat rather than a carry.
  `single_leg_dumbbell`'s own doc comment names *"Bulgarian split squat"* among the lifts it says it fixed. That fix only ever landed for lunges and step-ups — whose names happen not to contain "squat". Built in two halves, with a truthful-looking comment over the half that didn't land.
  **Why the clamp made it worse, not better:** a clamp produces a plausible figure, so nothing downstream complains — but once it engages the prescription **stops telling people apart**. Measured: 19 of 48 sampled profiles land on exactly the 48kg kettlebell ceiling, so a 55kg lifter and a 110kg lifter are handed the same bell.
  Measured at the fix, whole DB swept: **exactly 4 of 145** categories change, and the three cases where an earlier rule winning IS deliberate (`Goblet Squats`, `Hack Squat`, `Overhead Carry`) all hold. Loads: Romanian Deadlifts **47/48 profiles, median 0.56x**; Bulgarian Split Squats **48/48, median 0.40x** — a male intermediate at 85kg goes from **44kg to 18kg per hand**. 44kg in each hand for a Bulgarian split squat is not a conservative estimate needing a trim; it is a weight almost nobody should be handed.
  **The measurement caught something reasoning didn't:** a first attempt moved `carry` above the generic rules without keeping `overhead_carry` ahead of it, silently reclassifying `Overhead Carry`. That is why the four rules now live adjacent and commented rather than sorted by topic, and why the gate has a whole-DB trapdoor and not just named cases.
  `test:categorize-precedence`: the four intended routes, the three deliberate shadows, and a **generated** frozen snapshot of all 145 name→category pairs. The first draft of that snapshot was hand-typed and wrong on 15 of 20 categories — the gate caught its own author, which is the argument for freezing a measured state rather than a recollection of one. Mutations: restoring the original ordering fails 5 checks; the `carry`-before-`overhead_carry` near-miss fails 2.
  `test:audit` held at **0 / 13,967** — this changes load, not structure. Load-adjacent gates all green (`test:load-ceilings`, `test:ceiling-units`, `test:load-suggestions`, `test:added-load`, `test:block-consistency`, `test:week-load-consistency`, `test:pattern-tags`, `test:main-lift-rest`).
- [ ] **PRE-EXISTING RED GATE, not caused by this work: `test:injury-rebuild`.** Two checks fail deterministically (`slotsAfter: 436` vs `survivingAfterSub: 444` — the rebuild produces FEWER slots than plain substitution would leave, so "rebuild beats substitution" is false and "the two modes genuinely differ" reads the wrong way round). Verified by running it in a worktree at **694b750**, the commit this run started from: identical failure, identical numbers. Also identical with and without the categorize change. Deterministic across 3 runs, so **not** the unseeded-RNG flake this gate has had before. Injury filtering is safety-adjacent and gets its own plan.
- [x] CORRECTION: the committed `quality-report.txt` was **stale**. The live sweep gives **11.54 / 12**, not the 11.04 in the file, and Time fit is **1.92** rather than 1.53 — Progression (1.75) is the weakest dimension, not Time fit. An option put to Ashley was described using the stale file's numbers. How I got it wrong: I read the on-disk artifact instead of waiting for the run I had myself started, having already noted in the same breath that the artifact predated the current code. The same shape is worth watching wherever a committed report is read as if it were a measurement.

- [x] **WEIGHTS THAT DON'T EXIST, defect 2: kettlebell swings get their own anchor.** `hinge_accessory` was a single `0.55 x deadlift 1RM` bucket holding barbell Good Mornings, Hip Thrust, RDLs, several bodyweight drills — and kettlebell swings, whose implement stops at **48kg**. Result: an 85kg intermediate man was put on the heaviest bell in the gym, an 85kg beginner on 32kg, and **19 of 48 sampled profiles landed on exactly 48kg** — the prescription had stopped telling people apart.
  ASHLEY'S RULING, taken on the resulting weights rather than on a multiplier (it changes what real users lift): lighter, calibrated for general fitness. An 85kg man doing sets of 15 now gets **32kg advanced / 26kg intermediate / 14kg starting out**; a 60kg woman **12kg**; **0 of 48** pinned to the ceiling.
  **Split, not re-scaled** — and the mutation test is the argument: dropping the shared bucket's multiplier instead takes Good Mornings and Hip Thrust from 82.5kg to **32.5kg**. New `kettlebell_swing` category, gated on EQUIPMENT rather than name so `Leg Swings` — a warm-up mobility drill that had been sitting in the hinge bucket purely because its name contains "swing" — leaves too and reads "Bodyweight".
  Three mutations confirmed biting: swings back in the shared bucket (7 checks red, including all four of Ashley's numbers), the bucket re-scaled instead of split (Good Mornings and Hip Thrust move), and the swing scale nudged 0.22 -> 0.26 (5 red — her numbers are pinned as numbers, not as a formula).
  `test:audit` held at **0 / 13,967**. All load-adjacent gates green.
- [x] CORRECTION to my own plan doc: I listed **Hip Thrust being under-prescribed** as a defect. It isn't — `exercise-db.ts` documents it at the entry: it falls through to the `hip_hinge` default and *"under-prescribes. That is the safe direction and it is left deliberately conservative — a trainee adding plates is a better failure than a trainee pinned under them on the first session."* How I got it wrong: I read the number off a sweep and reasoned about it from coaching knowledge without opening the exercise entry's own comment — the exact thing PROJECT-LOG's "read a file's own header comment before assuming its contract" warns about, one level down at the data-row level.

- [x] **THE ARTIFACT RULE IS NOW ENFORCED, NOT REMEMBERED.** PROJECT-LOG has said *"never commit a regenerated report"* for a long time, and I violated it **twice in one session** — once with `audit-report.txt` (`3754730`, reverted in `8ea8934`) and again with `quality-report.txt` (`6d7378d`, which reached main). Both times identically: a background `test:audit`/`test:quality` sweep rewrote the file inside the window where `git add -A` picked it up. The second one happened *after* I had written the first up as a lesson, which is the whole argument — a sweep finishing at an unlucky moment beats anyone's memory, so the fix cannot be "be careful".
  `.githooks/pre-commit` now refuses any commit that stages `audit-report.txt`, `quality-report.txt`, `meal-quality-report.txt`, `differentiation-audit-report.txt` or `tsconfig.tsbuildinfo`, and prints the exact un-stage commands. A `prepare` script wires `core.hooksPath` on `npm install` so it installs itself. It **blocks rather than silently un-stages**: quietly dropping a staged file would make `git add -A && git commit` not commit what it said it did, which is a worse surprise than a readable error. `--no-verify` remains for a deliberate snapshot update.
  Also: **`meal-quality-report.txt` was never tracked before this session** (PROJECT-LOG §"Discard `meal-quality-report.txt` after — it was never committed"). It was swept in by the same mechanism in `dbc4c3a`. Now untracked and gitignored. The other two stay tracked — they are committed snapshots whose *regenerations* must not be, which is why they can't just be gitignored and need the hook instead.
- [x] **FINAL MEASUREMENT of the impossible-weights work**, same 9,216-plan sweep throughout so the numbers are comparable. Clamp warnings **574,937 -> 346,020 (defect 1) -> 136,093 (defect 2)**: a **76% reduction**. Everything remaining is calf raises — `Single-Leg Dumbbell Calf Raise` (90,854, computes 50kg against a 48kg ceiling) and `Calf Raises`/`Seated Calf Raises` (45,239, 102.5kg against 100kg). Those are MARGINAL overshoots, a different shape from the ones fixed here, where the computed number was 1.5-2x the implement's ceiling.
  Plan quality **11.54 -> 11.55 / 12** and `test:audit` **0 / 13,967** both held — this changed load without disturbing structure, which is what it was supposed to do.

- [x] **DEPLOYED TO PRODUCTION, 27 Aug 2026** — verified from the CLI output, not assumed. `chat-gemini`, `generate-meals`, `onboarding-chat` and `macro-calibration` all deployed to `sdkhuczcfnqqimdgfiks`, CLI relinked to TEST afterwards. `macro-calibration` was included because `_shared/food-db.ts` changed in the allergen work and gets bundled into every function importing it — its own file was untouched, so it would have shipped a stale food database if deployed by "what changed" alone.
  Migrations: `npm run db:push-both` reported **"Remote database is up to date"** on both TEST and PRODUCTION. So the whole "inert until two commands run" item above had gone stale — the migration half was already applied. Recorded because a backlog item asserting undone work is as misleading as a comment over dead code.
  **Still not measured against the real model**: the tone probes (`probe-coach-tone.mts`, `probe-onboarding-tone.mts`) and `test:meal-quality`. These call the DEPLOYED function, and `.env.local` points at PRODUCTION — so running them as-is would create profile rows on the live database, which CLAUDE.md forbids. They need pointing at TEST before anyone runs them; that is the next piece of work on this, not a command to hand over.

- [x] **THE INTRO NOW COMES FIRST** (Ashley, on a real phone): *"the introduction explaining what the app can do needs to be the very first message on the onboarding."* She was right and the gap was structural — `buildFirstRunIntro` fires in the MAIN CHAT, after a plan exists. So the four messages explaining what the app does were shown to someone who had already been through the entire interview, and a brand-new user's genuine first message was the interview opener: it explained the QUESTIONS ("I want to get to know you a bit") and never once said what the app was for. You were asked your name by something that had never introduced itself.
  `buildOnboardingIntro` now supplies onboarding's first four messages — who I am, what I build, how you use it, then the name ask LAST. Every claim maps to something that works (plan generation, injury/equipment adaptation, natural-language logging, `user_facts` memory, propose-then-confirm); no feature list, and nothing the coach would then decline.
  **`render:screens` earned its keep again.** The first draft ran to two EIGHT-LINE blocks and pushed the actual question BELOW THE FOLD before the user had typed a word — the same wall-of-text failure the main chat's intro hit, found the same way, by looking at it at 412px instead of counting words. Rewritten to four short messages that fit on one screen with the composer visible. A new `onboarding-intro` screen renders the REAL strings (imported, not retyped) and the two onboarding replicas now share one shell rather than duplicating the markup.
  Gate: `test:onboarding-conversational` §5b rewritten. It used to scan the component for a literal opener string — which broke the moment the copy moved into a module, because it was checking WHERE the words lived rather than what they SAID. It now imports `buildOnboardingIntro` and asserts the name ask is last, that something before it names training and food and keeping up with change, the say-so promise, no offer of tapping, no chips, and a **170-character-per-message ceiling** (~6 lines at 412px) so the wall of text cannot come back. Four mutations confirmed biting: name ask moved first, the wall restored, "or tap an option" reintroduced, and the component hand-rolling the opener again.
- [x] **A GATE I BROKE THIS MORNING AND NEVER RAN: `test:option-icons`.** It froze the dietary option list at 17 values; the allergen work took it to 22, so it went red at `dbc4c3a` and stayed red. Nothing caught it because I kept running hand-picked subsets of gates rather than the suite. Re-frozen at 22 with the reason recorded, and strengthened while I was there: the option list must now also match `DIETARY_PREFERENCES` in both directions, so an option nothing enforces — exactly the state those five allergens were in — fails here too.
  **Corrected the habit, not just the gate.** All 67 non-sweep gates now run: **65 pass, 2 fail, both pre-existing.** `test:injury-rebuild` and `test:band-slots` (`rehab-indicated bands still reach main slots: 318, floor 335`) both reproduce identically in a worktree at `694b750`, the commit this run started from.

- [x] **TONE: the onboarding coach reads cold and clipped** (Ashley, from a real session; she picked that description from four options rather than me guessing at it). Diagnosed rather than assumed: the voice block opened with ONE word of warmth — "Warm" — followed immediately by four length constraints and then THIRTEEN prohibitions. A coach assembled purely out of things-not-to-do comes out as bare question, bare question, bare question. The anti-grading rule in particular (added for a real bug she found — "That 12% target is a classic, sharp goal to aim for") told the model not to comment on routine answers, and combined with NO STOCK CLOSERS and a 2-4 sentence cap, left nothing between the questions.
  New `=== WHAT WARM ACTUALLY SOUNDS LIKE ===` block placed BEFORE the prohibitions, on the principle that the model should read what it IS before what to avoid. Its load-bearing claim: **warmth is not a compliment bolted onto a question, it lives in HOW you ask** — "Three days and coming back from a year out, how long have you realistically got on those days?" is not a comment on their answer, it is proof of listening. Plus taking their side out loud when something sounds hard, contractions and asides, curiosity as warmth, and a self-test: if your last three turns would work word-for-word for a different person, you are being a form again. The anti-grading ban is untouched.
  **THIS IS SAFE TO ATTEMPT NOW IN A WAY IT WAS NOT BEFORE.** `fa683fc` was a tone rewrite that fixed grading 7/7 -> 0/7 and silenced the model on 4 of 7 turns, because "react every turn" turned out to be load-bearing for RELIABILITY, not voice. That load now sits in `reply-resolver.ts` (`resolveReply`), gate-tested against every measured failure shape — so the prompt is free to stop demanding a reaction without the turn coming back empty. `test:reply-guarantee` green.
  **STILL UNMEASURED, and that is the honest state.** No tone change in this project has ever been measured against the real model. The before/after is now set up rather than promised: run `probe-onboarding-tone.mts` BEFORE deploying to capture the cold baseline, deploy, run it again.
- [x] **CORRECTION, and it was blocking the measurement: I told Ashley the tone probes would create profile rows on production.** False for the onboarding one. `onboarding-chat` has **NO database writes at all** — its only outbound POST is to the Gemini API. So `probe-onboarding-tone.mts` is safe against production today; it costs Gemini calls and writes nothing.
  How I got it wrong: I generalised from `chat-gemini`, which genuinely does write (`workout_sessions`, `exercise_set_logs`), to `onboarding-chat`, and inferred a write path instead of grepping for one. The probe's own header says "It creates no profile and writes nothing to any database" and I did not read it — the same "read the file's own comment before assuming its contract" failure as the Hip Thrust one, twice in a day.
  The split that actually holds: **`probe-onboarding-tone.mts` is production-safe; `probe-coach-tone.mts` is not** and still needs pointing at TEST, because chat-gemini writes.

- [x] **THE OPENER IS NOW ASHLEY'S, VERBATIM.** She supplied draft copy and iterated across three rounds: the intro must explain what the app can do; no question count ("don't say dozen questions" — a number makes the interview sound like a form with a length, where the topic list sets the expectation without a countdown); and, shown three complete candidates as previews, she rejected the capabilities-first five-message version outright ("i don't like it") and picked the one closest to her own draft — her flow (get to know you first, then what gets built), abilities in one plain sentence in the middle. Three messages: *"Hi — I'm your personal trainer. I want to get to know you a bit, then build your training and your food around what you tell me." / "After that you just talk to me — I'll log your workouts, swap what isn't working, plan around injuries, and remember what matters. Nothing changes without your okay." / "I'll ask about your goals, what you've got to train with, what your week actually looks like — and you can ask me anything as we go. First: what should I call you?"*
  The constants' comment now records that the words are hers **as a constraint, not a credit** — editorial "improvements" are how her decision gets quietly unmade. Every claim still maps to a shipped mechanism (log → NL logging, swap → proposals, injuries → adaptation path, remember → user_facts, okay → confirm cards, ask anything → the chat answers questions).
  Gate §5b: say-so regex widened to accept "without your okay" (the gate serves the approved copy, not the other way round), and a new check that the pre-name messages name the LOG capability — "explains what the app can do" is a product requirement now, and logging is what turns "talk to me" from slogan into mechanic. Mutations: stripping message 2 fails 3 checks; softening "okay" out of the promise fails 3. `render:screens` verified at 412px: three messages, question above the fold, composer visible with room.
  Client-side copy only — ships with the Vercel push, **no edge-function deploy needed** (the separate tone-prompt change still needs `npx supabase functions deploy onboarding-chat`).

- [x] **TONE CAN NOW BE MEASURED, not just asserted.** Every tone claim in this project's history — mine included — has been reasoning about a prompt, or a gate run against a scripted fake model. Ashley said the onboarding coach reads "cold and clipped"; the honest answer to that is a before/after on the DEPLOYED function, not a better-sounding prompt. `scripts/compare-tone.mts` (`npm run tone:compare -- before.json after.json`) reads two probe transcripts and puts numbers on it. Free to run — it reads JSON, no API calls, unlike the probe that produces the files.
  **What "cold and clipped" IS, made countable**: the signature is a turn that is nothing but a question. So: bare-question rate, sentences and words per turn, whether the reply reuses a distinctive word the user just said (the prompt's core warmth mechanic), contractions per turn, opener variety, chip rate.
  **Two metrics are guard rails, not warmth**, and they are why this exists rather than a vibe check. **Empty replies must stay 0** — `fa683fc` was a tone rewrite that fixed grading 7/7 → 0/7 and silenced the model on 4 of 7 turns, which is the exact failure mode of the change now queued. **Grading must stay 0** — the anti-appraisal rule was added for a real bug Ashley hit ("That 12% target is a classic, sharp goal to aim for"); warmth returning as compliments is not the fix, it is the old bug wearing the new one's clothes. Either breaking prints a STOP line naming the remedy.
  Validated against three synthetic fixtures with known answers rather than assumed correct: a deliberately cold transcript (100% bare questions, 0% echo) vs a warm one (0% bare, 60% echo, contractions up) moves every metric the right way; a **regression fixture** — warm-sounding but with grading back and one silent turn — is caught, 4 grading hits and 1 empty reply, STOP fires.
  Prints the sample size and what one turn is worth in percentage points (the persona is 9 turns, so ~11pp), because a 9-turn sample cannot support fine distinctions, and ends by saying outright that the numbers cannot tell you whether it sounds like a person — they say where to look.

- [x] **THE INJURY SUBSTITUTION WAS REPEATING ITSELF** (item 1 of three Ashley asked for in order). Chasing `test:injury-rebuild`'s two red assertions — "rebuild must keep more slots than substitution", failing 436 vs 444. The obvious reading is that the rebuild regressed. **It hadn't.** The loss was *spread, not concentrated*: 80 sessions in both, 16 gained a slot, 28 lost one, deload weeks untouched. Nothing failed to fill.
  The structural comparison is what the count was standing in for, and it inverted the conclusion: original 448 slots / 62 distinct / **0** duplicates; substituted 444 / 45 / **28 duplicates within a single session**; rebuilt 436 / 49 / **0**. A real substituted session, printed rather than counted: `Band Dislocates | Barbell Floor Press | Landmine Press | Landmine Press | Tricep Pushdowns | Side Plank | Barbell Floor Press` — seven "exercises", four movements. **Substitution's 444 was inflated by duplicates**, so an honest rebuild was being compared against a padded number.
  The defect: `substituteSlots` HAD a duplicate guard that could not work. It read the ORIGINAL `day.exercises` (so it only knew names already there, never ones chosen this pass) and every slot resolved inside `Promise.all` (so no slot could see another's choice). Two conflicting slots got the same ranked list and both took `candidates[0]`. The comment above it explaining the filtering gave false confidence over a mechanism that never ran — this codebase's signature defect, again.
  Fixed by sequencing **within a day** and accumulating chosen names; weeks and days stay parallel because that is the only place the collision occurs. Swept 96 profiles (4 equipment × 3 experience × 8 injuries): **224 duplicate placements → 0**.
  **The count assertions were RIGHT all along** and now pass on their own terms — substitution 444 → 432, rebuild unchanged at 436. No gate rewrite needed; the gate was vindicated. Added `[2b]` naming the real defect, so the next failure points at the duplicate rather than blaming the rebuild. Mutation: restoring the parallel form reproduces the exact original failure AND fails the new check.
  **The risk I flagged did not fire, and I measured rather than assumed:** dropping instead of repeating raises `dropped` 4 → 16 and `planLossRatio` 0.9% → 3.6%, which feeds `assessAdaptation`'s `shouldRebuild` threshold (15%). Across 96 profiles `shouldRebuild` stayed at **18/96, unchanged** — nobody is offered a different mode. Plan: `docs/PLAN-substitution-repeats-itself.md`.
- [x] **CALF RAISES: the ceiling was wrong, not the load** (item 2 — the last of the impossible weights). 45,239 clamp warnings, all 102.5kg against the generic 100kg `stack` ceiling. **A dedicated calf machine is not a cable tower** — it is plate-loaded or has its own stack and takes 150-200kg, and calves are genuinely strong. Cutting the formula to fit a ceiling never meant for that machine would have under-prescribed a whole muscle group to silence a warning. `CALF_MACHINE_CEILING_KG = 250`, following the `LEG_PRESS_CEILING_KG` precedent — a named exception with its reasoning, gated on loading mode so `Single-Leg Dumbbell Calf Raise` keeps its **correct** 48kg clamp (48kg really is the heaviest dumbbell most gyms own — the honest kind of clamp).
  ASHLEY'S RULING, put to her as weights rather than a number: lift the cap. The old 100kg cap flattened **every rep bracket to the same figure** for a 120kg advanced lifter — heavy triples and sets of twenty alike — the identical "stopped telling people apart" failure the kettlebell ceiling produced. 27 of 200 cases (14%) were capped; nobody lighter or newer changes. Top end 182.5kg ≈ 1.5× bodyweight.
  `SAFETY_CEILING_KG_TOTAL.isolation_calf` 140 → 230: it had been calibrated when the implement clamp held calf work at 100kg, so it was never exercised above that and only appeared consistent because the tighter limit bound first.
  **MY MEASUREMENTS FOR THIS ITEM WERE WRONG TWICE AND A GATE CAUGHT IT.** I called `prescribeLoad` with `reps`/`intensity` instead of `repRangeLabel`/`targetRpeLabel`, so it silently used defaults — every calf figure I quoted before `test:ceiling-units` failed was measured with the wrong API. Correct call: the load *does* scale by rep range (132.5 → 182.5kg), which is exactly what the cap was flattening. Wrong keys on an options object is a silent-default failure worth watching for; the gate disagreeing with me is what surfaced it.
  Mutation found a real gap: pointing the exception at the whole `isolation_calf` category — handing a dumbbell exercise a 250kg ceiling — **passed every gate**. Nothing asserted that a dumbbell lift gets a dumbbell-sized ceiling. New check: a named ceiling exception may only ever apply to `stack` mode, since the other four modes are physical facts about the implement. It now catches that mutation.
- [x] **The tone comparator would have reported a fabricated zero** (item 3). `compare-tone.mts` reads `turns[].actions` (onboarding shape); `probe-coach-tone.mts` writes `turns[].action` — singular, nullable, no array. It would not crash; it would confidently print "0 chip turns" forever. A fabricated zero that looks like a measurement is worse than an absent one, so the shape is now detected and inapplicable metrics are **omitted rather than zeroed** (chips are an onboarding concept — the main chat has no `present_slot`). Comparing a coach transcript against an onboarding one is refused outright: different probes, different conversations. Verified against both shapes plus the regression fixture, which still fires STOP.
  **CORRECTION, and it is the second of its kind today: `probe-coach-tone.mts` does NOT write to the live database.** I told Ashley it did and that it needed pointing at TEST first. It sends **no `profile_id`**, and every write in `chat-gemini` is guarded on `if (profileId)`. Both probes are production-safe. Both files say so in their own header comment. Same failure as the Hip Thrust call and the onboarding probe: reasoning about a file instead of reading what it says about itself — **three times in one day**, which makes it a habit rather than three incidents. Read the header first.
- [ ] STILL RED, pre-existing and untouched by this work: `test:band-slots` — rehab-band placements in main slots at 318 against a floor of 335. Property checks all pass (48 of 48 knee-injured profiles still receive a knee-indicated movement), so no rehab is lost; the count proxy has drifted a third time (348 → 340 → 318). Reproduces identically at `694b750`. Needs root-causing rather than a third floor adjustment.

- [x] **THREE OF THE EIGHT INJURIES DID NOTHING AT ALL.** Found by generating two plans and diffing: a plan for someone reporting a bad **hip, elbow or ankle** came back BYTE-FOR-BYTE IDENTICAL to a healthy person's. A hip-injured user was still handed **Deadlifts**. The app collected the injury, stored it, showed it back on the profile screen, and threw it away — the one place it silently discards safety information a user deliberately gave it, made worse by knees and shoulders working properly so nobody would suspect.
  **It was known and written down.** `getFlaggedJoints`' own doc comment read *"Only 5 of the 8 INJURY_OPTIONS codes are mapped — hips/ankles/elbows are collected at onboarding but currently have no joint tag to filter on."* Nothing failed while that was true.
  Not a one-line map fix: the `hip` tag sat on 1 exercise and `elbow` on 2, `ankle` did not exist. **Coverage and effect are different claims** — mapping `hips: ['hip']` alone would have satisfied "is it mapped?" while still changing nothing. 30 catalogue entries tagged with `contraindicated_joints`, per the shoulder audit's rule (*"`loads_joints` answers 'does this joint participate?', and it is being used to answer 'is this dangerous?'"*).
  ASHLEY'S RULING on hips set the calibration and transfers to the other two: **drop the heavy loaded work, keep the controlled movements, the session stays whole.** Hip drops barbell squats/deadlifts/RDLs/good mornings/swings/walking lunges/Bulgarian split squats and KEEPS leg press, goblet squats, hip thrusts, glute bridges, step-ups, all curls/extensions/calves. Verified: **hip wipes no movement pattern at any of the four equipment tiers.** All eight injuries now visibly change a plan.
  **I INTRODUCED A SERIOUS BUG DOING THIS AND A GATE CAUGHT IT.** `contraindicatedJoints()` is `contraindicated_joints ?? loads_joints`, so writing that field AT ALL replaces the `loads_joints` fallback instead of adding to it. Tagging Barbell Squats for the hip therefore **un-banned it for the knee**. Measured damage: knees 25 → 15, wrists 21 → 13, lower_back 12 → 5, shoulders 47 → 40 — four existing injuries silently weakened while the new three looked like a success. Surfaced by `test:band-slots` moving 318 → 221; I chased the number instead of assuming it was noise, which is the only reason it was caught. Fixed by merging `loads_joints` into the tag (22 entries needed the carry-over); all five pre-existing counts restored exactly and band-slots returned to its pre-existing 318.
  `test:injury-coverage`: §1 every code maps to a joint, §2 every code visibly changes a plan (the two are different, and §1 alone would have passed the old bug), §3 **the shoulder lesson** — no exercise may be both rehab FOR a joint and banned for it, §4 a frozen wipe list **verified pre-existing by computing it at the pre-change state and diffing** (all shoulder/neck/wrist wipes predate this; the only five new ones are ankle→calves, which is CORRECT, plus content gaps where no safe variant exists), §5 **frozen per-injury removal counts** — added after a mutation took a hip-injured trainee's squat options from 8 to 2 with every other check green, because over-broad tagging that stops short of wiping a pattern is otherwise invisible.
  Four mutations bite: hips unmapped (2 red), mapped-but-untagged (§2 red — coverage without effect), over-broad hip tag (§5 red), and the override bug reintroduced (§4 and §5 red, naming all four weakened injuries). `test:audit` held at 0 / 13,967.
